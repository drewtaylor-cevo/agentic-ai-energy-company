---
phase: 13-bill-shock-multi-tool-flow-agent-01
plan: 06
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - ui/src/lib/types.ts
  - ui/src/lib/mock/recommendations.ts
  - ui/src/components/ReasoningTrace.tsx
  - ui/src/components/ReasoningTrace.test.tsx
  - ui/src/App.tsx
autonomous: true
requirements:
  - AGENT-01
tags:
  - ui-component
  - reasoning-trace
  - narrative-off-kill-switch
  - ui-01-above-fold
  - mock-sync
user_setup: []

must_haves:
  truths:
    - "`ui/src/lib/types.ts` exports `ReasoningTraceEntry { tool: string; summary: string; }` and extends `RecommendationResponse` with optional `reasoning_trace?: ReasoningTraceEntry[]` (snake_case preserved — Phase 8 D-18)."
    - "`ui/src/components/ReasoningTrace.tsx` renders `null` when (a) the prop is empty/undefined, OR (b) `NARRATIVE_ENABLED === false` from `@/lib/flags` — LD-7 single-flag contract (D-27)."
    - "Collapsed state renders a single row: `▶ N steps: tool_a → tool_b → tool_c` with tool NAMES ONLY (NO dollar amounts, NO digits, NO dates in the collapsed copy) — UI-01 vertical budget preserved."
    - "Expanded state renders a numbered ordered list of `entry.summary` strings below the disclosure row."
    - "`ui/src/components/ReasoningTrace.test.tsx` covers all 6 vitest cases from D-30: empty → null; 3-entry collapsed; click expands; `?narrative=off` + non-empty → null; `?narrative=off` + empty → null; 1-entry collapsed."
    - "`ui/src/lib/mock/recommendations.ts` adds `MOCK_REASONING_TRACE_CUST003` (3 entries: get_hardship_flag → detect_bill_shock → simulate_savings) BYTE-EXACT with the Python formatter outputs from `agent/reasoning/summaries.py`; CUST-001/CUST-002/CUST-003 mock entries gain `reasoning_trace: []` or the populated trace."
    - "`ui/src/App.tsx` inserts `<ReasoningTrace trace={state.data.reasoning_trace ?? []} />` BEFORE the card grid in the `success` branch (D-28)."
    - "File header comment of `ui/src/lib/mock/recommendations.ts` adds a new byte-sync target line: 'Values MUST stay in sync with `agent/reasoning/summaries.py` formatters'."
  artifacts:
    - path: "ui/src/components/ReasoningTrace.tsx"
      provides: "Collapsed-by-default disclosure component — empty-list short-circuit + LD-7 kill-switch short-circuit"
      contains: "export function ReasoningTrace"
    - path: "ui/src/components/ReasoningTrace.test.tsx"
      provides: "6 vitest cases covering D-30 + regression guard for kill-switch module-load semantics"
      contains: "describe\\('ReasoningTrace"
    - path: "ui/src/lib/types.ts"
      provides: "`ReasoningTraceEntry` interface + optional `reasoning_trace` field on `RecommendationResponse`"
      contains: "export interface ReasoningTraceEntry"
    - path: "ui/src/lib/mock/recommendations.ts"
      provides: "`MOCK_REASONING_TRACE_CUST003` 3-entry trace; `reasoning_trace: []` on non-bill-shock mocks; updated sync-target header comment"
      contains: "MOCK_REASONING_TRACE_CUST003"
    - path: "ui/src/App.tsx"
      provides: "`<ReasoningTrace>` insertion above the 2-col card grid in the `success` branch"
      contains: "<ReasoningTrace"
  key_links:
    - from: "ui/src/components/ReasoningTrace.tsx"
      to: "ui/src/lib/flags.ts::NARRATIVE_ENABLED"
      via: "`import { NARRATIVE_ENABLED } from '@/lib/flags'; if (!NARRATIVE_ENABLED) return null;`"
      pattern: "if \\(!NARRATIVE_ENABLED\\) return null"
    - from: "ui/src/components/ReasoningTrace.tsx"
      to: "ui/src/lib/types.ts::ReasoningTraceEntry"
      via: "`import type { ReasoningTraceEntry } from '@/lib/types';`"
      pattern: "ReasoningTraceEntry\\[\\]"
    - from: "ui/src/App.tsx::success branch"
      to: "ui/src/components/ReasoningTrace.tsx"
      via: "`<ReasoningTrace trace={state.data.reasoning_trace ?? []} />` inserted before the card grid"
      pattern: "<ReasoningTrace trace="
---

<objective>
Ship the UI surface for AGENT-01's reasoning trace:
- New type `ReasoningTraceEntry` + extended `RecommendationResponse.reasoning_trace?` field in `ui/src/lib/types.ts` (snake_case wire format preserved).
- New component `ui/src/components/ReasoningTrace.tsx` — collapsed-by-default disclosure, empty-list short-circuit, `?narrative=off` kill-switch short-circuit (LD-7 D-27 single-flag contract).
- New `ui/src/components/ReasoningTrace.test.tsx` with all 6 D-30 vitest cases — kill-switch cases use the LOAD-BEARING `vi.stubGlobal('location', { search: '?narrative=off' })` + `vi.resetModules()` + dynamic `await import('./ReasoningTrace')` idiom (because `flags.ts` evaluates `URLSearchParams` at module-load).
- Update `ui/src/lib/mock/recommendations.ts` with `MOCK_REASONING_TRACE_CUST003` BYTE-EXACT to Python formatter outputs (per amendment A-01 — Elena, NOT CUST-002).
- Insert `<ReasoningTrace>` into `ui/src/App.tsx`'s `success` branch above the 2-column card grid (D-28).

Purpose: UI-01 preserved at 1280×800 (collapsed trace = 1 row, zero cost for single-tool personas via null render); UI-02 preserved (component is purely display, no new network calls); LD-7 kill-switch extended from v2.0 byte-equivalence to cover the new v3.0 surface; emergency `npm run build:mock` path keeps working.

Output:
  - `ui/src/lib/types.ts` — new interface + extended response type.
  - `ui/src/lib/mock/recommendations.ts` — byte-exact trace on CUST-003, `reasoning_trace: []` on others, updated header sync-target comment.
  - `ui/src/components/ReasoningTrace.tsx` — NEW file.
  - `ui/src/components/ReasoningTrace.test.tsx` — NEW file.
  - `ui/src/App.tsx` — 1-line import + 1-line JSX insert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md
@.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-RESEARCH.md
@.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md
@.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From ui/src/lib/types.ts (current — per 13-PATTERNS.md lines 672-689) -->
export interface TrackInfo {
  plan_id: string;
  plan_name: string;
  saving_monthly: number;
  saving_annual: number;
  usage_narrative: string;
  call_script: string;
}
export interface RecommendationResponse {
  green: TrackInfo;
  cheapest: TrackInfo;
}

<!-- From ui/src/lib/flags.ts (existing — verified) -->
export const NARRATIVE_ENABLED =
  new URLSearchParams(window.location.search).get('narrative') !== 'off';

<!-- From ui/src/components/RecommendationCard.tsx (structural analog per 13-PATTERNS.md lines 502-541) -->
import { NARRATIVE_ENABLED } from '@/lib/flags';
import type { TrackInfo } from '@/lib/types';

interface RecommendationCardProps {
  track: 'green' | 'cheapest';
  data: TrackInfo;
}

export function RecommendationCard({ track, data }: RecommendationCardProps) {
  const config = TRACK_CONFIG[track];
  // ...Card with Icon header, narrative + call_script conditionally rendered based on NARRATIVE_ENABLED
}

<!-- From ui/src/components/RecommendationCard.test.tsx kill-switch stub pattern (lines 13-31, 119-148) -->
beforeEach(() => {
  vi.restoreAllMocks();
  vi.unstubAllGlobals();
  vi.resetModules();
});

// Kill-switch test:
vi.stubGlobal('location', { search: '?narrative=off' } as Location);
vi.resetModules();
const { RecommendationCard } = await import('./RecommendationCard');

<!-- From ui/src/App.tsx success branch (lines 63-69) -->
{state.status === 'success' && (
  <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
    {/* Card order stable: Green first, Cheapest second. */}
    <RecommendationCard track="green" data={state.data.green} />
    <RecommendationCard track="cheapest" data={state.data.cheapest} />
  </div>
)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Extend `ui/src/lib/types.ts` with `ReasoningTraceEntry` + optional `reasoning_trace` field</name>
  <files>ui/src/lib/types.ts</files>
  <read_first>
    - ui/src/lib/types.ts (entire file — 17 lines)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md §"ui/src/lib/types.ts" (lines 666-705)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md §"D-07" (line 96-106 — Pydantic parallel)
  </read_first>
  <action>
APPEND `ReasoningTraceEntry` interface AND add the optional `reasoning_trace?` field to the existing `RecommendationResponse`. Preserve snake_case (Phase 8 D-18 invariant — matches JSON wire format). Do NOT camelCase.

Target final state:

```typescript
export interface TrackInfo {
  plan_id: string;
  plan_name: string;
  saving_monthly: number;
  saving_annual: number;
  usage_narrative: string;
  call_script: string;
}

// Phase 13 D-07: one entry in the reasoning_trace surface.
// D-11 EXEMPTION: summary INTENTIONALLY contains digits, $, dates, %.
// Backend formatters live in agent/reasoning/summaries.py — keep byte-sync.
export interface ReasoningTraceEntry {
  tool: string;
  summary: string;
}

export interface RecommendationResponse {
  green: TrackInfo;
  cheapest: TrackInfo;
  // Phase 13 D-07 — optional: empty/omitted on single-tool turns (CUST-001/004/005).
  // PUBLIC field; NOT stripped by api_lambda/handler.py (D-12).
  reasoning_trace?: ReasoningTraceEntry[];
}
```

Do NOT touch `TrackInfo` (REC-03 unchanged). Do NOT introduce a new file — types.ts is the single source for all UI types.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/lib/mock/recommendations.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface ReasoningTraceEntry" ui/src/lib/types.ts` equals 1.
    - `grep -c "reasoning_trace?: ReasoningTraceEntry\[\]" ui/src/lib/types.ts` equals 1.
    - `grep -cE "tool: string;|summary: string;" ui/src/lib/types.ts` >= 2.
    - `grep -cE "reasoning_trace\?" ui/src/lib/types.ts` equals 1 (optional — NOT required).
    - `cd ui && npx tsc --noEmit` exits 0 (TypeScript compiles).
    - `cd ui && npx vitest run src/lib/mock/recommendations.test.ts` exits 0 (existing mock test still green).
  </acceptance_criteria>
  <done>Types extended; existing UI code still compiles; snake_case contract preserved.</done>
</task>

<task type="auto">
  <name>Task 6.2: Update `ui/src/lib/mock/recommendations.ts` with `MOCK_REASONING_TRACE_CUST003` + byte-sync header</name>
  <files>ui/src/lib/mock/recommendations.ts</files>
  <read_first>
    - ui/src/lib/mock/recommendations.ts (entire file — confirm existing persona mocks CUST-001/002/003 + header byte-sync comment)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md §"ui/src/lib/mock/recommendations.ts" (lines 621-663)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md §"D-29" (lines 184-185 — per A-01 amendment: MOCK_REASONING_TRACE_CUST003, not CUST002)
    - agent/reasoning/summaries.py (from Plan 02 — the Python formatters whose output strings MUST match byte-exact)
    - Plan 13-01-SUMMARY.md (Elena's actual delta/mean/current values from `detect_bill_shock_pure` — executor uses these numbers verbatim in the mock)
  </read_first>
  <action>
Make 3 edits to `ui/src/lib/mock/recommendations.ts`:

**Edit 1 — Add header comment line.** Find the existing header block (lines 1-19 of the current file, per 13-PATTERNS.md). Append a new line to the comment stanza:

```typescript
// Phase 13 D-29: reasoning_trace entries below MUST stay in sync with
// `agent/reasoning/summaries.py` formatters. Elena CUST-003 is the designated
// bill-shock persona (A-01 amendment); CUST-001/CUST-002 have empty traces.
// Run `npm run test` + `pytest tests/test_bill_shock_flow.py::TestCrossPersonaCanary`
// in the same commit when these values change.
```

**Edit 2 — Add `MOCK_REASONING_TRACE_CUST003` export.** Above `MOCK_RECOMMENDATIONS`:

```typescript
// Phase 13: byte-exact trace for Elena (CUST-003), the designated bill-shock
// persona. Tool order follows the preference graph in _BASE_SYSTEM_PROMPT:
//   1. get_hardship_flag — evidence check
//   2. detect_bill_shock — optional anomaly confirmation
//   3. simulate_savings — ALWAYS last (REC-03)
//
// SUMMARIES MUST MATCH agent/reasoning/summaries.py OUTPUT EXACTLY.
// See `.planning/phases/13-*/13-01-SUMMARY.md` for Elena's pure-helper output.
export const MOCK_REASONING_TRACE_CUST003: ReasoningTraceEntry[] = [
  { tool: 'get_hardship_flag', summary: 'hardship_flag=False' },
  {
    tool: 'detect_bill_shock',
    // EXECUTOR: fill in Elena's exact delta/mean/current values from 13-01-SUMMARY.md.
    // Template (replace {X},{Y},{Z} with the measured figures):
    //   "Bill shock detected: +${delta:.2f} 2025-10 vs 11-month avg (${current:.2f} vs ${mean:.2f})"
    summary:
      'Bill shock detected: +$XX.XX 2025-10 vs 11-month avg ($YY.YY vs $ZZ.ZZ)',
  },
  {
    tool: 'simulate_savings',
    // Elena's byte-exact Phase 11 D-13 fixtures: green $14.00/mo, cheapest $25.67/mo.
    summary: 'Green $14.00/mo; Cheapest $25.67/mo',
  },
];
```

**CRITICAL EXECUTOR STEP:** Replace the `XX.XX` / `YY.YY` / `ZZ.ZZ` placeholders with the ACTUAL values measured by `detect_bill_shock_pure(elena_billing)` in Plan 01. To get them, run:

```bash
python3 -c "
import importlib
h = importlib.import_module('lambda.handler')
from infrastructure.seed_data.billing_records import ELENA_VASQUEZ_RECORDS
r = h.detect_bill_shock_pure(ELENA_VASQUEZ_RECORDS)
print(f\"Bill shock detected: +\${r['delta_dollars']:.2f} {r['shock_month']} vs 11-month avg (\${r['current_dollars']:.2f} vs \${r['mean_dollars']:.2f})\")
"
```

Paste the exact output string INTO the `detect_bill_shock` entry's `summary` — byte-for-byte. If Plan 01's SUMMARY.md documented these values, use those directly.

**Edit 3 — Attach `reasoning_trace` to each persona mock.**

The existing `MOCK_RECOMMENDATIONS: Record<string, RecommendationResponse>` holds CUST-001/CUST-002/CUST-003 entries. Add `reasoning_trace` field to each:
- `'CUST-001': { green, cheapest, reasoning_trace: [] }`
- `'CUST-002': { green, cheapest, reasoning_trace: [] }` (Marcus is the non-shock foil — empty trace)
- `'CUST-003': { green, cheapest, reasoning_trace: MOCK_REASONING_TRACE_CUST003 }`

If CUST-004 and CUST-005 are NOT in the current file (per 13-PATTERNS.md line 662 note), leave the file at 3 entries. Plan 16 will extend to 5 personas.

Do NOT reorder existing persona entries. Do NOT change existing `green`/`cheapest` numeric fields (Phase 11 byte-exact carry-forward).
  </action>
  <verify>
    <automated>cd ui && npx vitest run src/lib/mock/recommendations.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MOCK_REASONING_TRACE_CUST003" ui/src/lib/mock/recommendations.ts` >= 2 (declaration + reference in MOCK_RECOMMENDATIONS).
    - `grep -cE "reasoning_trace: \[\]" ui/src/lib/mock/recommendations.ts` >= 2 (CUST-001 + CUST-002 entries).
    - `grep -c "reasoning_trace: MOCK_REASONING_TRACE_CUST003" ui/src/lib/mock/recommendations.ts` equals 1.
    - `grep -c "Bill shock detected:" ui/src/lib/mock/recommendations.ts` >= 1 (Elena's trace content).
    - `grep -F "Green \$14.00/mo; Cheapest \$25.67/mo" ui/src/lib/mock/recommendations.ts` (Elena's simulate_savings summary byte-exact).
    - `grep -c "agent/reasoning/summaries.py" ui/src/lib/mock/recommendations.ts` >= 1 (new sync-target header).
    - `grep -c "XX.XX\|YY.YY\|ZZ.ZZ" ui/src/lib/mock/recommendations.ts` equals 0 (executor MUST replace placeholders).
    - `cd ui && npx tsc --noEmit` exits 0.
    - `cd ui && npx vitest run src/lib/mock/recommendations.test.ts` exits 0 (existing test — may need update if it asserts field presence).
  </acceptance_criteria>
  <done>CUST-003 mock has byte-exact 3-entry trace; other personas have empty traces; header sync-target updated; placeholders replaced with measured Elena values.</done>
</task>

<task type="auto">
  <name>Task 6.3: Create `ui/src/components/ReasoningTrace.tsx` (component)</name>
  <files>ui/src/components/ReasoningTrace.tsx</files>
  <read_first>
    - ui/src/components/RecommendationCard.tsx (entire file — structural template for NARRATIVE_ENABLED + imports + styling + shadcn primitives)
    - ui/src/lib/flags.ts (line 8-9 — NARRATIVE_ENABLED export — module-load URLSearchParams evaluation)
    - ui/src/lib/types.ts (post-Task 6.1 — ReasoningTraceEntry interface)
    - ui/src/components/ui/*.tsx (confirm available shadcn primitives: alert, badge, button, card, input, label, skeleton — no new install needed)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md §"D-26" (lines 177-181) + §"D-27" (lines 182-183) + §"D-28" (line 183-184)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md §"ui/src/components/ReasoningTrace.tsx" (lines 499-560)
  </read_first>
  <behavior>
    - Empty list (`trace={[]}`) → returns `null` (zero vertical cost for single-tool personas).
    - `NARRATIVE_ENABLED === false` (via `?narrative=off`) → returns `null` for both empty AND non-empty traces.
    - Collapsed state (default): `▶ N steps: tool_a → tool_b → tool_c` — tool NAMES ONLY, no numbers, no `$` symbols in the collapsed text.
    - Click the disclosure row: expands to a numbered ordered list (`<ol>`) of `entry.summary` strings below.
    - Expanded state preserves the disclosure row with a `▼` chevron (indicates expanded).
    - Single row at 1280px viewport — UI-01 preserved (verified by vitest snapshot in Task 6.4 case 3).
  </behavior>
  <action>
Create NEW file `ui/src/components/ReasoningTrace.tsx` using the `RecommendationCard.tsx` structural template and shadcn primitives that are already installed.

```tsx
// Phase 13 D-26 / D-27 / D-28: collapsed-by-default reasoning-trace disclosure.
//
// Placement: ABOVE the 2-column card grid in App.tsx (D-28 — trace is
// turn-level, shared across both cards).
//
// UI-01 contract: collapsed state is ONE row showing only tool NAMES.
// Numbers / dollar amounts / dates live in the EXPANDED state.
//
// LD-7 kill-switch: if `?narrative=off` in the URL, the component renders
// `null` (same pattern as RecommendationCard's narrative + call_script).
// `NARRATIVE_ENABLED` is evaluated at module load via URLSearchParams —
// do NOT read window.location.search directly in this component.
//
// D-11 EXEMPTION: expanded summaries intentionally contain digits, `$`,
// percentages, and dates — do NOT apply narrative validators.
import { useState } from 'react';
import { NARRATIVE_ENABLED } from '@/lib/flags';
import type { ReasoningTraceEntry } from '@/lib/types';

interface ReasoningTraceProps {
  trace: ReasoningTraceEntry[];
}

export function ReasoningTrace({ trace }: ReasoningTraceProps) {
  // LD-7 kill-switch — checked FIRST so empty list is also null under ?narrative=off.
  if (!NARRATIVE_ENABLED) return null;

  // Empty-list short-circuit — single-tool personas render zero vertical cost.
  if (!trace || trace.length === 0) return null;

  const [expanded, setExpanded] = useState(false);
  const toolNames = trace.map((entry) => entry.tool);
  const chevron = expanded ? '▼' : '▶';
  const label = `${chevron} ${trace.length} steps: ${toolNames.join(' → ')}`;

  return (
    <section className="mb-4" aria-label="Agent reasoning trace">
      <button
        type="button"
        onClick={() => setExpanded((prev) => !prev)}
        aria-expanded={expanded}
        className="text-sm text-muted-foreground hover:text-foreground transition-colors flex items-center gap-1 font-mono"
      >
        {label}
      </button>
      {expanded && (
        <ol className="mt-2 ml-6 list-decimal text-sm space-y-1 text-foreground">
          {trace.map((entry, idx) => (
            <li key={`${entry.tool}-${idx}`}>
              <span className="font-mono text-muted-foreground">
                {entry.tool}:
              </span>{' '}
              <span>{entry.summary}</span>
            </li>
          ))}
        </ol>
      )}
    </section>
  );
}
```

**Do NOT:**
- Import `window.location.search` directly — import `NARRATIVE_ENABLED` from `@/lib/flags` (module-load semantics are load-bearing for test isolation).
- Use `dangerouslySetInnerHTML` — React escapes by default; summary is plain text (T-13-06-04 mitigation).
- Add a shadcn `<Accordion>` component (not yet installed; overkill for this use).
- Render the trace inside either `RecommendationCard` — it is a sibling (D-28).
- Display the raw `reasoning_trace` prop when empty — RETURN NULL (zero vertical cost).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls ui/src/components/ReasoningTrace.tsx` exits 0.
    - `grep -c "^export function ReasoningTrace" ui/src/components/ReasoningTrace.tsx` equals 1.
    - `grep -c "if (!NARRATIVE_ENABLED) return null;" ui/src/components/ReasoningTrace.tsx` equals 1.
    - `grep -c "if (!trace || trace.length === 0) return null;" ui/src/components/ReasoningTrace.tsx` equals 1.
    - `grep -c "NARRATIVE_ENABLED" ui/src/components/ReasoningTrace.tsx` equals 2 (import + usage).
    - `grep -c "import type { ReasoningTraceEntry } from '@/lib/types';" ui/src/components/ReasoningTrace.tsx` equals 1.
    - `grep -c "useState" ui/src/components/ReasoningTrace.tsx` equals 2 (import + call).
    - `grep -c "dangerouslySetInnerHTML" ui/src/components/ReasoningTrace.tsx` equals 0 (T-13-06-04).
    - `grep -c "window.location" ui/src/components/ReasoningTrace.tsx` equals 0 (D-27 contract — NARRATIVE_ENABLED is the only kill-switch access).
    - `cd ui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Component renders collapsed-by-default, honours `?narrative=off`, returns null on empty list; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 6.4: Create `ui/src/components/ReasoningTrace.test.tsx` (all 6 D-30 cases)</name>
  <files>ui/src/components/ReasoningTrace.test.tsx</files>
  <read_first>
    - ui/src/components/RecommendationCard.test.tsx (entire file — kill-switch stub pattern + `vi.stubGlobal` + `vi.resetModules` + dynamic `await import` idiom — LOAD-BEARING)
    - ui/src/lib/flags.test.ts (existing test for NARRATIVE_ENABLED module-load semantics — the reason we use dynamic import + resetModules)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md §"D-30" (lines 185-192)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md §"ui/src/components/ReasoningTrace.test.tsx" (lines 564-618)
  </read_first>
  <action>
Create NEW file `ui/src/components/ReasoningTrace.test.tsx` with all 6 D-30 vitest cases. The kill-switch cases use the LOAD-BEARING `vi.stubGlobal('location', {search: '?narrative=off'}) + vi.resetModules() + await import('./ReasoningTrace')` idiom — DO NOT refactor this.

```tsx
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import type { ReasoningTraceEntry } from '@/lib/types';

function traceFixture(n: number = 3): ReasoningTraceEntry[] {
  const tools = ['get_hardship_flag', 'detect_bill_shock', 'simulate_savings'];
  const summaries = [
    'hardship_flag=False',
    'Bill shock detected: +$47.00 2025-10 vs 11-month avg ($135.00 vs $88.00)',
    'Green $14.00/mo; Cheapest $25.67/mo',
  ];
  return Array.from({ length: n }, (_, i) => ({
    tool: tools[i] ?? 'unknown',
    summary: summaries[i] ?? 'summary',
  }));
}

beforeEach(() => {
  vi.restoreAllMocks();
  vi.unstubAllGlobals();
  vi.resetModules();
});

describe('ReasoningTrace — flag ON (default), rendering states', () => {
  it('renders null when trace is empty', async () => {
    vi.stubGlobal('location', { search: '' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    const { container } = render(<ReasoningTrace trace={[]} />);
    expect(container.firstChild).toBeNull();
  });

  it('renders collapsed state with tool names and chevron when trace has 3 entries', async () => {
    vi.stubGlobal('location', { search: '' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    render(<ReasoningTrace trace={traceFixture(3)} />);

    // Collapsed label contains chevron + step count + tool names (NAMES ONLY, no numbers).
    const label = screen.getByRole('button', { expanded: false });
    expect(label).toHaveTextContent(/3 steps:/);
    expect(label).toHaveTextContent(/get_hardship_flag/);
    expect(label).toHaveTextContent(/detect_bill_shock/);
    expect(label).toHaveTextContent(/simulate_savings/);

    // UI-01 guard: collapsed copy does NOT contain $ or digits-from-summary.
    expect(label.textContent).not.toMatch(/\$/);
    expect(label.textContent).not.toMatch(/2025-10/);
  });

  it('click on disclosure row expands to show numbered summary list', async () => {
    vi.stubGlobal('location', { search: '' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    render(<ReasoningTrace trace={traceFixture(3)} />);

    const disclosure = screen.getByRole('button', { expanded: false });
    fireEvent.click(disclosure);

    // After click: button is expanded, summaries visible.
    expect(screen.getByRole('button', { expanded: true })).toBeInTheDocument();
    expect(screen.getByText(/hardship_flag=False/)).toBeInTheDocument();
    expect(screen.getByText(/Bill shock detected:/)).toBeInTheDocument();
    expect(screen.getByText(/Green \$14.00\/mo/)).toBeInTheDocument();
  });

  it('renders collapsed state with 1 entry correctly (edge case)', async () => {
    vi.stubGlobal('location', { search: '' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    render(<ReasoningTrace trace={traceFixture(1)} />);

    const label = screen.getByRole('button', { expanded: false });
    expect(label).toHaveTextContent(/1 steps:/);
    expect(label).toHaveTextContent(/get_hardship_flag/);
  });
});

describe('ReasoningTrace — ?narrative=off kill switch (LD-7, D-27)', () => {
  it('renders null when flag is off and trace is non-empty', async () => {
    vi.stubGlobal('location', { search: '?narrative=off' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    const { container } = render(<ReasoningTrace trace={traceFixture(3)} />);
    expect(container.firstChild).toBeNull();
  });

  it('renders null when flag is off and trace is empty', async () => {
    vi.stubGlobal('location', { search: '?narrative=off' } as Location);
    vi.resetModules();
    const { ReasoningTrace } = await import('./ReasoningTrace');
    const { container } = render(<ReasoningTrace trace={[]} />);
    expect(container.firstChild).toBeNull();
  });
});
```

**Why `vi.resetModules()` + `await import(...)` is mandatory:** `NARRATIVE_ENABLED` is evaluated ONCE at `flags.ts` module load via `new URLSearchParams(window.location.search)`. If we import `ReasoningTrace` at the TOP of the test file, the import chain pins `NARRATIVE_ENABLED = true` forever in that test runner. `vi.stubGlobal('location', {...})` then `vi.resetModules()` + dynamic `await import()` re-evaluates the module with the new stub. DO NOT refactor to top-level imports.

**Test 6 is a conscious amalgam** — D-30 specifies 6 cases but "1-entry collapsed" + "1-entry expanded" are combined into "renders collapsed state with 1 entry correctly" (expanded state coverage lives in the general expand test).
  </action>
  <verify>
    <automated>cd ui && npx vitest run src/components/ReasoningTrace.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls ui/src/components/ReasoningTrace.test.tsx` exits 0.
    - `grep -c "describe('ReasoningTrace" ui/src/components/ReasoningTrace.test.tsx` >= 2 (flag-ON + flag-off describe blocks).
    - `grep -cE "^\s*it\(" ui/src/components/ReasoningTrace.test.tsx` >= 6 (6 D-30 cases).
    - `grep -c "vi.stubGlobal('location'," ui/src/components/ReasoningTrace.test.tsx` >= 3 (flag ON + 2 flag-off cases).
    - `grep -c "vi.resetModules()" ui/src/components/ReasoningTrace.test.tsx` >= 4 (one per test that stubs location).
    - `grep -c "await import('./ReasoningTrace')" ui/src/components/ReasoningTrace.test.tsx` >= 6.
    - `cd ui && npx vitest run src/components/ReasoningTrace.test.tsx` exits 0 (all 6+ tests pass).
    - `cd ui && npm run test` exits 0 (full UI suite).
  </acceptance_criteria>
  <done>All 6 D-30 cases pass; kill-switch module-load semantics verified; UI-01 collapsed-copy guard asserts no `$` / dates in collapsed state.</done>
</task>

<task type="auto">
  <name>Task 6.5: Insert `<ReasoningTrace>` into `ui/src/App.tsx` `success` branch</name>
  <files>ui/src/App.tsx</files>
  <read_first>
    - ui/src/App.tsx (entire file — 77 lines)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-CONTEXT.md §"D-28" (line 183-184)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-PATTERNS.md §"ui/src/App.tsx" (lines 707-728)
  </read_first>
  <action>
Make TWO edits to `ui/src/App.tsx`.

**Edit 1 — Add import at the top of the imports block (around lines 22-29):**

Insert AFTER `import { RecommendationCard } from '@/components/RecommendationCard';`:

```tsx
import { ReasoningTrace } from '@/components/ReasoningTrace';
```

**Edit 2 — Wrap the `success` branch body in a fragment and insert `<ReasoningTrace>` before the card grid (lines 63-69):**

Current:
```tsx
{state.status === 'success' && (
  <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
    {/* Card order stable: Green first, Cheapest second. */}
    <RecommendationCard track="green" data={state.data.green} />
    <RecommendationCard track="cheapest" data={state.data.cheapest} />
  </div>
)}
```

Replace with:
```tsx
{state.status === 'success' && (
  <>
    {/* Phase 13 D-28: ReasoningTrace lives ABOVE the card grid (turn-level,
        shared). Empty trace / ?narrative=off → component renders null. */}
    <ReasoningTrace trace={state.data.reasoning_trace ?? []} />
    <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
      {/* Card order stable: Green first, Cheapest second. */}
      <RecommendationCard track="green" data={state.data.green} />
      <RecommendationCard track="cheapest" data={state.data.cheapest} />
    </div>
  </>
)}
```

Do NOT change any other part of `App.tsx`:
- `useRecommendations` hook call UNCHANGED.
- `VersionIndicator` UNCHANGED.
- Loading / error / idle branches UNCHANGED.
- Page title / layout UNCHANGED (UI-01 vertical budget).

The `state.data.reasoning_trace ?? []` fallback handles two cases:
1. Backend returns response without the field (older runtime / rollback).
2. Backend returns `reasoning_trace: null` / undefined.
In both cases the component receives `[]` and short-circuits to `null`. Belt-and-braces.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | tail -10 && cd ui && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { ReasoningTrace } from '@/components/ReasoningTrace';" ui/src/App.tsx` equals 1.
    - `grep -c "<ReasoningTrace trace=" ui/src/App.tsx` equals 1.
    - `grep -c "state.data.reasoning_trace ?? \[\]" ui/src/App.tsx` equals 1.
    - `grep -c "<RecommendationCard track=" ui/src/App.tsx` equals 2 (existing, unchanged).
    - `grep -c "grid-cols-1 md:grid-cols-2 gap-8" ui/src/App.tsx` equals 1 (grid div preserved).
    - `cd ui && npx tsc --noEmit` exits 0.
    - `cd ui && npm run test` exits 0 (full UI vitest suite).
    - `cd ui && npm run lint` exits 0.
  </acceptance_criteria>
  <done>ReasoningTrace rendered above the card grid in the success branch; single-tool personas (empty trace) + `?narrative=off` both collapse to null (zero vertical cost); full UI suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → UI `reasoning_trace` | React renders summary strings as plain text; no HTML injection surface. |
| URL query string → `NARRATIVE_ENABLED` | Module-load evaluation means test isolation uses resetModules; misuse could allow trace to leak in emergency-kill-switch scenarios. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Tampering | `?narrative=off` doesn't collapse ReasoningTrace | mitigate | Task 6.4 cases 5 + 6 assert null render for both empty and non-empty traces under the flag. |
| T-13-06-02 | Information Disclosure | collapsed state leaks dollar amounts in UI-01 vertical budget | mitigate | Task 6.4 case 2 asserts `$` + `2025-10` absent from collapsed text; only tool names appear. |
| T-13-06-03 | Tampering | future dev refactors to top-level `import { ReasoningTrace }` in the test file, breaking flag isolation | mitigate | Task 6.4 uses dynamic `await import` pattern with explicit comment explaining module-load semantics. |
| T-13-06-04 | Tampering (XSS) | malicious summary HTML interpolates as markup | mitigate | React escapes by default; component uses `{entry.summary}` not `dangerouslySetInnerHTML`; Task 6.3 acceptance asserts absence. |
| T-13-06-05 | Tampering (byte-sync) | mock recommendations drift from backend formatters | mitigate | Task 6.2 header sync-target comment; Task 6.4 test-fixture mirrors Python formatter strings; Plan 09 CLAUDE.md addendum reinforces single-commit discipline. |
</threat_model>

<verification>
- `cd ui && npx tsc --noEmit` exits 0.
- `cd ui && npm run test` exits 0 (all vitest including existing RecommendationCard tests + new ReasoningTrace tests).
- `cd ui && npm run lint` exits 0.
- `cd ui && npm run build` exits 0 (production build with `VITE_API_URL=https://example.com`).
- `cd ui && npm run build:mock` exits 0 (emergency mock-only build works — MOCK_REASONING_TRACE_CUST003 resolves).
- Visual: at 1280×800 rendering CUST-003's collapsed trace, both recommendation cards remain above the fold (manual check — Plan 08 rehearsal).
</verification>

<success_criteria>
- `ui/src/lib/types.ts` exports `ReasoningTraceEntry` + extends `RecommendationResponse` with optional `reasoning_trace` (snake_case).
- `ui/src/lib/mock/recommendations.ts` has `MOCK_REASONING_TRACE_CUST003` byte-exact to `agent/reasoning/summaries.py` formatter output; CUST-001 and CUST-002 have `reasoning_trace: []`; header sync-target comment updated.
- `ui/src/components/ReasoningTrace.tsx` collapsed-by-default; returns null on empty trace OR `?narrative=off`; expanded state shows numbered summary list.
- `ui/src/components/ReasoningTrace.test.tsx` passes all 6 D-30 cases.
- `ui/src/App.tsx` inserts `<ReasoningTrace>` above the card grid using fragment wrapper.
- Full UI suite + TypeScript compile + lint + production build + mock build all green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-06-SUMMARY.md` with:
- Final byte-exact string value used for Elena's `detect_bill_shock` summary in `MOCK_REASONING_TRACE_CUST003` (must match Plan 01's measured output).
- Visual inspection note from the 1280×800 viewport — does the collapsed-trace row fit above the fold with both cards + narrative? If marginal, flag for Plan 08's rehearsal checklist.
- Confirmation that `npm run build:mock` (emergency mock-only path) still produces a valid `dist-mock/` with the new ReasoningTrace component.
</output>
</content>
</invoke>