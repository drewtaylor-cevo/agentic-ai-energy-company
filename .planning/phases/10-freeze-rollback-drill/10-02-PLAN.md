---
phase: 10-freeze-rollback-drill
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - requirements.in
  - requirements-dev.in
  - requirements.txt
  - requirements-dev.txt
  - .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
  - .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
  - .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
autonomous: true
requirements:
  - DEMO-04
must_haves:
  truths:
    - "requirements.in + requirements-dev.in exist as pip-compile source-of-truth with -c requirements.txt constraint layering"
    - "requirements.txt + requirements-dev.txt are regenerated by pip-compile --generate-hashes and every non-comment non-directive line carries at least one --hash=sha256 annotation"
    - "pip install --require-hashes -r requirements-dev.txt succeeds in a fresh venv (reproducibility precondition for D-19)"
    - "FREEZE-MANIFEST.md template exists with all 8 D-07/D-10 top-level YAML keys present inside a single yaml code fence, hash fields scaffolded with placeholder values, break-glass block populated with working set-stack-policy commands referencing file paths under infrastructure/stack-policies/ for the *-allow-all.json bodies"
    - "10-DRILL-LOG.md skeleton exists with step scaffolding in speed-first order (narrative=off flag first, then build:mock, then git checkout demo-v1.0, then DynamoDB drill, then teardown) plus a Commands appendix containing every drill one-liner the operator will run"
    - "DEMO-RUNBOOK.md has four new sections added (section 7 T-48h Freeze Ceremony, section 8 T-30m Keep-Alive, section 9 T-10m Pre-Warm, section 10 T-eval Live Eval Harness Gate) — renumbered from the originally-proposed sections 3-6 because the existing file already uses those section numbers"
    - "pytest -m 'not smoke' baseline remains 81 passed / 6 skipped"
  artifacts:
    - path: requirements.in
      provides: "pip-compile source-of-truth for production deps"
      contains: "aws-cdk-lib"
    - path: requirements-dev.in
      provides: "pip-compile source-of-truth for dev deps with -c constraint on prod"
      contains: "-c requirements.txt"
    - path: requirements.txt
      provides: "Hash-pinned production lockfile regenerated by pip-compile --generate-hashes"
      contains: "--hash=sha256"
    - path: requirements-dev.txt
      provides: "Hash-pinned dev lockfile regenerated by pip-compile --generate-hashes"
      contains: "--hash=sha256"
    - path: .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
      provides: "Scaffolded manifest template with all D-07/D-10 top-level keys, hash fields blank (populated by 10-03)"
      contains: "bedrock_model_id"
    - path: .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
      provides: "Drill evidence log skeleton with step headers + Commands appendix (populated by 10-03)"
      contains: "Commands"
    - path: .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      provides: "Presenter runbook extended with Phase 10 sections 7-10 (renumbered from D-20 sections 3-6 to avoid collision)"
      contains: "T-48h Freeze Ceremony"
  key_links:
    - from: "requirements-dev.in"
      to: "requirements.txt"
      via: "-c requirements.txt constraint directive"
      pattern: "^-c requirements\\.txt$"
    - from: "FREEZE-MANIFEST.md break_glass.unlock_stack_policies"
      to: "infrastructure/stack-policies/*-allow-all.json (committed in 10-01)"
      via: "file protocol path argument referencing each allow-all JSON body"
      pattern: "file://infrastructure/stack-policies/"
    - from: "10-DRILL-LOG.md Commands appendix"
      to: "10-VALIDATION.md Per-Task Verification Map rows 10-03-09 through 10-03-15"
      via: "copy-paste one-liners for operator at T-48h"
      pattern: "aws dynamodb"
    - from: "DEMO-RUNBOOK.md section 9 T-10m Pre-Warm"
      to: "scripts/prewarm.py (Phase 9 artefact)"
      via: "npm run prewarm from ui/ with BACKEND_API_URL exported"
      pattern: "npm run prewarm"
    - from: "DEMO-RUNBOOK.md section 8 T-30m Keep-Alive"
      to: "scripts/demo-keepalive.sh (Phase 9 artefact)"
      via: "bash scripts/demo-keepalive.sh in a tmux pane"
      pattern: "demo-keepalive"
---

<objective>
Produce the freeze-ceremony artefacts that the T-48h operator (plan 10-03) will fill in and use:

1. **Reproducibility source-of-truth:** `requirements.in` + `requirements-dev.in` (new) driving
   `pip-compile --generate-hashes` regeneration of both `requirements*.txt` as hash-pinned
   lockfiles. This is the DEMO-04 byte-identical-Lambda-bundle precondition.

2. **FREEZE-MANIFEST.md scaffold:** single sectioned YAML-in-fence template (D-07 + D-10) with
   all 8 top-level keys, empty hash field placeholders, and a fully-populated break-glass block
   that references the 10-01 `-allow-all.json` bodies. The operator fills hash values in 10-03;
   the structure is locked here.

3. **10-DRILL-LOG.md skeleton:** step headers in speed-first recovery order (CONTEXT.md line 238 —
   `?narrative=off` fastest → `build:mock` emergency → `demo-v1.0` tag revert → DynamoDB drill),
   a `## Commands` appendix with every drill one-liner the operator will copy-paste, and YAML
   front-matter matching the Phase 9 VERIFICATION.md sibling convention.

4. **DEMO-RUNBOOK.md amendment:** append four new sections for T-48h Freeze Ceremony, T-30m
   Keep-Alive, T-10m Pre-Warm, T-eval Harness Gate. Note: D-20's originally-requested numbering
   (§3-§6) collides with existing file which already uses §1-§6. Per PATTERNS.md line 406
   recommendation, renumber the additions as §7-§10 (append-only, no collision).

**Purpose:** Plan 10-03 is the ceremony. This plan provides every non-AWS, non-tag-cutting
artefact the operator needs so the ceremony becomes "fill hashes + run commands", not "write a
manifest template at 3am".

**Output:** Two `.in` files + two regenerated hash-pinned `.txt` files + FREEZE-MANIFEST.md template
+ 10-DRILL-LOG.md skeleton + amended DEMO-RUNBOOK.md. No AWS calls. No stack-policy application.
No tag cut. pytest baseline stays 81 passed / 6 skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-freeze-rollback-drill/10-CONTEXT.md
@.planning/phases/10-freeze-rollback-drill/10-RESEARCH.md
@.planning/phases/10-freeze-rollback-drill/10-PATTERNS.md
@.planning/phases/10-freeze-rollback-drill/10-VALIDATION.md
@.planning/phases/10-freeze-rollback-drill/10-01-SUMMARY.md
@.planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
@requirements.txt
@requirements-dev.txt
@scripts/demo-keepalive.sh
@scripts/prewarm.py

<interfaces>
<!-- Shape contracts the executor MUST implement verbatim. -->

## requirements.in body (PATTERNS.md lines 174-189)

Starting from current `requirements.txt` (4 lines, `>=X` floors — verified via Read):

```
aws-cdk-lib>=2.250.0
aws-cdk.aws-bedrock-agentcore-alpha==2.250.0a0
constructs>=10.0.0
boto3>=1.42.0
```

Copy verbatim into `requirements.in`. Per Claude's Discretion #5 (CONTEXT.md line 133), the planner
MAY tighten `>=` to `==<pip-freeze-version>` to pin against currently-deployed versions — the
executor of this plan applies Pitfall 6 guidance (RESEARCH.md line 441): before running pip-compile,
snapshot `pip freeze` and rewrite `.in` to `==` against current versions. If the active venv has
already-resolved versions, that is authoritative for the freeze. Document the tactic used in the
plan SUMMARY so reviewers can trace version provenance.

## requirements-dev.in body (PATTERNS.md lines 192-215)

Current `requirements-dev.txt`:
```
-r requirements.txt
pytest>=7.0
pytest-mock>=3.0
requests>=2.28,<3
```

Rewrite as `requirements-dev.in` with `-r` changed to `-c` (constraint, not merge) per RESEARCH §Q2:
```
-c requirements.txt
pytest>=7.0
pytest-mock>=3.0
requests>=2.28,<3
```

Rationale: `-c` means "dev-layer picks same versions as prod" without re-emitting prod deps into
the dev `.txt`. `-r` would merge prod deps into dev file (duplicate hashes, harder audit).

## FREEZE-MANIFEST.md template shape (D-07, D-10, PATTERNS.md lines 249-300)

Single Markdown file. Opens with H1 heading. 2-4 lines of prose. Then ONE `yaml` code fence
containing all 8 top-level keys. Outside the fence, no other YAML — only presenter-friendly prose.

Top-level keys in this order (per RESEARCH Example 2 + D-10):
1. `git:` (freeze_commit_sha, freeze_timestamp_utc, tag)
2. `lockfiles:` (requirements_txt, requirements_dev_txt, ui_package_lock_json)
3. `dist_bundles:` (ui_dist, ui_dist_mock)
4. `synth_assets:` (list of `{logical, asset_hash, bundle_sha256}` — one entry per cdk.out/asset.*/ dir)
5. `cloudformation:` (FoundationStack, AgentCoreStack, BackendApiStack — all StackId ARN values)
6. `bedrock_model_id:` (literal `us.anthropic.claude-sonnet-4-6` — verified at agent/agent.py:305)
7. `dynamodb_backup:` (table_name, backup_arn, backup_timestamp_utc)
8. `break_glass:` (unlock_stack_policies, disable_termination_protection, after_fix)

Hash fields scaffolded as `"sha256:<pending>"` placeholder; operator in 10-03 replaces with real
64-char hex. Timestamp fields scaffolded as `"<pending-UTC>"`. ARN fields scaffolded as
`"<pending>"`. `bedrock_model_id` is filled NOW with the literal `us.anthropic.claude-sonnet-4-6`
(no placeholder — verified at agent/agent.py:305 via Read).

## FREEZE-MANIFEST.md break_glass block contents (D-04 + D-01 REVISED)

Must reference the 6 JSON files committed in 10-01. Use an explicit stack-to-file mapping so the
block is presenter-readable (NOT the `${stack,,}` bash-4 expansion from RESEARCH Example 2):

```yaml
break_glass:
  unlock_stack_policies: |
    # Apply allow-all policy to each stack (replaces the deny-Update:* freeze policy).
    # Each stack has its own policy file under infrastructure/stack-policies/.
    aws cloudformation set-stack-policy --stack-name CustomerTariff \
        --stack-policy-body file://infrastructure/stack-policies/foundation-allow-all.json \
        --region us-east-1 --profile cevo-dev25
    aws cloudformation set-stack-policy --stack-name CustomerTariffAgent \
        --stack-policy-body file://infrastructure/stack-policies/agentcore-allow-all.json \
        --region us-east-1 --profile cevo-dev25
    aws cloudformation set-stack-policy --stack-name CustomerTariffApi \
        --stack-policy-body file://infrastructure/stack-policies/backend-api-allow-all.json \
        --region us-east-1 --profile cevo-dev25

  disable_termination_protection: |
    for stack in CustomerTariff CustomerTariffAgent CustomerTariffApi; do
      aws cloudformation update-termination-protection --no-enable-termination-protection \
          --stack-name "$stack" --region us-east-1 --profile cevo-dev25
    done

  after_fix: |
    # After applying the emergency fix:
    #   1. Reapply deny-Update:* policies by swapping -allow-all.json -> -freeze.json above.
    #   2. Re-enable termination protection (set --enable-termination-protection).
    #   3. Recompute all manifest hashes (scripts/hash_dist.sh + scripts/hash_synth_assets.sh).
    #   4. Commit the updated FREEZE-MANIFEST.md and cut a new tag (demo-v2.0.1).
    # Full ceremony re-run is DEMO-RUNBOOK.md section 7 steps 2-7.
```

CustomerTariff / CustomerTariffAgent / CustomerTariffApi are the CFN stack names, while the JSON
file names use foundation / agentcore / backend-api. Explicit mapping is more portable and
presenter-readable than a shell-expansion trick (PATTERNS.md line 303 noted the tradeoff).

## 10-DRILL-LOG.md structure (PATTERNS.md lines 313-370)

Front-matter block (described as a list here to avoid colliding with this planning doc’s own YAML front-matter regex; the committed 10-DRILL-LOG.md uses a standard fenced front-matter block like 09-VERIFICATION.md lines 1-17):
- phase: 10-freeze-rollback-drill
- artifact: drill-log
- verified: "<pending-UTC>"
- status: pending
- score: 0/5
- overrides_applied: 0
- human_verification: block documenting the D-15 browser screenshot requirement


H1: `# Phase 10: Rollback Drill Log`.

One `## Drill Step N. <Name>` block per drill step, in speed-first order:
1. `?narrative=off` URL-flag drill (D-15)
2. `build:mock` <10s regeneration + hash-roundtrip (D-16)
3. `git checkout demo-v1.0` + pytest green (D-13)
4. DynamoDB restore-from-backup + scan + spot-check (D-12)
5. Scratch table teardown

Each step block captures Test / Expected / Commands / Stdout / Started (UTC) / Verdict / Deviations.

Final section: `## Drill Verdict` with overall PASS/FAIL summary + operator identity + total
duration (filled in 10-03). Separator: `---`.

`## Commands` appendix with every one-liner from 10-VALIDATION.md Per-Task Verification Map
rows 10-03-09 through 10-03-15 (scratch table restore + scan + spot-check + curl + build:mock +
teardown + hash compare) plus the demo-v1.0 fresh-clone-pytest sequence from D-13.

## DEMO-RUNBOOK.md amendment structure (PATTERNS.md lines 402-449)

CRITICAL: the existing file already has `## 1.` through `## 6.` headings. D-20's original
request for new sections 3-6 would collide. Per PATTERNS.md line 406 recommendation, renumber as
sections 7-10 (append-only — no collision with operational prose the presenter has already memorised).

Append AFTER the existing `## Cross-references` section at line 205 (or insert immediately
before it — either placement is defensible; recommend AFTER cross-references with a section
break so the existing v1.0 runbook reads as a complete unit and sections 7-10 clearly belong to v2.0).

New headings (H2):
- `## 7. T-48h Freeze Ceremony (D-18 / DEMO-04)`
- `## 8. T-30m Keep-Alive Start (DEMO-05 / D-20)`
- `## 9. T-10m Pre-Warm (DEMO-03 / D-20)`
- `## 10. T-eval Live Eval Harness Gate (DEMO-03 / D-20)`

Each section: short intro + numbered checklist items + fenced bash commands with inline expected-output comments.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create requirements.in + requirements-dev.in and regenerate hash-pinned requirements*.txt via pip-compile --generate-hashes</name>
  <files>requirements.in, requirements-dev.in, requirements.txt, requirements-dev.txt</files>
  <read_first>
    - requirements.txt (current unpinned form — source of the 4 prod dep lines)
    - requirements-dev.txt (current form — has `-r requirements.txt` which must become `-c requirements.txt` in `.in`)
    - .planning/phases/10-freeze-rollback-drill/10-PATTERNS.md (lines 168-217 — requirements.in + requirements-dev.in patterns)
    - .planning/phases/10-freeze-rollback-drill/10-RESEARCH.md (§Q2 lines 739-788 — pip-compile workflow; Pitfall 6 at line 441 — version-stability tactic)
    - .planning/phases/10-freeze-rollback-drill/10-CONTEXT.md (Claude's Discretion #5 line 133 — choose whether to pin `==` against `pip freeze` snapshot or let pip-compile re-resolve)
    - .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md (tasks 10-02-01, 10-02-02, 10-02-03, 10-02-04 — the 4 acceptance gates for this task)
  </read_first>
  <action>
    Four substeps executed in order:

    **1. Install pip-tools** (verified NOT currently installed per RESEARCH.md env-availability
    table). Install into the active venv used for development:

    ```bash
    pip install 'pip-tools>=7.4.0'
    ```

    **2. Create `requirements.in`** with current production floors copied verbatim from
    `requirements.txt`:

    ```
    aws-cdk-lib>=2.250.0
    aws-cdk.aws-bedrock-agentcore-alpha==2.250.0a0
    constructs>=10.0.0
    boto3>=1.42.0
    ```

    OPTIONAL (Claude's Discretion #5, RESEARCH Pitfall 6 mitigation): before running pip-compile,
    snapshot the active venv versions via `pip freeze` and rewrite `>=X` to `==X` against
    currently-installed versions. This pins against whatever is currently deployed rather than
    re-resolving forward. RECOMMEND: apply the Pitfall-6 tactic. Document the chosen versions
    in the plan SUMMARY so reviewers can confirm no inadvertent version drift.

    **3. Create `requirements-dev.in`** with `-c` layering (NOT `-r` merge):

    ```
    -c requirements.txt
    pytest>=7.0
    pytest-mock>=3.0
    requests>=2.28,<3
    ```

    Trailing newline on both `.in` files. No comments except if required for a specific pin
    justification.

    **4. Compile — prod first, dev second** (order matters because dev uses prod as constraint):

    ```bash
    pip-compile --generate-hashes --output-file requirements.txt requirements.in
    pip-compile --generate-hashes --output-file requirements-dev.txt requirements-dev.in
    ```

    Both outputs must have `--hash=sha256:` annotations on every non-comment non-directive line.

    **5. Verify a fresh venv install succeeds** (the reproducibility precondition for 10-03 D-19):

    ```bash
    python3 -m venv /tmp/hashcheck
    /tmp/hashcheck/bin/pip install --require-hashes -r requirements-dev.txt
    # Exit code must be 0. Any wheel substitution or missing hash blocks install.
    rm -rf /tmp/hashcheck
    ```

    Do NOT commit until all four files exist and the fresh-venv install succeeds.

    Commit the 4 files as one atomic commit. Suggested message: `chore(10-02): pin Python deps via pip-compile --generate-hashes`.
  </action>
  <verify>
    <automated>
      test -f requirements.in && test -f requirements-dev.in && \
        python3 -c "import sys; t=open('requirements.txt').read(); pkgs=[l for l in t.splitlines() if l and not l.startswith(('#','-','--'))]; [sys.exit(f'unpinned: {p}') for p in pkgs if '--hash=sha256:' not in p]" && \
        python3 -c "import sys; t=open('requirements-dev.txt').read(); pkgs=[l for l in t.splitlines() if l and not l.startswith(('#','-','--'))]; [sys.exit(f'unpinned: {p}') for p in pkgs if '--hash=sha256:' not in p]" && \
        python3 -m venv /tmp/hashcheck-ac && /tmp/hashcheck-ac/bin/pip install --require-hashes -r requirements-dev.txt && rm -rf /tmp/hashcheck-ac && \
        grep -q '^-c requirements\.txt$' requirements-dev.in
    </automated>
  </verify>
  <acceptance_criteria>
    <automated>
      # Verbatim from 10-VALIDATION.md tasks 10-02-01 through 10-02-04:

      # 10-02-01: .in files exist
      test -f requirements.in && test -f requirements-dev.in

      # 10-02-02: requirements.txt has --hash=sha256: on every dep line
      python3 -c "import sys; t=open('requirements.txt').read(); pkgs=[l for l in t.splitlines() if l and not l.startswith(('#','-','--'))]; [sys.exit(f'unpinned: {p}') for p in pkgs if '--hash=sha256:' not in p]"

      # 10-02-03: requirements-dev.txt has --hash=sha256: on every dep line
      python3 -c "import sys; t=open('requirements-dev.txt').read(); pkgs=[l for l in t.splitlines() if l and not l.startswith(('#','-','--'))]; [sys.exit(f'unpinned: {p}') for p in pkgs if '--hash=sha256:' not in p]"

      # 10-02-04: pip install --require-hashes succeeds in a fresh venv
      python3 -m venv /tmp/hashcheck-ac && /tmp/hashcheck-ac/bin/pip install --require-hashes -r requirements-dev.txt && rm -rf /tmp/hashcheck-ac

      # Sanity: `-c requirements.txt` constraint directive present (not `-r`):
      grep -q '^-c requirements\.txt$' requirements-dev.in
      ! grep -qE '^-r ' requirements-dev.in
    </automated>
  </acceptance_criteria>
  <done>
    `requirements.in` + `requirements-dev.in` committed with correct directive syntax (`-c` not `-r`).
    `requirements.txt` + `requirements-dev.txt` regenerated with `--hash=sha256:` on every dep.
    `pip install --require-hashes -r requirements-dev.txt` succeeds in a clean venv. Plan SUMMARY
    records whether Pitfall-6 version-stability tactic was applied and what resolved versions
    pip-compile picked.
  </done>
</task>

<task type="execute">
  <name>Task 2: Scaffold FREEZE-MANIFEST.md template with all D-07/D-10 keys and populated break-glass block</name>
  <files>.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md</files>
  <read_first>
    - .planning/phases/10-freeze-rollback-drill/10-CONTEXT.md (D-06 line 61, D-07 line 63, D-10 lines 69-77 — location + single-fence YAML + exact top-level keys)
    - .planning/phases/10-freeze-rollback-drill/10-RESEARCH.md (Example 2 lines 470-540 — full manifest YAML skeleton)
    - .planning/phases/10-freeze-rollback-drill/10-PATTERNS.md (lines 220-304 — template structure + break-glass block with explicit stack-to-file mapping)
    - .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md (task 10-02-05 — schema check asserts all 8 top-level keys present)
    - agent/agent.py (line 305 — verify the literal bedrock model ID `us.anthropic.claude-sonnet-4-6`)
    - infrastructure/stack-policies/foundation-allow-all.json (committed in 10-01 — break-glass file reference target)
  </read_first>
  <action>
    Create the file `.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md` (D-06 location —
    phase-scoped, NOT repo-root).

    File content is a Markdown doc containing:
    - YAML front-matter with `phase`, `artifact: freeze-manifest`, `status: template`, `created: <YYYY-MM-DD>`
    - H1: `# FREEZE-MANIFEST — Customer Tariff Demo v2.0`
    - 3-5 lines of prose framing (what this is, when frozen, break-glass pointer)
    - ONE `yaml` code fence (triple-backticks) containing ALL 8 top-level keys in exact order:
      `git`, `lockfiles`, `dist_bundles`, `synth_assets`, `cloudformation`, `bedrock_model_id`,
      `dynamodb_backup`, `break_glass`

    For each field inside the YAML fence:
    - `git.freeze_commit_sha`: `"<pending>"`
    - `git.freeze_timestamp_utc`: `"<pending-UTC>"`
    - `git.tag`: `demo-v2.0`
    - `lockfiles.*`: `"sha256:<pending>"` placeholder per key (requirements_txt, requirements_dev_txt, ui_package_lock_json)
    - `dist_bundles.ui_dist` + `dist_bundles.ui_dist_mock`: `"sha256:<pending>"`
    - `synth_assets`: YAML list with ONE stub entry showing the shape `{logical, asset_hash, bundle_sha256}` and a YAML comment above noting "Populated by 10-03 — one entry per cdk.out/asset.*/ directory"
    - `cloudformation.FoundationStack` / `AgentCoreStack` / `BackendApiStack`: `"<pending>"` each (full StackId ARN format as comment)
    - `bedrock_model_id`: `"us.anthropic.claude-sonnet-4-6"` — FILLED with the literal, NO placeholder
    - `dynamodb_backup.table_name`: `tariff-billing` (pre-filled literal)
    - `dynamodb_backup.backup_arn`: `"<pending>"`
    - `dynamodb_backup.backup_timestamp_utc`: `"<pending-UTC>"`
    - `break_glass.unlock_stack_policies`: block literal (`|`) containing the three explicit
      `aws cloudformation set-stack-policy` commands — one per stack, each using
      `file://infrastructure/stack-policies/<foundation|agentcore|backend-api>-allow-all.json`,
      each with `--region us-east-1 --profile cevo-dev25`
    - `break_glass.disable_termination_protection`: block literal with the shell loop over all
      three stack names calling `aws cloudformation update-termination-protection --no-enable-termination-protection`
    - `break_glass.after_fix`: block literal with a comment explaining the re-run sequence
      (reapply deny policies, re-enable termination protection, recompute hashes, new tag
      like `demo-v2.0.1`, reference DEMO-RUNBOOK.md section 7 steps 2-7)

    Verify bedrock_model_id matches agent/agent.py:305 exactly — use Read on that line first,
    then copy the literal into the manifest.

    Do NOT pre-fill any `<pending>` value with a real hash in this plan — those belong to 10-03.
    The only literal values pre-filled are `bedrock_model_id` and `dynamodb_backup.table_name`.

    Use the Write tool. Set the front-matter `created:` field via `date -u +%Y-%m-%d` at execution
    time (actual date, not a placeholder).
  </action>
  <verify>
    <automated>
      python3 -c "
      import yaml, re, sys
      src = open('.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md').read()
      m = re.search(r'\`\`\`yaml\n(.*?)\n\`\`\`', src, re.S)
      assert m, 'no yaml fence found'
      manifest = yaml.safe_load(m.group(1))
      expected = {'git','lockfiles','dist_bundles','synth_assets','cloudformation','bedrock_model_id','dynamodb_backup','break_glass'}
      missing = expected - manifest.keys()
      assert not missing, f'missing keys: {missing}'
      assert manifest['bedrock_model_id'] == 'us.anthropic.claude-sonnet-4-6', manifest['bedrock_model_id']
      "
      grep -q 'file://infrastructure/stack-policies/foundation-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
      grep -q 'file://infrastructure/stack-policies/agentcore-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
      grep -q 'file://infrastructure/stack-policies/backend-api-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
    </automated>
  </verify>
  <acceptance_criteria>
    <automated>
      # Verbatim from 10-VALIDATION.md task 10-02-05 (schema check):
      python3 -c "
      import yaml, re, sys
      src = open('.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md').read()
      m = re.search(r'\`\`\`yaml\n(.*?)\n\`\`\`', src, re.S)
      assert m, 'no yaml fence found'
      manifest = yaml.safe_load(m.group(1))
      expected = {'git','lockfiles','dist_bundles','synth_assets','cloudformation','bedrock_model_id','dynamodb_backup','break_glass'}
      missing = expected - manifest.keys()
      sys.exit(f'missing keys: {missing}' if missing else 0)
      "

      # bedrock_model_id already filled with the live literal:
      python3 -c "
      import yaml, re
      src = open('.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md').read()
      m = re.search(r'\`\`\`yaml\n(.*?)\n\`\`\`', src, re.S).group(1)
      manifest = yaml.safe_load(m)
      assert manifest['bedrock_model_id'] == 'us.anthropic.claude-sonnet-4-6', manifest['bedrock_model_id']
      "

      # Break-glass block references all 3 allow-all JSON files committed in 10-01:
      grep -q 'file://infrastructure/stack-policies/foundation-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
      grep -q 'file://infrastructure/stack-policies/agentcore-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
      grep -q 'file://infrastructure/stack-policies/backend-api-allow-all.json' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md

      # No stray secret patterns:
      ! grep -iE '(AKIA[0-9A-Z]{16}|aws_secret_access_key|BEGIN RSA PRIVATE|BEGIN OPENSSH)' .planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md
    </automated>
  </acceptance_criteria>
  <done>
    `FREEZE-MANIFEST.md` exists at the D-06 location. Contains one `yaml` fence with all 8
    D-10 top-level keys parseable by yaml.safe_load. `bedrock_model_id` matches
    `us.anthropic.claude-sonnet-4-6` (the literal from `agent/agent.py:305`). Break-glass block
    references all three 10-01 allow-all JSON files. No credential patterns present.
  </done>
</task>

<task type="execute">
  <name>Task 3: Scaffold 10-DRILL-LOG.md skeleton with speed-first drill steps and Commands appendix</name>
  <files>.planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md</files>
  <read_first>
    - .planning/phases/10-freeze-rollback-drill/10-CONTEXT.md (D-14 line 87 — manual runbook format with Commands appendix; D-15 / D-16 / D-17 for per-step structure; specifics line 238 — speed-first ordering)
    - .planning/phases/10-freeze-rollback-drill/10-PATTERNS.md (lines 313-370 — front-matter + numbered-step block + Commands appendix pattern; analog = 09-VERIFICATION.md)
    - .planning/phases/09-pre-warm-tooling-eval-harness-keep-alive/09-VERIFICATION.md (lines 1-17 front-matter + lines 108-122 step format — exact shape to mirror)
    - .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md (rows 10-03-09 through 10-03-15 — exact commands to embed in Commands appendix)
  </read_first>
  <action>
    Create `.planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md`. Use the Write tool.

    Document structure:

    1. YAML front-matter (between `---` markers): `phase: 10-freeze-rollback-drill`,
       `artifact: drill-log`, `verified: "<pending-UTC>"`, `status: pending`, `score: 0/5`,
       `overrides_applied: 0`, and a `human_verification:` block describing the browser
       screenshot requirement for D-15.

    2. H1 heading: `# Phase 10: Rollback Drill Log`.

    3. 2-3 lines prose: evidence log populated at T-48h per DEMO-RUNBOOK section 7 step 3;
       ordered by recovery speed (cheapest lever first); drill gate — Phase 10 does NOT close
       until every step shows Verdict: PASS + final Drill Verdict overall PASS.

    4. Five H2 drill step blocks in EXACT speed-first order:
       - `## Drill Step 1. ?narrative=off URL-flag proof (D-15)`
       - `## Drill Step 2. build:mock <10s regeneration + hash-roundtrip (D-16)`
       - `## Drill Step 3. git checkout demo-v1.0 + pytest green from fresh clone (D-13)`
       - `## Drill Step 4. DynamoDB restore-from-backup + scan + spot-check (D-12)`
       - `## Drill Step 5. Scratch table teardown (D-12 cleanup)`

    5. Each step block contains:
       - `**Test:**` one-sentence description of what is being proven
       - `**Expected:**` bulleted list of success criteria
       - `**Command(s):**` fenced code block with `<pending — paste from ## Commands appendix>`
       - `**Stdout:**` fenced code block with `<pending — operator pastes output>`
       - `**Screenshot:**` (only in Step 1) — `<pending>`
       - `**Started:**` `<pending-UTC>`
       - `**Verdict:**` `pending`
       - `**Deviations:**` `(none expected)`

    6. H2 `## Drill Verdict` section at the end: Overall (pending), Drill duration (pending),
       Operator (pending).

    7. Separator `---`.

    8. H2 `## Commands` appendix. Copy-paste one-liners with five numbered subsections:
       - `### Step 1 commands (?narrative=off) — row 10-03-11`: `curl -sf "$BACKEND_API_URL/recommendations/CUST-001" | jq -e '.green.usage_narrative | strings'` + browser instructions
       - `### Step 2 commands (build:mock timing + hash-roundtrip) — rows 10-03-12 + 10-03-13`: `cd ui && /usr/bin/time -p npm run build:mock 2>&1 | tee /tmp/build-mock-time.txt` + `awk '/^real/ { if ($2 > 10.0) exit 1 }' /tmp/build-mock-time.txt` + hash-equality check against manifest
       - `### Step 3 commands (git checkout demo-v1.0 + pytest) — row 10-03-10`: `git clone . /tmp/freeze-repro` + venv setup + `git checkout demo-v1.0` + `git rev-parse HEAD` (assert == aba3a99c67994f39d9d496ddfd29c9116b756928) + `.venv/bin/pytest -m "not smoke"` (assert 81 passed, 6 skipped)
       - `### Step 4 commands (DynamoDB restore + scan + spot-check) — row 10-03-09`: extract `BACKUP_ARN` from manifest, `aws dynamodb restore-table-from-backup`, wait-table-exists, scan with select COUNT (assert 36), `aws dynamodb get-item` for 3 personas × 2025-04 (assert kwh present)
       - `### Step 5 commands (scratch teardown) — row 10-03-15`: `aws dynamodb delete-table`, `wait table-not-exists`, `describe-table` asserting ResourceNotFoundException

    All commands use `--profile cevo-dev25 --region us-east-1`. Front-matter `created:` filled
    via `date -u +%Y-%m-%d` at execution time.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
      grep -c '^## Drill Step [1-5]\. ' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md | grep -q '^5$'
      grep -q '^## Commands$' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
      grep -c '^### Step [1-5] commands' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md | grep -q '^5$'
    </automated>
  </verify>
  <acceptance_criteria>
    <automated>
      # File exists:
      test -f .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md

      # All 5 drill step headers present:
      grep -c '^## Drill Step [1-5]\. ' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md | grep -q '^5$'

      # Commands appendix present:
      grep -q '^## Commands$' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md

      # 5 numbered step subheadings in Commands appendix:
      grep -c '^### Step [1-5] commands' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md | grep -q '^5$'

      # Speed-first ordering verified:
      python3 -c "
      import sys
      lines = open('.planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md').read().splitlines()
      narr = next((i for i, l in enumerate(lines) if l.startswith('## Drill Step 1') and 'narrative=off' in l), -1)
      build = next((i for i, l in enumerate(lines) if l.startswith('## Drill Step 2') and 'build:mock' in l), -1)
      tag = next((i for i, l in enumerate(lines) if l.startswith('## Drill Step 3') and 'demo-v1.0' in l), -1)
      assert narr >= 0 and build >= 0 and tag >= 0 and narr < build < tag, f'speed-first ordering broken: {narr}, {build}, {tag}'
      "

      # Commands cite the stable identifiers:
      grep -q 'CUST-001' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
      grep -q 'tariff-billing-rollback-drill' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
      grep -q 'aba3a99' .planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md
    </automated>
  </acceptance_criteria>
  <done>
    10-DRILL-LOG.md exists with all 5 drill step blocks in speed-first order, a Commands appendix
    with 5 subheadings containing copy-pasteable one-liners, and YAML front-matter matching the
    09-VERIFICATION.md sibling convention. All placeholder `<pending>` fields preserved for
    10-03 population.
  </done>
</task>

<task type="execute">
  <name>Task 4: Amend DEMO-RUNBOOK.md with new sections 7-10 (renumbered from D-20 original 3-6 per PATTERNS.md collision resolution)</name>
  <files>.planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md, .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md (entire file — confirm existing sections 1-6 + locate the `## Cross-references` section at line 205 to insert new content AFTER it)
    - .planning/phases/10-freeze-rollback-drill/10-CONTEXT.md (D-20 line 108 — list of 4 new sections)
    - .planning/phases/10-freeze-rollback-drill/10-PATTERNS.md (lines 402-449 — collision note + renumber-to-7-10 recommendation + H2/H3/checklist shape)
    - .planning/phases/10-freeze-rollback-drill/10-RESEARCH.md (§Q6 for DynamoDB commands; §Q7 for termination protection; §Q8 for git tag — source of the exact commands embedded in section 7)
    - .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md (task 10-02-06 — the acceptance regex referencing sections 3-6 which MUST be updated to reference the renumbered sections 7-10 in this same task)
    - scripts/prewarm.py (the Phase 9 tool section 9 invokes)
    - scripts/demo-keepalive.sh (the Phase 9 tool section 8 invokes)
    - tests/test_narrative_eval_live.py (the Phase 9 test section 10 invokes)
  </read_first>
  <action>
    Two edits in one task:

    **Edit A — Append sections 7-10 to DEMO-RUNBOOK.md.**

    After the existing `## Cross-references` section (ends near line 205), append FOUR new H2
    sections:

    1. `## 7. T-48h Freeze Ceremony (D-18 / DEMO-04)` — one-line intro + five H3 subsections
       (`### 7.1 Reproducibility gate (D-19)`, `### 7.2 Drift gate (D-05)`, `### 7.3 Rollback drill (D-12 through D-17)`,
       `### 7.4 Stack lock (D-01 REVISED / D-02 / D-03)`, `### 7.5 DynamoDB backup (D-18 step 5)`,
       `### 7.6 FREEZE-MANIFEST.md population (D-08 REVISED / D-09 REVISED / D-10)`,
       `### 7.7 Tag cut + origin push (D-18 step 7 REVISED)`).

       Each H3 contains checklist items `- [ ]` and fenced bash code blocks with inline
       `# Expect: ...` comments. The stack lock block references
       `file://infrastructure/stack-policies/<foundation|agentcore|backend-api>-freeze.json`
       for each of the three stacks (CustomerTariff, CustomerTariffAgent, CustomerTariffApi) —
       EXPLICIT stack-to-file mapping (not a bash-shell-expansion trick). The DynamoDB backup
       block includes a wait-loop polling `BackupStatus` until `AVAILABLE`. The tag section
       includes `git push origin demo-v2.0` (revised D-18 step 7).

    2. `## 8. T-30m Keep-Alive Start (DEMO-05 / D-20)` — open tmux pane, `export BACKEND_API_URL=...`,
       run `bash scripts/demo-keepalive.sh`, confirm first ping succeeds before leaving pane.

    3. `## 9. T-10m Pre-Warm (DEMO-03 / D-20)` — `cd ui && npm run prewarm`, expect exit 0 +
       warm median <3000ms on all 3 personas.

    4. `## 10. T-eval Live Eval Harness Gate (DEMO-03 / D-20)` — `BACKEND_API_URL=... pytest tests/test_narrative_eval_live.py -m smoke`,
       expect green; note `?narrative=off` fallback if narrative layer is broken.

    Every `aws cloudformation ...` / `aws dynamodb ...` command in sections 7-10 carries
    `--profile cevo-dev25 --region us-east-1`.

    Preserve existing sections 1-6 untouched.

    **Edit B — Update 10-VALIDATION.md row 10-02-06.**

    The existing row 10-02-06 has the regex `^## §[3-6]` which matched the old numbering. Replace
    with the updated regex `^## (7|8|9|10)\. ` (expected count remains `4`). Update the comment
    in the row to note the renumber per PATTERNS.md line 406.

    Commit both files in one commit. Suggested message: `docs(10-02): extend DEMO-RUNBOOK with sections 7-10 for Phase 10 ceremony`.
  </action>
  <verify>
    <automated>
      grep -cE '^## (7|8|9|10)\. ' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md | grep -q '^4$'
      grep -q '^## 7\. T-48h Freeze Ceremony' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      grep -q 'git push origin demo-v2.0' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      grep -cE '^## [0-9]+\. ' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md | grep -q '^10$'
    </automated>
  </verify>
  <acceptance_criteria>
    <automated>
      # Four new H2 sections present with correct numbering:
      grep -cE '^## (7|8|9|10)\. ' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md | grep -q '^4$'

      # Each section title present:
      grep -q '^## 7\. T-48h Freeze Ceremony' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      grep -q '^## 8\. T-30m Keep-Alive Start' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      grep -q '^## 9\. T-10m Pre-Warm' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md
      grep -q '^## 10\. T-eval Live Eval Harness Gate' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md

      # Section 7 ceremony references all 3 freeze JSON files + all 3 CFN stack names:
      for s in foundation-freeze agentcore-freeze backend-api-freeze; do
        grep -q "$s" .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md || exit 1
      done
      for s in CustomerTariff CustomerTariffAgent CustomerTariffApi; do
        grep -q "$s" .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md || exit 1
      done

      # Section 7 backup has wait-loop for AVAILABLE:
      grep -q 'AVAILABLE' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md

      # Section 7 tag step includes origin push:
      grep -q 'git push origin demo-v2.0' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md

      # Existing sections 1-6 preserved (total 10 H2 numbered sections):
      grep -cE '^## [0-9]+\. ' .planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md | grep -q '^10$'

      # 10-VALIDATION.md row 10-02-06 regex updated for renumbered sections:
      grep -qE '7\|8\|9\|10|\(7\|8\|9\|10\)' .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md
    </automated>
  </acceptance_criteria>
  <done>
    DEMO-RUNBOOK.md now has exactly 10 H2 numbered sections (existing 6 + 4 new). Sections 7-10
    contain the full T-48h ceremony / T-30m keep-alive / T-10m pre-warm / T-eval harness content
    with correct command blocks, file references to the 10-01 JSON policies, wait-loops for
    async operations, and the revised-D-18 `git push origin demo-v2.0` step. 10-VALIDATION.md
    row 10-02-06 regex updated to match renumbered sections. No existing sections 1-6 prose
    modified.
  </done>
</task>

<task type="execute">
  <name>Task 5: Regression gate — confirm pytest baseline holds after 10-02 commits</name>
  <files>(none — validates Phase 10's no-new-runtime-code invariant)</files>
  <read_first>
    - .planning/phases/10-freeze-rollback-drill/10-CONTEXT.md (line 248 — baseline invariant)
    - .planning/phases/10-freeze-rollback-drill/10-VALIDATION.md (task 10-02-07 — exact gate)
  </read_first>
  <action>
    Run the offline pytest suite after all 10-02 commits land. Expected: `81 passed, 6 skipped`.
    Phase 10 adds no Python runtime code — any drift indicates unintended churn.

    ```bash
    pytest -m "not smoke" -x --tb=short
    ```

    If drift is observed:
    - Diagnose: is it the pip-compile resolution picking different dep versions? That would
      not normally change test results but could surface deprecation warnings that flip a test.
    - If it IS the requirements resolution, investigate whether the resolved versions match
      currently-deployed Lambda. Roll back the version drift if necessary (rewrite `.in` with
      `==<current-version>` per Pitfall 6, recompile, re-test).

    Pure regression check — no code changes expected on the happy path.
  </action>
  <verify>
    <automated>
      pytest -m "not smoke" -x --tb=short 2>&1 | tail -5 | grep -qE '81 passed, 6 skipped'
    </automated>
  </verify>
  <acceptance_criteria>
    <automated>
      # Verbatim from 10-VALIDATION.md task 10-02-07:
      pytest -m "not smoke" -x --tb=short 2>&1 | tail -5 | grep -qE '81 passed, 6 skipped'
    </automated>
  </acceptance_criteria>
  <done>
    `pytest -m "not smoke" -x --tb=short` exits 0 with `81 passed, 6 skipped`. Baseline invariant
    holds through 10-02 commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Active venv → pip-compile resolver | The executor's local Python environment influences which versions pip-compile pins. Version drift from the deployed Lambda is a real risk. |
| Committed `requirements*.txt` → fresh-venv install at T-48h | The hash-pinned lockfiles are the sole input to D-19's reproducibility gate. If hashes are stale or a wheel was yanked, install fails and the freeze blocks. |
| Committed FREEZE-MANIFEST.md template → operator at T-48h | The template is what the operator fills in. Malformed YAML schema would make the D-22 closeout parse-check fail. |
| Committed 10-DRILL-LOG.md skeleton → drill evidence trail | If the skeleton steps are in the wrong order or missing a step, the drill can't assert all 5 rollback-mechanism invariants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | `requirements*.txt` missing `--hash=sha256:` on any dep (supply-chain risk: silent wheel substitution) | mitigate | Task 1 acceptance_criteria rows 10-02-02 + 10-02-03 programmatically scan every non-comment non-directive line and fail on any missing hash. Row 10-02-04 verifies `pip install --require-hashes` succeeds end-to-end in a fresh venv. |
| T-10-02-02 | Tampering | pip-compile picks versions newer than currently-deployed Lambda (Pitfall 6) | mitigate | RESEARCH.md §Q2 + Pitfall 6 prescribe the `pip freeze` to `==<current-version>` tactic. Task 1 action recommends applying it. Plan SUMMARY records the resolved versions so reviewers can trace. |
| T-10-02-03 | Information disclosure | FREEZE-MANIFEST.md template accidentally embeds credential patterns | mitigate | Task 2 acceptance_criteria greps for `AKIA...`, `aws_secret_access_key`, and PEM headers; fails on any match. |
| T-10-02-04 | Denial of service (self) | FREEZE-MANIFEST.md YAML fence malformed — `yaml.safe_load` fails; 10-03 ceremony step 6 validation blocks tag cut | mitigate | Task 2 acceptance_criteria row 10-02-05 parses the fence via yaml.safe_load and asserts all 8 top-level keys exist. Catches malformed YAML before commit. |
| T-10-02-05 | Tampering | Break-glass unlock block references wrong stack-to-file mapping (e.g., foundation-allow-all applied to CustomerTariffAgent) | mitigate | Task 2 interface specifies explicit stack-to-file mapping (NOT a shell expansion); acceptance_criteria greps each specific file path reference. |
| T-10-02-06 | Tampering | 10-DRILL-LOG.md skeleton mis-orders steps (puts demo-v1.0 tag-revert before narrative=off URL flag) | mitigate | Task 3 acceptance_criteria includes a Python assertion verifying Step 1 mentions narrative=off, Step 2 mentions build:mock, Step 3 mentions demo-v1.0, with strict index ordering. |
| T-10-02-07 | Elevation of privilege | DEMO-RUNBOOK.md section 7 command block missing `--profile cevo-dev25` — operator runs against wrong AWS account | mitigate | Task 4 interface + action specify every aws-cli command carries `--profile cevo-dev25 --region us-east-1`. Executor audits every command in the plan SUMMARY. |
| T-10-02-08 | Repudiation | Section renumbering (sections 3-6 to 7-10) diverges from D-20 locked decision without traceability | accept — documented divergence | PATTERNS.md line 406 explicitly flags the collision and recommends the renumber. Task 4 action + plan SUMMARY record the renumber decision with PATTERNS.md citation. |
</threat_model>

<verification>
Overall plan checks (all automated):

1. Both `.in` files exist with correct syntax:
   ```
   test -f requirements.in && test -f requirements-dev.in
   grep -q '^-c requirements\.txt$' requirements-dev.in
   ```

2. Both `.txt` files regenerated, every dep hash-pinned.

3. Fresh-venv `pip install --require-hashes` succeeds.

4. FREEZE-MANIFEST.md parses as YAML and contains all 8 keys.

5. 10-DRILL-LOG.md has 5 drill-step H2s and a Commands appendix.

6. DEMO-RUNBOOK.md has sections 7-10 appended (10 H2 sections total).

7. pytest baseline holds: `81 passed, 6 skipped`.
</verification>

<success_criteria>
- `requirements.in` + `requirements-dev.in` created with `-c` constraint layering (not `-r` merge)
- `requirements.txt` + `requirements-dev.txt` regenerated with `--hash=sha256:` on every dep
- `pip install --require-hashes -r requirements-dev.txt` succeeds in a fresh venv (D-19 precondition)
- `.planning/phases/10-freeze-rollback-drill/FREEZE-MANIFEST.md` scaffolded with all 8 D-10 keys + pre-filled `bedrock_model_id` + break-glass block referencing 10-01 JSON files
- `.planning/phases/10-freeze-rollback-drill/10-DRILL-LOG.md` skeleton with 5 drill steps in speed-first order + Commands appendix
- `.planning/milestones/v1.0-phases/05-demo-hardening/DEMO-RUNBOOK.md` extended with sections 7-10 (renumbered from D-20 sections 3-6 per PATTERNS.md collision resolution)
- `.planning/phases/10-freeze-rollback-drill/10-VALIDATION.md` row 10-02-06 regex updated to match renumbered sections
- pytest baseline invariant holds: `81 passed, 6 skipped`

Maps to:
- SC-1 reproducibility: `.in` + hash-pinned `.txt` + fresh-venv install proof (artefact side; ceremony-side gate runs in 10-03)
- SC-3 manifest scaffold: FREEZE-MANIFEST.md template with all D-10 keys + break-glass block
- SC-5 drill scaffold: 10-DRILL-LOG.md skeleton with step ordering locked
- DEMO-RUNBOOK.md amendment: presenter runbook extension for Phase 10 operational flows
</success_criteria>

<output>
After completion, create `.planning/phases/10-freeze-rollback-drill/10-02-SUMMARY.md` including:

- Resolved pip-compile versions for every prod + dev dep (full `requirements.txt` dep list with
  versions + whether any `>=` floor drifted up vs. what was currently installed — Pitfall 6 audit)
- Whether the Pitfall-6 version-stability tactic was applied (yes / no, with rationale)
- `pip install --require-hashes` exit code from the fresh-venv test (must be 0)
- Confirmation that FREEZE-MANIFEST.md template parses via yaml.safe_load and contains all 8 keys
- Confirmation that 10-DRILL-LOG.md has 5 step headers in speed-first order and Commands appendix
- Explicit note that DEMO-RUNBOOK.md sections were renumbered from D-20 sections 3-6 to 7-10 per
  PATTERNS.md line 406 recommendation, AND that 10-VALIDATION.md row 10-02-06 regex was updated
  to match
- pytest summary line (`81 passed, 6 skipped` expected)
- Preconditions for plan 10-03: hash-pinned lockfiles exist + manifest scaffold exists + drill log
  skeleton exists + runbook amendments landed
</output>
