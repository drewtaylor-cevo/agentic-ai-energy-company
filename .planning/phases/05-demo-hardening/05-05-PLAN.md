---
phase: 05-demo-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - ".planning/phases/05-demo-hardening/05-VERIFICATION.md"
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02
  - UI-02
tags:
  - demo-hardening
  - rehearsal
  - latency

must_haves:
  truths:
    - "Presenter executes 2 full passes (cold + warm) against the live endpoint via ui/dist/ preview"
    - "All 3 personas (CUST-001, CUST-002, CUST-003) render correct Green + Cheapest cards in both passes"
    - "Invalid-format case (cust999) renders verbatim 400 error copy"
    - "Unknown-customer case (CUST-999) renders verbatim 404 error copy"
    - "Latency table records cold ms + warm median ms (n≥2) + verdict for each persona"
    - "Warm median for EVERY persona is <3000ms (UI-02 pass gate per D-09)"
    - "Cold latency is documented transparently but does NOT gate pass (D-09)"
  artifacts:
    - path: ".planning/phases/05-demo-hardening/05-VERIFICATION.md"
      provides: "Rehearsal section + latency evidence table appended (D-10 columns exactly)"
      contains: "Latency Evidence (D-10)"
      min_lines: 200
  key_links:
    - from: "presenter's Chrome DevTools Network panel"
      to: "ui/dist/ rendered via vite preview against live ApiEndpoint"
      via: "fetch() duration measured per persona"
      pattern: "warm median \\(ms, n="
    - from: "error copy rendered on screen"
      to: "ui/src/lib/errors.ts verbatim strings"
      via: "verbatim-match check during rehearsal"
      pattern: "That doesn't look like a customer ID"
---

<objective>
Execute two full rehearsal passes (cold + warm, D-14) of the demo against the live endpoint deployed in Plan 02 using the production `ui/dist/` bundle from Plan 03. Capture latency per persona using Chrome DevTools Network + phone stopwatch (D-08) and record the evidence in `05-VERIFICATION.md`. This is the gate for Phase 5 Success Criteria #1 (end-to-end persona sequence without failure) and #2 (<3s warm median for all personas).

Purpose: D-15 says the user (presenter) executes the rehearsal; Claude records the results. The presenter needs to know the demo works under real conditions before tagging `demo-v1.0`. This is also the rehearsal for the presenter themselves — they see every persona, every error path, and every latency number before demo day.

Output: `05-VERIFICATION.md` (created in Plan 04) is appended with a fully-populated "Rehearsal Evidence" section and "Latency Evidence" table. ROADMAP Success Criterion #1 and #2 flip from `⏳ PENDING (Plan 05)` to `✓ VERIFIED` (or `✗ FAIL` — if FAIL, the phase does not pass, and a gap-closure plan is needed before `demo-v1.0` is cut).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-demo-hardening/05-CONTEXT.md
@.planning/phases/05-demo-hardening/05-PATTERNS.md
@.planning/phases/05-demo-hardening/05-02-SUMMARY.md
@.planning/phases/05-demo-hardening/05-03-SUMMARY.md
@.planning/phases/05-demo-hardening/05-04-SUMMARY.md
@.planning/phases/05-demo-hardening/05-VERIFICATION.md
@.planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md
@.planning/phases/04-agent-assist-ui/04-UI-SPEC.md
@.planning/phases/04-agent-assist-ui/04-VERIFICATION.md
@ui/src/lib/errors.ts

<interfaces>
<!-- Verbatim error strings the rehearsal validates — source: ui/src/lib/errors.ts lines 17-25 -->
<!-- Any drift between on-screen copy and these strings is a bug (per <specifics> in 05-CONTEXT.md) -->

400 error copy (invalid format):
  "That doesn't look like a customer ID. Format is CUST followed by 3–6 digits."
  (note: U+2013 en-dash between "3" and "6", NOT a hyphen)

404 error copy (unknown customer):
  "No customer found for CUST-999. Check the ID and try again."
  (customer ID is echoed verbatim via template — here the user types CUST-999)

Expected rehearsal values per persona (authoritative source: Phase 1 seed data + Phase 4 smoke results):
- CUST-001 Sarah Chen (high usage):   Green $30.00/mo · $360.00/yr · EcoFlex 100  |  Cheapest $55.00/mo · $660.00/yr · Value 12
- CUST-002 Marcus Webb  (mid usage):   Green $16.90/mo                              |  Cheapest $30.98/mo
- CUST-003 Elena Vasquez (low usage):  Green $14.00/mo                              |  Cheapest $25.67/mo

Card order is stable: Green left, Cheapest right (UI-SPEC "Card order: Green first, Cheapest second").
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Presenter runs rehearsal Pass 1 (cold) + Pass 2 (warm) against live endpoint</name>
  <read_first>
    - .planning/phases/05-demo-hardening/05-CONTEXT.md (D-14 rehearsal scope, D-15 human execution, D-09 warm median gate)
    - .planning/phases/04-agent-assist-ui/04-UI-SPEC.md (locked error copy — verbatim strings to match)
    - ui/src/lib/errors.ts (source of truth for error copy characters including U+2013 en-dash)
    - .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md (the live endpoint being exercised)
  </read_first>
  <what-built>
    Two complete rehearsal passes executed by the presenter against the live primary `ui/dist/` bundle. Nothing new is built — this is an evidence-gathering human checkpoint. Claude does not touch the screen; the presenter does. Claude records the observations into the VERIFICATION.md in Task 2.

    Rehearsal structure (per pass, D-14):
    - 3 persona lookups: CUST-001, CUST-002, CUST-003 (expected values in <interfaces> above)
    - 1 invalid-format case: `cust999` (expected: 400 with verbatim 400 copy)
    - 1 unknown-customer case: `CUST-999` (expected: 404 with verbatim 404 copy with `CUST-999` echoed)
    - Latency measurement per persona via Chrome DevTools Network panel `fetch()` duration
    - Card visual check: both cards visible above the fold at 1280×800

    Two passes:
    - **Pass 1 (cold):** first invocations of the day. Cold latency is expected to be higher — recorded transparently.
    - **Pass 2 (warm):** same personas re-queried after Pass 1. Warm median across n≥2 samples per persona. This is the UI-02 gate per D-09.
  </what-built>
  <how-to-verify>
    Setup (run once, before Pass 1):
    ```bash
    # 1. Confirm the live API endpoint is responsive
    export BACKEND_API_URL=$(grep -oE 'https://[a-z0-9]+\.execute-api\.us-east-1\.amazonaws\.com[^ `|]*' .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md | head -1)
    curl -s -o /dev/null -w "%{http_code}\n" "$BACKEND_API_URL/recommendations/CUST-001"
    # Expected: 200 (the deploy from Plan 02 is still healthy)

    # 2. Start the primary preview
    cd ui && npm run preview
    # Expected: "Local:   http://127.0.0.1:4173/"
    # Leave this running in a terminal.
    ```

    Open Chrome at `http://127.0.0.1:4173/`. Resize window to 1280×800 (DevTools device mode or manual resize). Open DevTools → Network tab. Clear it between each persona lookup so only the one fetch is visible.

    **Pass 1 (cold):**
    1. Persona CUST-001: type `CUST-001`, press Enter. Start phone stopwatch on Enter, stop when both cards are visible. Read DevTools Network `recommendations/CUST-001` request duration.
       - Expected: Green `$30.00/mo · $360.00/yr · EcoFlex 100`, Cheapest `$55.00/mo · $660.00/yr · Value 12`.
       - Record: cold ms (DevTools), stopwatch ms, card values match yes/no.
    2. Persona CUST-002: type `CUST-002`, Enter. Record latency + values (expected Green `$16.90/mo`, Cheapest `$30.98/mo`).
    3. Persona CUST-003: type `CUST-003`, Enter. Record latency + values (expected Green `$14.00/mo`, Cheapest `$25.67/mo`).
    4. Invalid format: type `cust999` (no dash, lowercase — deliberately malformed), Enter.
       - Expected: the form normalizes to `CUST999`, regex fails client-side, error alert renders verbatim: `That doesn't look like a customer ID. Format is CUST followed by 3–6 digits.` (en-dash U+2013 between "3" and "6").
       - Record: verbatim copy match yes/no.
    5. Unknown customer: type `CUST-999`, Enter.
       - Expected: 404 from live API; error alert renders `No customer found for CUST-999. Check the ID and try again.`
       - Record: verbatim copy match yes/no.

    **Pause ~30 seconds** (stack warms; not too long or Lambda cools back off).

    **Pass 2 (warm):**
    1. Re-query CUST-001 twice in a row (for the n≥2 median). Record both DevTools latencies.
    2. Re-query CUST-002 twice in a row. Record both latencies.
    3. Re-query CUST-003 twice in a row. Record both latencies.
    4. Re-check invalid format + unknown customer cases (latency not tracked for these — just confirming error copy is still verbatim).

    Evidence to capture (paste into a chat message for Claude to convert into VERIFICATION.md):
    ```
    PASS 1 (cold):
      CUST-001: DevTools=<ms> stopwatch=<ms> values=<match/mismatch>  cards-above-fold=<yes/no>
      CUST-002: DevTools=<ms> stopwatch=<ms> values=<match/mismatch>  cards-above-fold=<yes/no>
      CUST-003: DevTools=<ms> stopwatch=<ms> values=<match/mismatch>  cards-above-fold=<yes/no>
      400 case: copy-verbatim=<yes/no> (cust999)
      404 case: copy-verbatim=<yes/no> (CUST-999)

    PASS 2 (warm) — re-queried each persona 2x for median:
      CUST-001: sample1=<ms> sample2=<ms> median=<ms>  values=<match>
      CUST-002: sample1=<ms> sample2=<ms> median=<ms>  values=<match>
      CUST-003: sample1=<ms> sample2=<ms> median=<ms>  values=<match>
      400 case: copy-verbatim=<yes/no>
      404 case: copy-verbatim=<yes/no>

    Any surprises / visual issues: <free text>
    ```

    After pasting results to Claude: Ctrl+C the `npm run preview` terminal to stop the server.
  </how-to-verify>
  <resume-signal>
    Type `approved` with the captured evidence block filled in. Do NOT approve if ANY of:
    - A persona's card values did not match the expected values in <interfaces>
    - Either error-copy case did not match verbatim (missing en-dash, wrong string)
    - Any warm median latency for any persona exceeded 3000ms
    - Either card was NOT visible above the fold at 1280×800
    - A persona call returned an error that wasn't expected

    If any of those: report as a gap — Phase 5 does not close, and we decide whether to re-deploy, debug Phase 1/2 seed data, or accept.
  </resume-signal>
  <acceptance_criteria>
    - User has pasted a completed evidence block matching the "Evidence to capture" template
    - Every `values=` field in the evidence block reads `match` (not `mismatch`)
    - Every `copy-verbatim=` field reads `yes`
    - Every `cards-above-fold=` field reads `yes`
    - Every `median=` value (Pass 2) is <3000ms
    - User has typed `approved`
    - `npm run preview` has been terminated (no stale local server left running)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — rehearsal is human-executed; evidence is the pasted data block captured from the user's session. Task 2 performs the automated check by asserting the data-block contents are written verbatim into 05-VERIFICATION.md and that all warm medians in that file parse as &lt;3000.</automated>
  </verify>
  <done>
    User has approved; all 3 personas passed cold + warm with values matching; both error paths passed verbatim-copy check; warm medians all <3000ms; evidence block captured for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Rehearsal + Latency sections to 05-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/05-demo-hardening/05-VERIFICATION.md (the existing file with Plan 04's No-CRM Audit — we APPEND to it, do NOT rewrite)
    - .planning/phases/05-demo-hardening/05-PATTERNS.md (Latency Evidence table shape, lines 115-131 — exact D-10 columns)
    - .planning/phases/05-demo-hardening/05-CONTEXT.md (D-09 warm median gate, D-10 column shape)
    - Task 1 evidence block (captured from the user's `approved` message)
  </read_first>
  <action>
    Read the existing `05-VERIFICATION.md` from disk. Locate the stub sections `## Rehearsal Evidence (filled by Plan 05)` and `## Latency Evidence (filled by Plan 05)` (placed there by Plan 04). REPLACE those stubs with the populated content below, using the Task 1 evidence block. Do NOT touch any other section (No-CRM Audit, frontmatter, Plan 07 stubs, etc.).

    Also update the ROADMAP Success Criteria table: flip rows 1 and 2 from `⏳ PENDING (Plan 05)` to `✓ VERIFIED` (or `✗ FAIL` if any criterion failed — in which case also flip the top-level `status:` in frontmatter to `failed` and STOP).

    Use the Edit tool to make precise replacements. Target text to replace (paste exact strings):

    **Replacement 1 — ROADMAP Success Criteria table row 1:**
    Find: `| 1 | End-to-end persona sequence without failure (all 3 personas, live endpoint) | ⏳ PENDING (Plan 05) | Rehearsal results — filled by Plan 05 |`
    Replace with: `| 1 | End-to-end persona sequence without failure (all 3 personas, live endpoint) | ✓ VERIFIED | Rehearsal Pass 1 (cold) + Pass 2 (warm) — see "Rehearsal Evidence" section below; all 3 personas returned correct Green + Cheapest values in both passes |`

    **Replacement 2 — ROADMAP Success Criteria table row 2:**
    Find: `| 2 | <3s warm-median latency for all personas (UI-02) | ⏳ PENDING (Plan 05) | Latency table — filled by Plan 05 |`
    Replace with: `| 2 | <3s warm-median latency for all personas (UI-02) | ✓ VERIFIED | Warm median <3000ms for all 3 personas per "Latency Evidence" table below (D-09 gate) |`

    **Replacement 3 — Rehearsal section:**
    Find: `## Rehearsal Evidence (filled by Plan 05)\n\n(Plan 05 appends: 2 passes × 3 personas × latency + 2 error paths with verbatim error-copy check.)`
    Replace with:

    ```markdown
    ## Rehearsal Evidence (D-14, D-15)

    **Rehearsal date:** <ISO-8601 UTC — Task 1 approval timestamp>
    **Executed by:** user (presenter) — per D-15
    **Recorded by:** Claude — per D-15
    **Environment:** live `CustomerTariffApi` endpoint (see 05-DEPLOY-OUTPUTS.md), primary `ui/dist/` bundle via `npm run preview` (Plan 03), Chrome DevTools Network panel for latency (D-08)

    ### Pass 1 — Cold (first invocations of the day)

    | Step | Input | Expected | Observed | Latency (DevTools) | Cards above fold | Status |
    |------|-------|----------|----------|--------------------|------------------|--------|
    | 1 | CUST-001 | Green `$30.00/mo · $360.00/yr · EcoFlex 100` + Cheapest `$55.00/mo · $660.00/yr · Value 12` | <from evidence block> | <ms> | <yes/no> | ✓ PASS / ✗ FAIL |
    | 2 | CUST-002 | Green `$16.90/mo` + Cheapest `$30.98/mo` | <from evidence block> | <ms> | <yes/no> | ✓ PASS / ✗ FAIL |
    | 3 | CUST-003 | Green `$14.00/mo` + Cheapest `$25.67/mo` | <from evidence block> | <ms> | <yes/no> | ✓ PASS / ✗ FAIL |
    | 4 | cust999 (invalid format) | 400 alert with verbatim `That doesn't look like a customer ID. Format is CUST followed by 3–6 digits.` (U+2013 en-dash) | <from evidence block> | n/a | n/a | ✓ PASS / ✗ FAIL |
    | 5 | CUST-999 (unknown) | 404 alert with verbatim `No customer found for CUST-999. Check the ID and try again.` | <from evidence block> | n/a | n/a | ✓ PASS / ✗ FAIL |

    ### Pass 2 — Warm (re-queried personas, 2 samples each for the n≥2 median)

    | Step | Input | Sample 1 (ms) | Sample 2 (ms) | Median (ms) | Expected values match | Status |
    |------|-------|---------------|---------------|-------------|-----------------------|--------|
    | 1 | CUST-001 | <ms> | <ms> | <median> | <yes/no> | ✓ PASS / ✗ FAIL |
    | 2 | CUST-002 | <ms> | <ms> | <median> | <yes/no> | ✓ PASS / ✗ FAIL |
    | 3 | CUST-003 | <ms> | <ms> | <median> | <yes/no> | ✓ PASS / ✗ FAIL |

    Error paths re-verified in Pass 2 (no latency tracking — verbatim-copy check only):
    - 400 case (`cust999`): copy verbatim match: <yes/no>
    - 404 case (`CUST-999`): copy verbatim match: <yes/no>

    ### Observations / notes

    <free-text from evidence block — any surprises, visual quirks, or presenter comments>

    ## Latency Evidence (D-10)

    **Measurement method:** Chrome DevTools Network panel `fetch()` duration cross-checked with phone stopwatch (D-08). Warm median is computed across n≥2 invocations per persona (D-09). Gate: `<3000ms warm median` is the pass threshold. Cold latency is recorded transparently but does NOT gate the phase pass (D-09).

    | Persona  | Cold (ms) | Warm median (ms, n=2) | Verdict (warm vs 3000ms) |
    |----------|-----------|------------------------|--------------------------|
    | CUST-001 | <cold ms from Pass 1>  | <median ms from Pass 2>  | ✓ PASS / ✗ FAIL |
    | CUST-002 | <cold ms from Pass 1>  | <median ms from Pass 2>  | ✓ PASS / ✗ FAIL |
    | CUST-003 | <cold ms from Pass 1>  | <median ms from Pass 2>  | ✓ PASS / ✗ FAIL |

    **Gate decision:** <PASS if every warm-median row is <3000ms; FAIL otherwise — in which case Phase 5 does NOT close>.
    ```

    **Do not modify** the No-CRM Audit section, the `## Environment Lock Evidence (filled by Plan 07)` stub, the frontmatter keys other than possibly flipping `status: in_progress` → `status: in_progress` (stay in-progress; Plan 07 flips to `passed`), or the file trailer.

    After editing, verify the file still parses cleanly and all three ROADMAP criteria rows are ✓ VERIFIED.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; grep -q '^## Rehearsal Evidence (D-14, D-15)' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; grep -q '^## Latency Evidence (D-10)' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; grep -q '### Pass 1 — Cold' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; grep -q '### Pass 2 — Warm' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; ! grep -q '⏳ PENDING (Plan 05)' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; grep -q '✓ VERIFIED' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; ! grep -q '&lt;from evidence block&gt;' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; ! grep -q '&lt;ms&gt;' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; ! grep -q '&lt;median&gt;' .planning/phases/05-demo-hardening/05-VERIFICATION.md &amp;&amp; echo "VERIFICATION rehearsal + latency sections populated"</automated>
  </verify>
  <acceptance_criteria>
    - `05-VERIFICATION.md` still exists and contains the Plan 04 No-CRM Audit section unchanged (`grep -q "## No-CRM Audit" .planning/phases/05-demo-hardening/05-VERIFICATION.md` returns match)
    - New heading `## Rehearsal Evidence (D-14, D-15)` is present
    - New heading `## Latency Evidence (D-10)` is present
    - Sub-headings `### Pass 1 — Cold` and `### Pass 2 — Warm` both exist
    - ZERO remaining `⏳ PENDING (Plan 05)` strings in the file
    - ZERO remaining `<from evidence block>`, `<ms>`, `<median>`, `<cold ms from Pass 1>` placeholder strings (all substituted with actual numbers/values)
    - Latency table for every persona row has a concrete integer in the "Warm median" column and `✓ PASS` in the "Verdict" column (all <3000ms)
    - ROADMAP Success Criteria rows 1 and 2 both show `✓ VERIFIED` (grep for `✓ VERIFIED` returns ≥3 matches — row 3 was done in Plan 04)
    - Stub `## Environment Lock Evidence (filled by Plan 07)` still exists unchanged (Plan 07 will populate it)
  </acceptance_criteria>
  <done>
    VERIFICATION.md contains the complete rehearsal narrative, a populated latency table with real numbers, and all three ROADMAP success criteria marked verified. Phase 5 is functionally green — only the environment-lock ceremony (Plan 06/07) remains.
  </done>
</task>

</tasks>

<verification>
Overall Plan 05 completion checks:

```bash
# Rehearsal + latency sections are present and populated
grep -c '^## Rehearsal Evidence' .planning/phases/05-demo-hardening/05-VERIFICATION.md   # Expected: 1
grep -c '^## Latency Evidence' .planning/phases/05-demo-hardening/05-VERIFICATION.md     # Expected: 1

# All 3 ROADMAP criteria now verified
grep -c '✓ VERIFIED' .planning/phases/05-demo-hardening/05-VERIFICATION.md   # Expected: >=3

# No pending-from-plan-05 stubs remain
grep -c '⏳ PENDING (Plan 05)' .planning/phases/05-demo-hardening/05-VERIFICATION.md   # Expected: 0

# No-CRM section from Plan 04 still intact
grep -c '## No-CRM Audit' .planning/phases/05-demo-hardening/05-VERIFICATION.md   # Expected: 1

# Plan 07 stub still waiting
grep -c '(filled by Plan 07)' .planning/phases/05-demo-hardening/05-VERIFICATION.md   # Expected: >=1
```
</verification>

<success_criteria>
- Rehearsal executed by user; approved with evidence block captured
- All 3 personas returned correct Green + Cheapest values in both cold and warm passes
- Both error paths (400 and 404) rendered verbatim error copy from `ui/src/lib/errors.ts`
- Warm median for every persona is <3000ms (UI-02 gate cleared)
- `05-VERIFICATION.md` has a complete rehearsal narrative + D-10 latency table with real numbers
- ROADMAP Success Criteria rows 1 and 2 flipped from `⏳ PENDING` to `✓ VERIFIED`
- No-CRM Audit section from Plan 04 preserved intact
- Plan 07 stub sections still present for environment-lock content
</success_criteria>

<output>
Create `.planning/phases/05-demo-hardening/05-05-SUMMARY.md` recording:
- Rehearsal timestamps (cold pass, warm pass)
- Summary table of persona results (expected vs observed)
- Summary latency table (cold ms + warm median per persona)
- Error-copy verbatim match summary
- Any presenter observations worth noting in the runbook
</output>
