---
phase: 13.1
slug: agent-01-gap-closure-latency-short-circuit-404-detection
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 13.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (Python 3.13) + vitest (UI untouched this phase) |
| **Config file** | `pytest.ini` (markers: `smoke`) |
| **Quick run command** | `pytest tests/test_bill_shock_flow.py tests/test_backend_api_handler.py -x` |
| **Full suite command** | `pytest -m "not smoke"` |
| **Smoke suite command** | `AWS_PROFILE=cevo-dev25 AWS_DEFAULT_REGION=us-east-1 BACKEND_API_URL=https://y9w9qwegwe.execute-api.us-east-1.amazonaws.com/ pytest -m smoke -x` |
| **Estimated runtime** | offline ~45s / smoke ~3-5min (includes 90s CloudWatch propagation sleeps per Plan 07 D-21) |

---

## Sampling Rate

- **After every task commit:** Run the quick command above (focused pytest on the touched test file)
- **After every plan wave:** Run the full offline suite
- **Before `/gsd-verify-work`:** Full offline suite must be green; Wave 2 smoke suite must be green post-ceremony
- **Max feedback latency:** 60 seconds (offline) / 5 minutes (smoke post-deploy)

---

## Per-Task Verification Map

> Task IDs will be populated by gsd-planner. This table stubs the expected invariants each task must guard — planner MUST extend with concrete Task IDs and commands when it writes PLAN.md files.

| Plan (expected) | Wave | Invariant | Test Type | Automated Command | File Exists | Status |
|-----------------|------|-----------|-----------|-------------------|-------------|--------|
| 01 — Prompt short-circuit + empty-billing STOP | 1 | `_BASE_SYSTEM_PROMPT` retains ARITHMETIC INTEGRITY rules 2-7 verbatim | unit (grep) | `grep -c "ARITHMETIC INTEGRITY" agent/agent.py` → `>=1` | ✅ | ⬜ pending |
| 02 — Offline regression guards (mock BedrockModel) | 1 | CUST-001/002 emit ≤2 reasoning_trace entries; CUST-003 emits exactly 3 | unit (real Strands + MockedModelProvider) | `pytest tests/test_bill_shock_flow.py::TestShortCircuit -x` | ❌ W0 | ⬜ pending |
| 02 — Offline empty-billing agent-side test | 1 | InMemoryProvider + empty billing → agent emits errorMessage shape (no green/cheapest keys) | unit | `pytest tests/test_bill_shock_flow.py::TestEmptyBilling -x` | ❌ W0 | ⬜ pending |
| 03 — api_lambda sentinel | 1 | `plan_id == "UNKNOWN"` on either track → HTTP 404 | unit | `pytest tests/test_backend_api_handler.py::test_unknown_customer_sentinel_returns_404 -x` | ❌ W0 | ⬜ pending |
| 03 — api_lambda primary heuristic unchanged | 1 | `no green/cheapest keys` → HTTP 404 (existing behaviour) | unit | `pytest tests/test_backend_api_handler.py -k "not_found" -x` | ✅ | ⬜ pending |
| 04 — CUST-999 smoke | 2 | `GET /recommendations/CUST-999` returns HTTP 404 | smoke | `pytest -m smoke tests/test_backend_api_smoke.py::test_unknown_customer_returns_404 -x` | ✅ | ⬜ pending |
| 04 — Non-shock 2-tool live canary | 2 | CUST-001 live response has `len(reasoning_trace) <= 2` | smoke | `pytest -m smoke tests/test_narrative_eval_live.py::test_agent01_non_shock_stays_2_tools -x` | ❌ W0 | ⬜ pending |
| 04 — Per-flow latency gate | 2 | CUST-001 warm p95 under 3000ms; CUST-003 warm p95 under 2500ms (gate — may be verification finding) | smoke | `BACKEND_API_URL=... python3 scripts/prewarm.py` exit 0 | ✅ | ⬜ pending |
| 04 — Pre/post byte-equivalence gate | 2 | Savings fields (saving_monthly, saving_annual, plan_id on both tracks) byte-equal pre vs post for CUST-001/002/003. `reasoning_trace` EXCLUDED (shape expected to change). | smoke | diff pre/post baselines | ❌ W0 | ⬜ pending |
| 05 — Documentation sweep | 2 | CLAUDE.md + DEMO-RUNBOOK.md addenda landed | unit (grep) | `grep -q "plan_id == 'UNKNOWN'" CLAUDE.md && grep -q "2-tool" CLAUDE.md` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Infrastructure-new tests the planner MUST ensure exist before Wave 1 tasks run:

- [ ] `tests/test_bill_shock_flow.py::TestShortCircuit` — mock-BedrockModel regression (D-13.1-03/15)
- [ ] `tests/test_bill_shock_flow.py::TestEmptyBilling` — agent-side empty-billing guard (D-13.1-09)
- [ ] `tests/test_backend_api_handler.py::test_unknown_customer_sentinel_returns_404` — handler sentinel (D-13.1-08)
- [ ] `tests/test_narrative_eval_live.py::test_agent01_non_shock_stays_2_tools` — live 2-tool canary (D-13.1-16)
- [ ] `tests/conftest.py` OR inline fixture — `MockedModelProvider` + `StrictMockedModelProvider` from RESEARCH.md (D-13.1-15)
- [ ] `.planning/phases/13.1-*/baseline/{pre,post}/{customer_id}.json` capture scripts (D-13.1-18)

*Framework: pytest already installed via `requirements-dev.txt`; Strands 1.37.0 already pinned; no new deps.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Stack-policy lift ceremony (2-stack) | D-13.1-11, D-13.1-17 | `aws cloudformation set-stack-policy` + `cdk deploy` is intentionally operator-gated (destructive to frozen stacks); autonomous: false per Phase 13 Plan 08 precedent | See DEMO-RUNBOOK.md §8 — apply `stack-policies/allow-all.json` to `CustomerTariffAgent` and `CustomerTariffApi`, run `cdk deploy CustomerTariffAgent CustomerTariffApi`, verify, re-apply `stack-policies/deny-update.json` |
| Live demo visual smoke (UI) | UI-01 "visible reasoning" | Post-ceremony UI walkthrough confirms CUST-001 trace renders as collapsed "▶ 2 steps:" row, CUST-003 as 3-step row | Open deployed UI, look up CUST-001 (expect 2-step trace), CUST-003 (expect 3-step trace), CUST-999 (expect 404 / "customer not found" UI state) |
| CLAUDE.md + DEMO-RUNBOOK.md review | D-13.1-20/21 | Documentation accuracy is a human-judged property | Read the addenda; confirm sentinel rationale and 2-tool note are clear |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the 6 items above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s offline / 5min smoke
- [ ] `nyquist_compliant: true` set in frontmatter after planner populates Task IDs

**Approval:** pending
