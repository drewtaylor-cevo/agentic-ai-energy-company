---
phase: 13.1-agent-01-gap-closure-latency-short-circuit-404-detection
plan: 01
subsystem: agent
tags:
  - prompt
  - agent
  - short-circuit
  - empty-billing-stop
requirements:
  - AGENT-01a
  - LD-4
  - D-12
dependency_graph:
  requires: []
  provides:
    - _BASE_SYSTEM_PROMPT contains SHORT-CIRCUIT RULE (D-13.1-14)
    - _BASE_SYSTEM_PROMPT contains EMPTY BILLING STOP RULE (D-13.1-12)
    - Tool 2 / tool 3 cross-references to new rules in AVAILABLE TOOLS block
  affects:
    - Plan 02 (mock-model shape assertions — real prompt is the system-under-test)
    - Plan 03 (api_lambda sentinel — pairs with the prompt-side STOP rule)
    - Plan 04 (Wave 2 ceremony — container rebuild via `cdk deploy CustomerTariffAgent`)
tech_stack:
  added: []
  patterns:
    - Prompt-clause amendment (insert between AVAILABLE TOOLS and ARITHMETIC INTEGRITY)
    - VERBATIM preservation of RULES 1-7 (SAV-03 regression gate)
key_files:
  created: []
  modified:
    - agent/agent.py
decisions:
  - D-13.1-12 persona-framing prose over is_shock=False (avoids contradicting the 2-tool observed shape)
  - Rule 1 preserved verbatim; new SHORT-CIRCUIT RULE overrides preference-ordering for non-shock personas
  - EMPTY BILLING STOP RULE anchored on `errorMessage` + "customer not found" (matches D-04 fallback shape)
metrics:
  duration_minutes: 1
  tasks_completed: 1
  files_modified: 1
  completed_date: "2026-04-29"
---

# Phase 13.1 Plan 01: _BASE_SYSTEM_PROMPT Gap-Closure Clauses Summary

Prompt-side fix for Phase 13 Gap 1 (latency) and Gap 2 (404 detection): `_BASE_SYSTEM_PROMPT` now instructs Sonnet 4.6 to take the 2-tool `get_hardship_flag → simulate_savings` path on non-shock personas and to STOP (no `simulate_savings`, no UNKNOWN placeholder tracks) when `get_billing_history` returns empty.

## Objective Recap

Amend `agent/agent.py::_BASE_SYSTEM_PROMPT` to add two load-bearing clauses:

1. **SHORT-CIRCUIT RULE (D-13.1-14)** — non-shock customers skip both `detect_bill_shock` and `get_billing_history`; the 2-tool path is complete. Closes Gap 1 (17.2s/19.7s warm latency vs 3000ms/2500ms gates caused by the preference-ordered 3-tool flow on all personas).
2. **EMPTY BILLING STOP RULE (D-13.1-12)** — empty `get_billing_history` → stop, no `simulate_savings`, no `UNKNOWN` placeholder tracks, return `{"errorMessage": "customer not found"}`. Closes Gap 2 (CUST-999 returning HTTP 200 with synthetic tracks).

Rules 2-7 of the ARITHMETIC INTEGRITY block preserved VERBATIM per D-13.1-12; SAV-03 regression gate (byte-for-byte / "TOOL OUTPUT IS THE SOURCE OF TRUTH" / "Never return only one track" fingerprints) held intact.

## What Was Done

### Task 01.1 — Amend `_BASE_SYSTEM_PROMPT`

- **File:** `agent/agent.py`
- **Commit:** `c5c7562` — `feat(13.1-01): add SHORT-CIRCUIT + EMPTY BILLING STOP clauses to _BASE_SYSTEM_PROMPT`
- **Insertion line range:** Lines 601-624 of post-edit `agent/agent.py` (inserted between "Do not call unnecessary tools…" at line 599 and "ARITHMETIC INTEGRITY" which moved from line 600 to line 626).
  - Lines 601-613: SHORT-CIRCUIT RULE block (13 lines including header)
  - Lines 615-624: EMPTY BILLING STOP RULE block (10 lines including header)
- **Also updated:** Tool 2 (`detect_bill_shock`) and tool 3 (`get_billing_history`) bullet descriptions at lines 587-594 to cross-reference the new rules (prose-only rewording, no semantic change to the tool contract).
- **Preserved VERBATIM:** ARITHMETIC INTEGRITY block at new lines 626-653 (RULES 1-7 at lines 642-653 byte-equal to pre-edit).
- **Untouched:** `SYSTEM_PROMPT = _BASE_SYSTEM_PROMPT + "\n\n" + NARRATIVE_PROMPT` concatenation at line 657; model literal `us.anthropic.claude-sonnet-4-6` at line 662; `_four_tool_cap` wiring at lines 670-679; bi-mode imports; `RecommendationResponse` / `ReasoningTraceEntry` schemas; `invoke()` function; no `Agent(max_iterations=...)` added.

### `git diff --stat agent/agent.py`

```
 agent/agent.py | 40 +++++++++++++++++++++++++++++++++-------
 1 file changed, 33 insertions(+), 7 deletions(-)
```

Single-file surgical edit, +33/-7 lines — within the plan's stated 30-50 line delta range. Net +26 lines reflects the two new prose blocks (SHORT-CIRCUIT RULE 13 lines + EMPTY BILLING STOP RULE 10 lines + cross-reference prose 3 net lines − original tool 2/3 prose 7 lines replaced).

## Acceptance Criteria — All 13 Gates Satisfied

| # | Gate | Expected | Actual |
|---|------|----------|--------|
| 1 | `grep -c "SHORT-CIRCUIT RULE" agent/agent.py` | >= 1 | 1 ✓ |
| 2 | `grep -c "EMPTY BILLING STOP RULE" agent/agent.py` | >= 1 | 2 ✓ |
| 3 | `grep -c "errorMessage" agent/agent.py` | >= 1 | 4 ✓ |
| 4 | `grep -c "customer not found" agent/agent.py` | >= 1 | 1 ✓ |
| 5 | `grep -c "ARITHMETIC INTEGRITY" agent/agent.py` | >= 1 | 1 ✓ |
| 6 | `grep -c "byte-for-byte" agent/agent.py` (Rule 2 fingerprint) | >= 1 | 1 ✓ |
| 7 | `grep -c "TOOL OUTPUT IS THE SOURCE OF TRUTH" agent/agent.py` | >= 1 | 1 ✓ |
| 8 | `grep -c "Never return only one track" agent/agent.py` (Rule 5) | >= 1 | 1 ✓ |
| 9 | `grep -c max_iterations agent/agent.py` (4-tool cap invariant) | EXACTLY 0 | 0 ✓ |
| 10 | `grep -c "us.anthropic.claude-sonnet-4-6" agent/agent.py` | >= 1 | 1 ✓ |
| 11 | `grep -Fc "SYSTEM_PROMPT = _BASE_SYSTEM_PROMPT" agent/agent.py` | >= 1 | 1 ✓ |
| 12 | `python3 -c "import ast; ast.parse(open('agent/agent.py').read())"` | exit 0 | `SYNTAX OK` ✓ |
| 13 | `pytest -m "not smoke" tests/test_bill_shock_flow.py tests/test_schema.py tests/test_agent_tools.py -x -q` | exit 0 | **74 passed**, 1 warning, 3.53s ✓ |

Additional bonus check (acceptance-criteria row 14): `_BASE_SYSTEM_PROMPT = "` still exists as module-level assignment — count = 1 ✓.

## Prompt Order — Post-edit Layout

Confirmed order of clauses inside `_BASE_SYSTEM_PROMPT`:

1. Header + "Your ONLY job" preamble (lines 572-580)
2. **AVAILABLE TOOLS** (preference-ordered, with tool 2/3 cross-references) (lines 582-597)
3. "Do not call unnecessary tools" one-liner (line 599)
4. **SHORT-CIRCUIT RULE (Phase 13.1 D-13.1-14)** — NEW (lines 601-613)
5. **EMPTY BILLING STOP RULE (Phase 13.1 D-13.1-12)** — NEW (lines 615-624)
6. **ARITHMETIC INTEGRITY (SAV-03, extended)** — unchanged (lines 626-640)
7. **RULES 1-7** — byte-for-byte VERBATIM preserved (lines 642-653)

Concatenation with `NARRATIVE_PROMPT` at line 657 is untouched.

## Deviations from Plan

None — plan executed exactly as written. The plan's suggested prose for both new clauses was adopted verbatim (minor whitespace normalisation only). Tool 2/3 description rewording matches the plan's proposed replacement text line-for-line.

## Verification Run

```
$ pytest -m "not smoke" tests/test_bill_shock_flow.py tests/test_schema.py tests/test_agent_tools.py -x -q
........................................................................ [ 97%]
..                                                                       [100%]
74 passed, 1 warning in 3.53s
```

Suite includes:
- `tests/test_bill_shock_flow.py` — AGENT-01 bill-shock flow offline tests (`TestCrossPersonaCanary` included)
- `tests/test_schema.py` — Pydantic schema + `TestReasoningTraceEntryExemption` (D-11 exemption counter-test)
- `tests/test_agent_tools.py` — tool wrapper + bi-mode import tests

The warning is an unrelated pydantic-v2 deprecation in `bedrock_agentcore.runtime.context` — pre-existing.

## Threat Surface

No new network endpoints, auth paths, file-access patterns, or schema changes introduced. Prompt edit only — runtime surface unchanged. Threat register items T-13.1-01-01 through T-13.1-01-05 addressed in-task:

- **T-13.1-01-01 (ARITHMETIC INTEGRITY drift):** mitigated — grep-gated canonical phrases "byte-for-byte", "TOOL OUTPUT IS THE SOURCE OF TRUTH", "Never return only one track" all present.
- **T-13.1-01-02 (accidental 4-tool cap removal):** mitigated — `grep -c max_iterations agent/agent.py == 0` verified; `_four_tool_cap` HookProvider wiring at ~line 670 not touched.
- **T-13.1-01-03 (SAV-03 regression):** mitigated — rules 2-7 VERBATIM; Plan 04 ceremony will re-verify byte-equivalence live.
- **T-13.1-01-04 (extra-tool-call latency regression):** accepted — Plan 02 offline guard + Plan 04 smoke canary + prewarm gate catch it before ceremony close.
- **T-13.1-01-05 (customer_id leak):** mitigated — new prose contains no customer_ids.

## Forward Notes

### To Plan 02 (Wave 2 mock-model assertions)
- This prompt is the system-under-test for `TestShortCircuit` / `TestEmptyBillingStop` / `TestCrossPersonaCanary` shape assertions against `MockedModelProvider`.
- If Sonnet 4.6 observably calls `detect_bill_shock` before short-circuiting (even once, to sample the shock flag), the plan anticipates that and gates on the `reasoning_trace` containing EXACTLY `[get_hardship_flag, simulate_savings]` for CUST-001 / CUST-002 per D-13.1-14.
- Any reword needed to achieve that observed shape lands as a follow-up commit on this branch in Wave 2 iteration — NOT a re-plan.
- The persona-framing ("NON-SHOCK customers (the common case)") was chosen over `is_shock=False` phrasing precisely because the D-13.1-14 test contract is the observed 2-tool shape, not the mechanism.

### To Plan 04 (Wave 2 ceremony)
- `cdk diff CustomerTariffAgent` is EXPECTED non-zero — the prompt edit rebuilds the container per Assumption A3 in `13.1-CONTEXT.md`.
- `cdk deploy CustomerTariffAgent` must run after stack-policy lift + must be followed by byte-equivalence gate on CUST-001/002/003 (SAV-03 preservation) + `TestCrossPersonaCanary` re-run (C5 fabrication guard) per CLAUDE.md D-22.
- No other stack needs touching in Wave 2 unless Plan 03 (sentinel) lands — in which case `CustomerTariffApi` is also in-scope.

### To Plan 05 (ceremony retrospective)
- Short-circuit observability: reasoning_trace summaries remain code-composed in `agent/reasoning/summaries.py`; no D-15 dual-gate applied to summaries (D-11 exemption preserved).
- Cross-persona canary (Elena vs Marcus reasoning_trace byte-diff) unchanged — C5 fabrication regression guard still intact because SAV-03 rules 2-7 preserved.

## Self-Check: PASSED

- [x] `agent/agent.py` exists and has been modified (1 file changed, +33/-7 lines)
- [x] Commit `c5c7562` exists in git log: `git log --oneline -1` → `c5c7562 feat(13.1-01): add SHORT-CIRCUIT + EMPTY BILLING STOP clauses to _BASE_SYSTEM_PROMPT`
- [x] All 13 automated verification gates passed (see Acceptance Criteria table)
- [x] `pytest -m "not smoke" tests/test_bill_shock_flow.py tests/test_schema.py tests/test_agent_tools.py -x -q` → 74 passed, 0 failed
- [x] Python syntax valid
- [x] No deletions in commit, no untracked files
- [x] No modifications to STATE.md / ROADMAP.md (parallel-worktree mode)
