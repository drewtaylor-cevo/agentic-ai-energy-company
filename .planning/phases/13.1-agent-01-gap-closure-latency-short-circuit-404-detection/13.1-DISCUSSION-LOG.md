# Phase 13.1: AGENT-01 Gap Closure — Discussion Log

**Discussion date:** 2026-04-29
**Facilitated by:** `/gsd-discuss-phase 13.1` (default mode)
**Inherits:** `.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13.1-REMEDIATION-DECISIONS.md` — 11 decisions (D-13.1-01 through D-13.1-11) pre-locked during `/gsd-plan-phase 13 --gaps` routing. This session added D-13.1-12 through D-13.1-22.

## Session Flow

### Pre-flight

- No `.continue-here.md` blocking anti-patterns found in phase dir.
- No existing SPEC.md or CONTEXT.md for 13.1 (fresh discussion).
- No interrupted checkpoint.
- Phase 13 CONTEXT.md (33 decisions) and VERIFICATION.md (2 P0 gaps) loaded as primary inheritance.
- 13.1-REMEDIATION-DECISIONS.md loaded as the primary locked-decisions inheritance.

### Gray areas presented (multiSelect)

User selected ALL FOUR:

1. **Empty-billing response shape** — what agent emits when `get_billing_history` returns `[]`.
2. **api_lambda sentinel rule** — exact heuristic at `api_lambda/handler.py:152`.
3. **Short-circuit trigger ordering** — whether `get_hardship_flag` still fires for non-shock personas.
4. **Offline test fixture approach** — synthetic AgentResult vs real-Strands-with-mocked-Bedrock.

---

## Area 1 — Empty-billing response shape

**Question:** When `get_billing_history` returns `[]`, what shape should the agent produce?

**Options presented:**

| Option | Summary |
|---|---|
| **A — Prompt instructs raise/skip tracks (Recommended)** | System prompt STOP rule; reuse existing D-04 `{errorMessage}` shape; zero schema changes. Primary 404 = existing "no green/cheapest keys" heuristic. |
| B — Raise `RuntimeError` in `@tool get_billing_history` wrapper | Strands retry-loop risk; needs research. |
| C — New Pydantic `NotFoundResponse` union branch | Bleeds Phase 14's `kind` union scope into 13.1. |

**User selected:** **A — Prompt instructs raise/skip tracks.**

**Decision:** D-13.1-12 — prompt-driven STOP rule; zero schema changes; existing `{errorMessage}` shape reused.

**Scope guardrail invoked:** Option C was redirected to Phase 14 scope (LD-2 reserves `kind: hardship` as the first union branch).

---

## Area 2 — api_lambda sentinel rule

**Question:** What heuristic does `api_lambda/handler.py:152` add as defence-in-depth?

**Options presented:**

| Option | Summary |
|---|---|
| **A — `plan_id == "UNKNOWN"` on EITHER track (Recommended)** | String match on exact sentinel Sonnet 4.6 emitted live (CEREMONY-LOG §404 detection). Narrow, symmetric, no false-positive risk. |
| B — `saving_monthly == 0.0` on BOTH tracks | False-positive risk if any real persona has both tracks at $0. |
| C — Both combined (OR) | Overkill for defence-in-depth that SHOULD never fire. |

**User selected:** **A — `plan_id == "UNKNOWN"` on EITHER track.**

**Decision:** D-13.1-13 — exact heuristic pinned in CONTEXT.md with code snippet. Sentinel sits immediately after the existing line-152 `"no green/cheapest keys"` branch. Symmetric across both tracks.

---

## Area 3 — Short-circuit trigger ordering

**Question:** What tool sequence should the prompt drive for non-shock personas?

**Options presented:**

| Option | Summary |
|---|---|
| **A — 2-tool: hardship → simulate (Recommended)** | Hardship always fires (preserves Phase 14 foreshadow); non-shock drops 3→2 tools; Elena stays 3-tool. |
| B — 1-tool simulate-only for non-shock | Breaks Phase 14 hardship-first assumption; demo-thin trace. |
| C — LLM-decides (status quo with tighter wording) | What Plan 03 did; Gap 1 resulted. Rejected in D-13.1-01. |

**User selected:** **A — 2-tool hardship → simulate.**

**Decision:** D-13.1-14 — flow shapes pinned:
- CUST-001 Sarah (non-shock): `hardship → simulate` (2 tools)
- CUST-002 Marcus (non-shock): `hardship → simulate` (2 tools)
- CUST-003 Elena (shock): `hardship → shock → simulate` (3 tools, preferred)

**Open note:** the mechanism fix may not fully close the 2500ms/3000ms gates. Per D-13.1-02 the SLO stays locked; any residual gap becomes a Phase 13.1 verification finding and a separate LD-4 discussion — not 13.1's scope.

---

## Area 4 — Offline test fixture approach

**Question:** How should the Phase 13.1 offline regression guards be wired?

**Options presented:**

| Option | Summary |
|---|---|
| A — Synthetic `AgentResult` (Plan 03/05 pattern) | The exact blind spot that let Gap 1 ship. NOT RECOMMENDED. |
| **B — Real Strands `Agent()` with mocked `BedrockModel` (Recommended)** | Exercises the prompt's decision loop; catches prompt drift at merge time. |
| C — Smoke-only (no offline guard) | Regression only surfaces post-deploy — exactly where Gap 1 surfaced. |

**User selected:** **B — Real Strands Agent() with mocked BedrockModel.**

**Decisions:**
- D-13.1-15 — regression guards use real Strands `Agent()` with scripted mock-model; if prompt drives extra tool calls, mock runs out of responses and test fails.
- D-13.1-16 — ALSO add smoke canary `test_agent01_non_shock_stays_2_tools` at live stack. Belt-and-braces.

**Highest-risk research item flagged:** exact Strands 1.37.0 mock-model API. Researcher must grep `.venv/lib/python3.13/site-packages/strands/` for idiomatic pattern (MockModel, testing module, or bespoke BedrockModel subclass).

---

## Additional decisions captured (planner discretion remainder)

- **D-13.1-17** — 2-stack lift confirmed (`CustomerTariffAgent` + `CustomerTariffApi`); `CustomerTariff` foundation + `CustomerTariffFrontend` untouched. Planner runs `cdk diff` during planning; unlikely to drop the API stack.
- **D-13.1-18** — Pre/post byte-equivalence covers CUST-001 + CUST-002 + CUST-003 (savings fields byte-equal; `reasoning_trace` shape expected to change; excluded from byte-equivalence, asserted separately).
- **D-13.1-19** — CUST-999 unknown-customer smoke + direct `curl` HTTP 404 MUST be re-run post-ceremony as Gap 2 close-gate.
- **D-13.1-20** — CLAUDE.md addendum: D-12 sentinel description, short-circuit note, empty-billing STOP contract.
- **D-13.1-21** — DEMO-RUNBOOK.md addendum: updated per-flow latency numbers, 2-tool non-shock flow reference for presenter.
- **D-13.1-22** — Phase 14 plan preamble citation requirement (becomes checklist item when `/gsd-plan-phase 14` runs).

---

## Scope Creep Redirects

None. All discussion stayed within the D-13.1-01 through D-13.1-11 boundary. Option C for Area 1 (new Pydantic union branch) was the only item that brushed against Phase 14 scope; it was rejected as an implementation choice, not deferred as a separate idea.

---

## Deferred Ideas (captured in CONTEXT.md `<deferred>`)

- SLO revision for AGENT-01a (if mechanism fix insufficient — separate LD-4 territory)
- `kind: "not_found"` discriminated union (Phase 14 may own)
- `@tool get_billing_history` raises on empty (Strands retry-loop research not funded)
- `saving_monthly == 0.0` sentinel (false-positive risk)
- 1-tool simulate-only non-shock path
- Adjusting `get_billing_history` Lambda to raise (foundation stack untouched)
- UI changes for `{errorMessage}` shape (existing UI path handles it)
- New `pytest -m prompt` marker (overkill)
- Phase 14 pre-LLM guard preview (out of scope)
- Revising Phase 13 D-20 canary to cover 3rd persona (trace-shape extension only)

---

## Downstream agent handoff

**Researcher (gsd-phase-researcher) must:**
1. Confirm exact Strands 1.37.0 mock-model API by grepping `.venv/lib/python3.13/site-packages/strands/`. Highest-risk item.
2. Confirm `cdk diff` output for `CustomerTariffApi` after the `api_lambda/handler.py:152` sentinel edit — expected to show a diff; planner branch if diff == 0.
3. Confirm `InMemoryProvider` behaviour on unknown-customer ID (should return `[]` billing already — Phase 12 contract).

**Planner (gsd-planner) must:**
1. Honour all D-13.1-01 through D-13.1-22 locked decisions.
2. Write plans in the suggested 5-plan / 2-wave shape from VERIFICATION.md §"Phase 13.1 Scope Proposal", OR justify consolidation / split with a rationale in PLAN.md.
3. Add Phase 14 planning checklist item for the preamble citation (D-13.1-22).
4. Re-run Phase 13 Plan 08 ceremony pattern for 2 stacks (scripted lift → deploy → verify → re-apply deny-Update:* → byte-equivalence gate → close-gate smokes → commit).

---

*Discussion closed: 2026-04-29*
*Next step: `/gsd-plan-phase 13.1`*
