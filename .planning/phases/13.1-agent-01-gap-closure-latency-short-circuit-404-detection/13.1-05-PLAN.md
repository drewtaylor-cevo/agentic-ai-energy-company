---
phase: 13.1-agent-01-gap-closure-latency-short-circuit-404-detection
plan: 05
type: execute
wave: 4
depends_on:
  - 13.1-04
files_modified:
  - CLAUDE.md
  - DEMO-RUNBOOK.md
autonomous: true
requirements:
  - D-12
  - AGENT-01a
tags:
  - docs
  - runbook
  - invariants
must_haves:
  truths:
    - "CLAUDE.md's D-12 bullet is rewritten in-place to reference the new plan_id == 'UNKNOWN' sentinel with empirical-Sonnet-4.6 provenance"
    - "CLAUDE.md contains a new invariant bullet for the non-shock 2-tool short-circuit (D-13.1-14)"
    - "CLAUDE.md contains a new invariant bullet for the empty-billing STOP contract (D-13.1-12)"
    - "DEMO-RUNBOOK.md contains the post-13.1 warm-p95 expectations with actual measured numbers from Plan 04's ceremony-log.md"
    - "DEMO-RUNBOOK.md contains the 2-tool non-shock flow reference for presenter rehearsal (ReasoningTrace collapsed row text)"
    - "No load-bearing invariants removed or weakened elsewhere in CLAUDE.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated D-12 description + 2 new invariant bullets"
      contains: "UNKNOWN sentinel"
    - path: "CLAUDE.md"
      provides: "Non-shock short-circuit invariant"
      contains: "2-tool path"
    - path: "CLAUDE.md"
      provides: "Empty-billing STOP invariant"
      contains: "EMPTY BILLING STOP"
    - path: "DEMO-RUNBOOK.md"
      provides: "Post-13.1 latency expectations + 2-tool non-shock trace visual reference"
      contains: "Phase 13.1"
  key_links:
    - from: "CLAUDE.md D-12 bullet"
      to: "api_lambda/handler.py:152 sentinel branch"
      via: "line-number reference + CEREMONY-LOG.md empirical evidence citation"
      pattern: "api_lambda/handler.py"
    - from: "DEMO-RUNBOOK.md latency section"
      to: "Plan 04 ceremony-log.md actual warm-p95"
      via: "copied measurements"
      pattern: "Phase 13.1"
---

<objective>
Complete Phase 13.1 by committing three documentation updates that preserve institutional memory:

1. **CLAUDE.md D-12 rewrite** (D-13.1-20 update 1): edit the existing "Customer-not-found detection" bullet in-place to describe the new dual-mechanism detection (primary heuristic + UNKNOWN sentinel with empirical-Sonnet-4.6 provenance).
2. **CLAUDE.md new invariant bullets** (D-13.1-20 updates 2 + 3): add two sibling bullets in the §"Critical invariants" section for the non-shock 2-tool short-circuit and the empty-billing STOP contract.
3. **DEMO-RUNBOOK.md addenda** (D-13.1-21): two updates — post-13.1 warm-p95 expectations with actual numbers from Plan 04's ceremony, and a 2-tool non-shock flow reference for the presenter (what the CUST-001/002 collapsed trace row looks like vs CUST-003).

Purpose: Institutional memory on this codebase is carried by CLAUDE.md invariants + DEMO-RUNBOOK operator notes. Without this plan, the next developer (or next-session Claude) will not know WHY the sentinel string is `"UNKNOWN"` specifically, WHY non-shock personas have a 2-tool trace, or WHAT the post-13.1 warm-p95 expectations are. All three documentation updates are load-bearing for future-phase planning.

The Phase 14 AGENT-02a plan preamble citation (D-13.1-10 / D-13.1-22) is NOT in this plan's scope — it becomes a checklist item when `/gsd-plan-phase 14` runs later.

Output: Surgical in-place edits to two top-level docs. Zero code changes. Zero test changes. Zero dependency changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-CONTEXT.md
@.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-RESEARCH.md
@.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-PATTERNS.md
@.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/ceremony-log.md
@CLAUDE.md
@DEMO-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 05.1: Rewrite CLAUDE.md D-12 bullet in-place + add two new invariant bullets</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (read §"Critical invariants — break these and the demo dies" in full; locate the existing D-12 bullet — currently reads "Customer-not-found detection is 'no `green` or `cheapest` keys in body'...")
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-PATTERNS.md §CLAUDE.md addendum (lines 620-640 — verbatim rewrite + two new bullets)
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-RESEARCH.md §Open Question 3 recommendation (lines 1056-1060 — rewrite-in-place rationale)
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-CONTEXT.md D-13.1-20 (lines 104-107 — three required updates)
    - .planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-08-CEREMONY-LOG.md §Post-freeze Live Sanity (lines 112-121 — empirical UNKNOWN evidence being cited)
  </read_first>
  <action>
Edit `CLAUDE.md` in-place. Three sub-edits, all within the §"Things to know before changing things" / §"Critical invariants" block.

**Sub-edit 1: Rewrite the D-12 bullet in-place.** Locate the existing bullet beginning `**Customer-not-found detection** is` (currently reads something like):

```markdown
- **Customer-not-found detection** is "no `green` or `cheapest` keys in body" (`api_lambda/handler.py:152`), because the agent's tool-failure fallback path returns `{"errorMessage": "..."}` without tracks.
```

Replace with (VERBATIM from 13.1-PATTERNS.md §CLAUDE.md addendum):

```markdown
- **Customer-not-found detection (D-12, extended by Phase 13.1 D-13.1-13).** Primary heuristic is "no `green` or `cheapest` keys in body" (`api_lambda/handler.py:152`) — the agent's tool-failure fallback path returns `{"errorMessage": "..."}` without tracks. Defence-in-depth sentinel immediately after: `plan_id == "UNKNOWN"` on EITHER track also returns 404 (`api_lambda/handler.py` sentinel branch, see Phase 13.1 Plan 03). The UNKNOWN sentinel string was empirically observed from Sonnet 4.6 live emission (`.planning/phases/13-bill-shock-multi-tool-flow-agent-01/13-08-CEREMONY-LOG.md §Post-freeze Live Sanity`); a future model bump requires sentinel re-verification per D-22 Strands/model pin. Both branches pytest-covered (`tests/test_backend_api_handler.py::test_unknown_customer_sentinel_returns_404` + asymmetric variant).
```

**Sub-edit 2: Add new invariant bullet — non-shock 2-tool short-circuit.** Insert AS A NEW BULLET immediately after the rewritten D-12 bullet, in the same §"Critical invariants" block:

```markdown
- **Non-shock short-circuit (Phase 13.1 D-13.1-14).** As of Phase 13.1, non-shock personas drop to the 2-tool path `get_hardship_flag → simulate_savings`. The preference-ordered 3-tool graph still applies when the persona is bill-shock (CUST-003 Elena): `get_hardship_flag → detect_bill_shock → simulate_savings`. `reasoning_trace` shape is persona-dependent: 2 entries for CUST-001/002 (non-shock), 3 entries for CUST-003 (shock). Enforced by the system-prompt SHORT-CIRCUIT RULE clause in `agent/agent.py::_BASE_SYSTEM_PROMPT` (Phase 13.1 Plan 01) and offline-locked by `tests/test_bill_shock_flow.py::TestShortCircuit` (mock-Bedrock regression guards). Live-asserted by `tests/test_narrative_eval_live.py::test_agent01_non_shock_stays_2_tools` (smoke canary, D-13.1-16).
```

**Sub-edit 3: Add new invariant bullet — empty-billing STOP contract.** Insert AS THE NEXT NEW BULLET immediately after the short-circuit bullet:

```markdown
- **Empty-billing STOP contract (Phase 13.1 D-13.1-12).** If `get_billing_history` returns `[]`, the system prompt STOPs the agent before `simulate_savings` and emits an `{errorMessage: "customer not found"}` body with NO `green`/`cheapest` keys — NO synthesised UNKNOWN tracks. This is the prompt-side half of D-12; the `api_lambda/handler.py` UNKNOWN-plan_id sentinel (above) is defence-in-depth. Enforced by `_BASE_SYSTEM_PROMPT`'s EMPTY BILLING STOP RULE clause; offline-locked by `tests/test_bill_shock_flow.py::TestEmptyBillingStop`.
```

**DO NOT remove or weaken any other bullets in §"Critical invariants"**, especially:
- SAV-03 LLM never does arithmetic
- REC-03 both tracks always returned
- D-04 never-500 contract
- D-11 reasoning_trace exemption
- D-15 narrative dual-gate
- D-22 Strands 1.37.0 pinned
- D-15 4-tool cap via FourToolCapHook (not max_iterations)
- Bi-mode imports in agent/agent.py
- Config(read_timeout=25, connect_timeout=5) on API Lambda
- runtimeSessionId generated INSIDE handler() (SC-3)
- `?prewarm=1` returns 204 on success AND failure
- `?narrative=off` kill switch
- `_narrative_source` marker is internal
- `**API Lambda boto3 client uses `Config(read_timeout=25, connect_timeout=5)`**`

These bullets are LOAD-BEARING and protected — only the D-12 bullet is REWRITTEN; the short-circuit + empty-billing bullets are ADDED alongside.

**Output ordering recommendation:** place the three new/rewritten D-12-family bullets CONTIGUOUSLY in this order: (1) rewritten D-12, (2) Non-shock short-circuit, (3) Empty-billing STOP. This groups the Gap 1 + Gap 2 closure story together for future readers.
  </action>
  <verify>
    <automated>
    bash -c 'set -e
    # 1. Rewritten D-12 bullet contains the three new fingerprints
    grep -c "D-13.1-13" CLAUDE.md   # traceability
    grep -c "UNKNOWN sentinel" CLAUDE.md
    grep -c "13-08-CEREMONY-LOG.md" CLAUDE.md   # empirical provenance
    # 2. New short-circuit bullet
    grep -c "Non-shock short-circuit" CLAUDE.md
    grep -c "D-13.1-14" CLAUDE.md
    grep -c "2-tool path" CLAUDE.md
    grep -c "TestShortCircuit" CLAUDE.md
    # 3. New empty-billing STOP bullet
    grep -c "Empty-billing STOP contract" CLAUDE.md
    grep -c "D-13.1-12" CLAUDE.md
    grep -c "EMPTY BILLING STOP RULE" CLAUDE.md
    grep -c "TestEmptyBillingStop" CLAUDE.md
    # 4. Load-bearing invariants still present (spot-check key fingerprints)
    grep -c "SAV-03" CLAUDE.md
    grep -c "REC-03" CLAUDE.md
    grep -c "D-04" CLAUDE.md
    grep -c "D-11" CLAUDE.md
    grep -c "D-15" CLAUDE.md
    grep -c "D-22" CLAUDE.md
    grep -c "FourToolCapHook" CLAUDE.md
    grep -c "runtimeSessionId" CLAUDE.md
    grep -c "read_timeout=25, connect_timeout=5" CLAUDE.md
    grep -c "narrative=off" CLAUDE.md
    grep -c "_narrative_source" CLAUDE.md
    # 5. OLD D-12 bullet phrasing ("without tracks") preserved since the new bullet includes the primary heuristic description
    grep -c "without tracks" CLAUDE.md'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-13.1-13" CLAUDE.md` returns >= 1
    - `grep -c "UNKNOWN sentinel" CLAUDE.md` returns >= 1
    - `grep -c "13-08-CEREMONY-LOG.md" CLAUDE.md` returns >= 1 (empirical provenance)
    - `grep -c "Non-shock short-circuit" CLAUDE.md` returns >= 1
    - `grep -c "D-13.1-14" CLAUDE.md` returns >= 1
    - `grep -c "2-tool path" CLAUDE.md` returns >= 1
    - `grep -c "TestShortCircuit" CLAUDE.md` returns >= 1
    - `grep -c "Empty-billing STOP contract" CLAUDE.md` returns >= 1
    - `grep -c "D-13.1-12" CLAUDE.md` returns >= 1
    - `grep -c "EMPTY BILLING STOP RULE" CLAUDE.md` returns >= 1
    - `grep -c "TestEmptyBillingStop" CLAUDE.md` returns >= 1
    - All load-bearing invariant fingerprints still present (SAV-03, REC-03, D-04, D-11, D-15, D-22, FourToolCapHook, runtimeSessionId, `read_timeout=25, connect_timeout=5`, `narrative=off`, `_narrative_source`)
    - `git diff --stat CLAUDE.md` shows a single file changed with delta of ~20-40 added lines
  </acceptance_criteria>
  <done>
    - D-12 bullet rewritten in-place.
    - Two new sibling bullets added contiguously.
    - No other invariants touched.
    - Fingerprint grep-gates all pass.
  </done>
</task>

<task type="auto">
  <name>Task 05.2: Append DEMO-RUNBOOK.md addenda — post-13.1 latency + 2-tool non-shock flow reference</name>
  <files>DEMO-RUNBOOK.md</files>
  <read_first>
    - DEMO-RUNBOOK.md (read §"2 T-24h" visual rehearsal section AND §freeze section — the two addendum target areas)
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-PATTERNS.md §DEMO-RUNBOOK.md addendum (lines 642-657 — verbatim templates for both updates)
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/ceremony-log.md (from Plan 04 — ACTUAL warm-p95 numbers per persona; extract from Gate 5 prewarm.py output; Plan 04 SUMMARY also surfaces these)
    - .planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-CONTEXT.md D-13.1-21 (lines 108-110 — two required updates)
  </read_first>
  <action>
Open `DEMO-RUNBOOK.md`. Two distinct addenda, each appended into the appropriate existing section.

**Step 1: Read the actual warm-p95 numbers from the Plan 04 ceremony log.** Extract from `.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/ceremony-log.md` §Gate 5 (scripts/prewarm.py output). The log will contain per-persona warm medians. If for any reason the numbers are missing (e.g. prewarm.py failed to run), use the smoke-test timing output from §Gate 4 (`pytest -m smoke` includes `test_agent01_latency_floor` which logs Elena warm time; CUST-001 smoke doesn't log timing explicitly but the new `test_agent01_non_shock_stays_2_tools` runs `requests.get` which can be re-run manually to capture). Record which source was used.

**Step 2: Update 1 — post-13.1 warm-p95 expectations.** Add a new bullet/section under §"2 T-24h" (visual rehearsal) OR under the freeze section (planner discretion — match the existing doc's structure). Suggested wording (fill in actual numbers; example values shown are PLACEHOLDERS):

```markdown
### Post-Phase-13.1 warm-p95 expectations (measured 2026-04-29)

Per-flow prewarm gate rotation (`BACKEND_API_URL=<api> python3 scripts/prewarm.py`); numbers measured at end of Phase 13.1 ceremony (see `.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/ceremony-log.md §Gate 5`):

| Persona | Tool count | Warm-p95 measured | Per-flow gate | Outcome |
|---------|-----------|-------------------|---------------|---------|
| CUST-001 Sarah (non-shock) | 2 tools (hardship + simulate) | <ACTUAL ms> | 3000ms (single-tool/non-shock) | <PASS or FINDING> |
| CUST-002 Marcus (non-shock) | 2 tools (hardship + simulate) | <ACTUAL ms> | 3000ms | <PASS or FINDING> |
| CUST-003 Elena (shock)       | 3 tools (hardship + shock + simulate) | <ACTUAL ms> | 2500ms (multi-tool AGENT-01a) | <PASS or FINDING> |

If Elena missed the 2500ms gate, that is a Phase-13.1 verification finding per D-13.1-02 (mechanism fix shipped; SLO contract remains 2500ms but may need a LD-4 revision in a subsequent phase).
```

Replace `<ACTUAL ms>` and `<PASS or FINDING>` with the values from the ceremony log. If the ceremony log explicitly recorded a verification finding, quote it.

**Step 3: Update 2 — 2-tool non-shock flow reference for the presenter.** Add to the same section (or alongside §T-24h visual rehearsal):

```markdown
### Phase 13.1 reasoning-trace visuals (what the presenter should expect)

- **CUST-001 (Sarah, non-shock) and CUST-002 (Marcus, non-shock):** ReasoningTrace renders a collapsed row reading "▶ 2 steps: get_hardship_flag → simulate_savings". No `detect_bill_shock` or `get_billing_history` entries.
- **CUST-003 (Elena, shock):** ReasoningTrace renders "▶ 3 steps: get_hardship_flag → detect_bill_shock → simulate_savings". This is the visible AGENT-01a short-circuit signal — non-shock personas have a shorter trace by design, and that difference is intentional (Phase 13.1 D-13.1-14). If the presenter sees a 3-step trace on CUST-001 or CUST-002, treat it as a Gap 1 regression and halt the demo.

The `?narrative=off` kill switch still collapses reasoning traces entirely (v2.0 shape) regardless of tool count per the D-10 single-flag contract.
```

**Do NOT modify any other section of DEMO-RUNBOOK.md** — especially not:
- The stack-policy lift ceremony section (the pattern Plan 04 executed — unchanged)
- The T-24h visual rehearsal preamble
- The freeze section steps
- The §8 break-glass / stack-policy ceremony instructions

These are all load-bearing and Phase 13.1 does not change their semantics.
  </action>
  <verify>
    <automated>
    bash -c 'set -e
    # 1. New latency section present
    grep -c "Post-Phase-13.1 warm-p95 expectations" DEMO-RUNBOOK.md
    grep -c "CUST-001 Sarah (non-shock)" DEMO-RUNBOOK.md
    grep -c "CUST-003 Elena (shock)" DEMO-RUNBOOK.md
    grep -c "per-flow gate" DEMO-RUNBOOK.md
    # 2. Placeholder replacement — no "<ACTUAL ms>" or "<PASS or FINDING>" leftover
    test "$(grep -c "<ACTUAL ms>" DEMO-RUNBOOK.md)" = "0"
    test "$(grep -c "<PASS or FINDING>" DEMO-RUNBOOK.md)" = "0"
    # 3. Reasoning-trace visual reference present
    grep -c "Phase 13.1 reasoning-trace visuals" DEMO-RUNBOOK.md
    grep -c "2 steps: get_hardship_flag" DEMO-RUNBOOK.md
    grep -c "3 steps: get_hardship_flag" DEMO-RUNBOOK.md
    # 4. narrative=off single-flag contract still referenced (not removed)
    grep -c "narrative=off" DEMO-RUNBOOK.md
    # 5. Syntax-level: markdown valid (tables rendered as | ... | ... |)
    grep -c "| Persona |" DEMO-RUNBOOK.md
    # 6. Ceremony log linkage for audit
    grep -c "ceremony-log.md" DEMO-RUNBOOK.md'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Post-Phase-13.1 warm-p95 expectations" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "CUST-001 Sarah (non-shock)" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "CUST-003 Elena (shock)" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "per-flow gate" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "<ACTUAL ms>" DEMO-RUNBOOK.md` returns EXACTLY 0 (placeholders replaced with measured numbers)
    - `grep -c "<PASS or FINDING>" DEMO-RUNBOOK.md` returns EXACTLY 0 (placeholders replaced)
    - `grep -c "Phase 13.1 reasoning-trace visuals" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "2 steps: get_hardship_flag" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "3 steps: get_hardship_flag" DEMO-RUNBOOK.md` returns >= 1
    - `grep -c "narrative=off" DEMO-RUNBOOK.md` returns >= 1 (kill-switch contract reference preserved)
    - `grep -c "ceremony-log.md" DEMO-RUNBOOK.md` returns >= 1 (audit linkage)
    - `git diff --stat DEMO-RUNBOOK.md` shows a single file changed with delta of ~30-50 added lines
  </acceptance_criteria>
  <done>
    - Both addenda landed with ACTUAL numbers from ceremony-log.md (no placeholder leftovers).
    - Reasoning-trace visual reference contains both 2-step (non-shock) and 3-step (shock) strings.
    - Existing DEMO-RUNBOOK sections untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md (invariant canon) → next developer session | load-bearing documentation; wrong or missing entries cause future regressions |
| DEMO-RUNBOOK.md → presenter rehearsal | operator-facing runbook; inaccurate latency expectations mislead T-24h rehearsal |
| Plan 04 ceremony-log.md → Plan 05 latency number copy | this plan trusts Plan 04's recorded numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-05-01 | Tampering / Accidental invariant deletion | CLAUDE.md load-bearing bullets | mitigate | Task 05.1 acceptance criteria include grep-gates for SAV-03, REC-03, D-04, D-11, D-15, D-22, FourToolCapHook, runtimeSessionId, `read_timeout=25, connect_timeout=5`, `narrative=off`, `_narrative_source` — all MUST remain present |
| T-13.1-05-02 | Tampering / Placeholder values shipped | `<ACTUAL ms>` / `<PASS or FINDING>` in DEMO-RUNBOOK | mitigate | Task 05.2 acceptance criteria assert `grep -c "<ACTUAL ms>"` == 0 AND `grep -c "<PASS or FINDING>"` == 0 — blocks plan completion if placeholders leaked |
| T-13.1-05-03 | Tampering / Sentinel-string provenance drift | CLAUDE.md D-12 rewrite | mitigate | Rewrite explicitly names `13-08-CEREMONY-LOG.md §Post-freeze Live Sanity` as empirical source + `D-22 Strands/model pin` as the gate for re-verification on model bumps |
| T-13.1-05-04 | Information Disclosure | CLAUDE.md / DEMO-RUNBOOK leak operational secrets | accept | LOW — the latency numbers, sentinel strings, and test-file names are all already in the repo in other places; no new secret material introduced |
| T-13.1-05-05 | Availability / Next Sonnet bump silently bypasses sentinel | CLAUDE.md D-12 rewrite language | mitigate | Explicit "a future model bump requires sentinel re-verification per D-22" clause in the rewritten bullet makes this a mandatory D-22 decimal-phase checklist item |
</threat_model>

<verification>
- CLAUDE.md D-12 bullet rewritten; two new sibling bullets added; load-bearing invariants intact (grep-fingerprinted).
- DEMO-RUNBOOK.md two addenda landed with ACTUAL measured numbers (no placeholder leftovers).
- Ceremony log is referenced for audit traceability.
- `git diff` shows exactly TWO files modified (CLAUDE.md + DEMO-RUNBOOK.md).
- Zero code changes; zero test changes; zero dependency changes.
</verification>

<success_criteria>
- CLAUDE.md carries D-12's rewritten description + two new invariant bullets (short-circuit + empty-billing STOP).
- Every load-bearing CLAUDE.md invariant fingerprint survives the edit.
- DEMO-RUNBOOK.md contains the post-13.1 latency table with real numbers from Plan 04 + the reasoning-trace visual reference for presenter rehearsal.
- No placeholder strings (`<ACTUAL ms>`, `<PASS or FINDING>`) shipped.
- Empirical sentinel provenance (Sonnet 4.6 live emission → `13-08-CEREMONY-LOG.md`) cited in CLAUDE.md.
- Future D-22 Strands/model-bump decimal phase has a clear re-verification hook.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-agent-01-gap-closure-latency-short-circuit-404-detection/13.1-05-SUMMARY.md` with:
- `git diff --stat CLAUDE.md DEMO-RUNBOOK.md` output
- Grep-gate counts for all acceptance criteria (paste actual counts)
- The ACTUAL warm-p95 numbers per persona as copied into DEMO-RUNBOOK.md (for easy verification at phase-close)
- Whether Elena gate missed (verification finding recorded in DEMO-RUNBOOK or ceremony log)
- Forward note to Phase 14 planning: when `/gsd-plan-phase 14` runs, the Phase 14 PLAN preamble MUST cite Phase 13.1 Gap 2 closure (D-13.1-10 + D-13.1-22) — this checklist item was explicitly deferred from Phase 13.1 scope
- Forward note to `/gsd-verify-work 13.1`: verification pass should confirm all five plans landed + ceremony log + baselines + docs; if Elena gate missed, verifier should record it as an open finding against AGENT-01a per D-13.1-02
</output>
