# Phase 06.1: Resolve Sonnet 4.6 Tool-Use Regression (DEMO-02) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 06.1-resolve-sonnet-4-6-tool-use-regression-demo-02
**Areas discussed:** Resolution Track, Canary Tests, System Prompt, Closeout Scope, Migration API Target, Hedge

---

## Resolution Track

| Option | Description | Selected |
|--------|-------------|----------|
| Strands structured_output migration | Migrate from deprecated `Agent.structured_output()` (Pitfall 3) to the `Agent(structured_output_model=...)` constructor pattern. Most durable fix — Claude 4.6 is the target model for ACTIVE Bedrock access; 3.7 reactivation is a temporary band-aid. | ✓ |
| Claude 3.7 reactivation + revert | Reactivate 3.7 Sonnet in Bedrock console, revert model_id, remove tightened prompt. Fastest (zero code change once access returns) but leaves the deprecated API in place and the Legacy-unused rule can re-trigger. | |
| Both tracks (belt-and-braces) | Do migration as primary fix AND reactivate 3.7 as rollback path. Highest safety, ~2x work and freeze surface. | |
| Strands migration but TRY 3.7 reactivation first as a 30-minute timebox | Reactivate 3.7 first; if access is restored and smoke passes, run the migration as a follow-up. Lowest risk to demo timing. | |

**User's choice:** Strands structured_output migration (Recommended)
**Notes:** Strategic-model direction wins. Claude Sonnet 4.6 is the ACTIVE inference profile and v2.0's target; going backward to Legacy-tagged 3.7 would silently re-trigger the same Bedrock access rule if 3.7 goes unused again before the demo.

---

## Canary Tests

| Option | Description | Selected |
|--------|-------------|----------|
| All three personas pinned | Add test_marcus_flagship_values ($16.90/$30.98) and test_elena_flagship_values ($14/$25.67) alongside test_sarah_flagship_values. 06-03-SUMMARY Threat Flag recommends this explicitly. | ✓ |
| Sarah only (status quo) | Keep just test_sarah_flagship_values. Minimal change; risks coincident-fabrication passing through. | |
| All three + a no-tool-call regression guard | Three personas pinned PLUS a test that asserts CloudWatch shows Lambda invocation (latency > 400ms or ToolUse block in response). | |

**User's choice:** All three personas pinned (Recommended)
**Notes:** The third option (no-tool-call guard via latency/ToolUse check) was deferred to Phase 9's eval harness. Three-persona coverage is sufficient for Phase 06.1's scope — the silent-fabrication pattern (same pair across personas) will fail two of three canaries.

---

## System Prompt

| Option | Description | Selected |
|--------|-------------|----------|
| Keep — belt-and-braces | Tightened TOOL-OUTPUT-AS-SOURCE-OF-TRUTH block + Rule 7 stay. Low cost, non-zero benefit as a backstop against rounding or approximation if tool-use fires. | ✓ |
| Revert — if migration fixes tool-use, the extra rules are prompt-bloat | Remove the tightened block to minimise prompt tokens and freeze surface. Keep only rules 1-6. | |
| Keep only if Strands migration is chosen; revert if 3.7 rollback | Track-dependent. | |

**User's choice:** Keep — belt-and-braces (Recommended)
**Notes:** Since 3.7 rollback is rejected, the track-dependent option collapses into "keep". No additional deploy cycle needed to remove prompt text.

---

## Closeout Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full closeout — populate 06-SAMPLES.md AND enter Plan 06-03 Task 2 human-verify | Phase 06.1 closes Phase 6 fully. Run scripts/capture_samples.py, commit 06-SAMPLES.md, enter Task 2 checkpoint with resume signals. | ✓ |
| Sample capture only; defer Task 2 checkpoint to a Phase 6 close-out task | Populate 06-SAMPLES.md; leave human-verify for a dedicated close-out. | |
| Tool-use fix only; defer samples + checkpoint to Phase 7 live-smoke | Minimum viable 6.1 — just fix the regression. Samples + checkpoint happen when Phase 7 runs live smoke. | |

**User's choice:** Full closeout — populate 06-SAMPLES.md AND enter Plan 06-03 Task 2 human-verify (Recommended)
**Notes:** Cleanest closure — Phase 7 starts on a fully-closed Phase 6. 06-SAMPLES.md goes to the Phase 6 directory (its canonical home), not the 06.1 directory. ROADMAP.md Phase 6 progress flips to ✓ Complete in the same commit as 06.1's close.

---

## Migration API Target

| Option | Description | Selected |
|--------|-------------|----------|
| Research-driven | Plan says "migrate away from deprecated `Agent.structured_output()`; target API chosen during research". Researcher reads current Strands docs for the canonical tool+structured-output pattern on Claude 4.6. | ✓ |
| Commit to Agent(structured_output_model=Model) constructor pattern now | Lock the pattern the 06-03 SUMMARY hypothesised. | |
| Two-pattern spike — try both constructor-level and per-invoke arg | Planner writes parallel spike tests; pick whichever works. | |

**User's choice:** Research-driven (Recommended)
**Notes:** Avoids pre-committing to an API that may have evolved. Researcher validates the pattern before the plan locks tasks. Also preserves Phase 6 contract — researcher MUST verify simulate_savings still fires, validator still runs, retry semantics still work, and _narrative_source marker still emitted.

---

## Hedge

| Option | Description | Selected |
|--------|-------------|----------|
| No hedge — migration-only | Commit to the migration. If it fails, raise as a blocker via checkpoint; don't pre-bake a fallback that dilutes focus. | ✓ |
| Include a documented rollback task | Add one explicit "if migration spike fails, request 3.7 reactivation" task as a named fallback. | |
| Note it in CONTEXT.md deferred ideas only | No task, no plan step — just a note so Phase 10 can pick it up if needed. | |

**User's choice:** No hedge — migration-only (Recommended)
**Notes:** Plan stays crisp. 3.7 is documented as deferred (Phase 10 rollback-drill scope), but Phase 06.1 does not touch it.

---

## Claude's Discretion

Four areas left to planner judgement (see CONTEXT.md `<decisions>` → Claude's Discretion section):
- Researcher approach for API discovery (docs-first vs local spike harness)
- Deploy cadence (one deploy vs staged)
- CloudWatch observability additions for tool-call confirmation
- How to test the migration locally before deploy (unit test vs live-smoke only)

## Deferred Ideas

Captured in CONTEXT.md `<deferred>`:
- Bedrock Legacy pre-freeze access check (Phase 10)
- CloudWatch alarm on retry_count > 0 (v3.0)
- SHAPE-TOKENS.md standalone reference (Phase 8)
- Strands version bump (stay on 1.37.0; raise as blocker if required)
- No-tool-call regression guard (Phase 9 eval harness)
- Revert to Claude 3.7 Sonnet (explicitly out of scope per D-02)
