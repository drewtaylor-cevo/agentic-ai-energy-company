---
phase: 08-ui-integration-feature-flag-version-indicator
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md
autonomous: false
requirements: [UI-03, UI-04, UI-06, UI-07, UI-08]

must_haves:
  truths:
    - "`npm test` full suite passes (Phase 4 tests + Phase 8 additions)"
    - "`npm run build` completes and the built bundle contains the current git SHA"
    - "`npm run build:mock` completes and the mock dist renders the 6 verbatim fallback strings (<10s emergency-swap path preserved — ROADMAP Success Criterion 5)"
    - "Both cards remain above the fold at 1280×800 at maximum generated narrative length (Success Criterion 2)"
    - "?narrative=off suppresses both narrative rows AND skeleton placeholders (Success Criterion 3 + D-10)"
    - "v2.0 · <sha> indicator matches the built SHA (Success Criterion 4)"
    - "Skeleton → content transition is shift-free (Success Criterion 1)"
  artifacts:
    - path: ".planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md"
      provides: "D-23 closeout evidence — 9 screenshots (3 personas × {on, off, loading}) at 1280×800 with narrative"
      contains: "CUST-001"
  key_links:
    - from: "ui/dist/assets/*.js"
      to: "git rev-parse --short HEAD output"
      via: "Vite define + VersionIndicator render — matches at built-bundle inspection"
      pattern: "__GIT_SHA__"
    - from: "ui/dist-mock/"
      to: "ui/src/lib/mock/recommendations.ts narrative strings"
      via: "build:mock embeds the extended TrackInfo shape; renders narrative + call_script from the mock fixture"
      pattern: "Strong cool-season"
---

<objective>
Run the D-23 closeout gate: prove that every Phase 8 success criterion holds in a real browser at 1280×800 against the LIVE API and against `build:mock`, and capture the evidence into `08-SAMPLES.md`. This plan does not ship code — it verifies the code shipped in Plans 01-03 and produces the evidence artefact Phase 10's DEMO-06 rollback drill will reference.

Purpose: Phase 8 cannot close without human visual UAT at 1280×800 (jsdom cannot measure real layout; Playwright was explicitly rejected — D-21/D-22). This is the single point where all 5 ROADMAP success criteria are confirmed TRUE together, in the environment that matters. It is also where the `?narrative=off` rollback contract is proven before Phase 10 depends on it.

Output: `08-SAMPLES.md` with 9 1280×800 screenshots (3 personas × {flag-on, flag-off, loading}) plus a short narrative per persona confirming each success criterion. Success Criterion 5 (`build:mock` regenerates a working dist) is demonstrated by the mock-dist preview capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-integration-feature-flag-version-indicator/08-CONTEXT.md
@.planning/phases/08-ui-integration-feature-flag-version-indicator/08-01-SUMMARY.md
@.planning/phases/08-ui-integration-feature-flag-version-indicator/08-02-SUMMARY.md
@.planning/phases/08-ui-integration-feature-flag-version-indicator/08-03-SUMMARY.md
@.planning/phases/07-api-pass-through-pre-warm-route/07-01-SUMMARY.md
@.planning/phases/07-api-pass-through-pre-warm-route/07-02-SUMMARY.md

<interfaces>
<!-- Live API endpoint (from v1.0 + Phase 7 SUMMARYs) -->
<!-- Use: https://y9w9qwegwe.execute-api.us-east-1.amazonaws.com/ -->
<!-- Personas: CUST-001 (Sarah), CUST-002 (Marcus), CUST-003 (Elena) -->

<!-- Viewport for every screenshot: 1280 × 800 -->
<!-- Tool: Chrome DevTools Responsive Device Toolbar at 1280×800, or browser window resized to 1280×800 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three automated gates (npm test, npm run build, npm run build:mock)</name>
  <files>(none — this task produces command output, captured in the next task's SAMPLES.md)</files>
  <read_first>
    - ui/package.json (verify `build`, `build:mock`, `test` scripts)
    - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-CONTEXT.md D-23 steps 1-3
    - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-01-SUMMARY.md (confirms the extended TrackInfo + mock landed)
    - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-02-SUMMARY.md (confirms card + skeleton renders landed)
    - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-03-SUMMARY.md (confirms VersionIndicator landed)
  </read_first>
  <action>
    Record the current git SHA FIRST so the subsequent build-bundle inspections can compare against it:

    ```bash
    EXPECTED_SHA=$(git rev-parse --short HEAD)
    echo "Expected __GIT_SHA__: $EXPECTED_SHA"
    ```

    Step 1 — D-23 gate 1: full vitest suite.

    ```bash
    cd ui
    npx tsc -b --noEmit
    npx vitest run 2>&1 | tee /tmp/08-04-vitest.log
    ```

    Verify: zero failures. Capture the pass count (expected: all Phase 4 tests + Plan 01/02/03 test files).

    Step 2 — D-23 gate 2: production build.

    ```bash
    cd ui
    rm -rf dist
    npm run build 2>&1 | tee /tmp/08-04-build.log
    ```

    Verify:
    - Exit 0.
    - `dist/assets/*.js` contains the expected SHA string. Run:
      ```bash
      grep -l "$EXPECTED_SHA" ui/dist/assets/*.js
      ```
      Must return at least one file path.
    - The built HTML `dist/index.html` references the JS bundle correctly (`grep -q "assets/" ui/dist/index.html`).

    Step 3 — D-23 gate 3: mock build (<10s emergency-swap path).

    ```bash
    cd ui
    rm -rf dist-mock
    time npm run build:mock 2>&1 | tee /tmp/08-04-build-mock.log
    ```

    Verify:
    - Exit 0.
    - Wall-clock time < 10s (v1.0 D-07 contract — Success Criterion 5).
    - `dist-mock/assets/*.js` contains the SAME expected SHA (both `build` and `build:mock` share `vite.config.ts`):
      ```bash
      grep -l "$EXPECTED_SHA" ui/dist-mock/assets/*.js
      ```
    - `dist-mock/assets/*.js` contains at least one of the verbatim fallback strings:
      ```bash
      grep -l "Strong cool-season usage" ui/dist-mock/assets/*.js
      ```
      (Confirms mock fixture Plan 01 shipped made it into the mock dist.)

    If any of the three gates fails, STOP — do not proceed to Task 2 (human UAT). File the failure as a blocker; loop back through Plans 01-03 as needed.

    Record all outputs in `/tmp/08-04-*.log` so Task 2 can embed timings + SHA match into `08-SAMPLES.md`.
  </action>
  <verify>
    <automated>cd /Users/drewtaylor/Documents/Cevo/Customer-Tariff && EXPECTED_SHA=$(git rev-parse --short HEAD) && cd ui && npx tsc -b --noEmit && npx vitest run && rm -rf dist dist-mock && npm run build && grep -l "$EXPECTED_SHA" dist/assets/*.js && npm run build:mock && grep -l "$EXPECTED_SHA" dist-mock/assets/*.js && grep -l "Strong cool-season usage" dist-mock/assets/*.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` from `ui/` exits 0.
    - `npx vitest run` from `ui/` exits 0 with zero failing tests. Log at `/tmp/08-04-vitest.log` shows at least these test files passing: `personas.test.ts`, `validate.test.ts`, `useRecommendations.test.ts` (v1.0 baseline), `flags.test.ts`, `mock/recommendations.test.ts`, `RecommendationCard.test.tsx`, `RecommendationSkeletons.test.tsx`, `VersionIndicator.test.tsx` (Phase 8 additions).
    - `npm run build` exits 0. `ui/dist/` exists. `grep -l "$(git rev-parse --short HEAD)" ui/dist/assets/*.js` returns at least one path (the `__GIT_SHA__` define worked).
    - `npm run build:mock` exits 0 in under 10 seconds wall-clock (Success Criterion 5 / v1.0 D-07 contract). `ui/dist-mock/` exists.
    - `grep -l "$(git rev-parse --short HEAD)" ui/dist-mock/assets/*.js` returns at least one path (same SHA in both dists — confirms shared `vite.config.ts` define).
    - `grep -l "Strong cool-season usage" ui/dist-mock/assets/*.js` returns at least one path (mock fixture's verbatim fallback string made it into the mock dist — confirms the extended TrackInfo shape is live in mock build).
    - `grep -l "Warm-season heavy" ui/dist-mock/assets/*.js` returns at least one path (sanity probe for CUST-003's narrative string).
  </acceptance_criteria>
  <done>
    All three D-23 automated gates pass: vitest green, `build` completes with SHA-embedded bundle, `build:mock` completes in under 10s with extended TrackInfo shape serving verbatim fallback strings. Logs and timing captured for `08-SAMPLES.md`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT at 1280×800 — capture 9 screenshots and write 08-SAMPLES.md</name>
  <files>.planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-001-on.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-001-off.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-001-loading-on.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-001-loading-off.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-002-on.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-002-off.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-002-loading-on.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-003-on.png, .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-003-off.png</files>
  <read_first>
    - .planning/phases/08-ui-integration-feature-flag-version-indicator/08-CONTEXT.md D-23 (closeout gate steps 4-5)
    - .planning/ROADMAP.md §"Phase 8" (5 success criteria)
    - /tmp/08-04-vitest.log, /tmp/08-04-build.log, /tmp/08-04-build-mock.log (Task 1 outputs)
  </read_first>
  <action>
    This task is a human-verification checkpoint. Full operational procedure is documented in the checkpoint body below (`<what-built>`, `<how-to-verify>`, `<resume-signal>`). Executor runs the setup shell commands, then drives a real Chrome browser at 1280×800 against the live API to capture 9 screenshots and produce `08-SAMPLES.md`. DO NOT proceed without human sign-off on each success criterion.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md && ls .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES-CUST-00*.png | wc -l | grep -q '^ *9$' && grep -q 'Corner marker: `v2.0' .planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-SAMPLES.md` exists in the phase directory and contains a filled-out `D-23 Gate Results` table (all 3 gates marked PASS) plus `Success Criteria` section with all 5 criteria checked.
    - Exactly 9 PNG files matching glob `08-SAMPLES-CUST-00*.png` exist in the phase directory.
    - `08-SAMPLES.md` references `EXPECTED_SHA` and states the corner-marker SHA matches it for every success-state screenshot.
    - `08-SAMPLES.md` confirms ?narrative=off suppresses both narrative rows and skeleton placeholders (D-10 contract) for all 3 personas.
    - Operator has typed `approved` per the resume-signal block — tracked by the execute-phase runtime.
  </acceptance_criteria>
  <done>
    Nine screenshots captured at 1280×800, `08-SAMPLES.md` committed with every ROADMAP success criterion marked TRUE and evidence pointers, operator has approved the checkpoint.
  </done>
  <what-built>
    Plans 01-03 shipped the extended TrackInfo, flag module, version indicator, narrative/call_script rendering in both `RecommendationCard` and `RecommendationSkeletons`, and the `App.tsx` wiring. Task 1 of this plan confirmed the automated gates (vitest, `build`, `build:mock`). What remains is the ONLY verification jsdom cannot do: real layout at 1280×800, real skeleton → content transition, real corner-marker visibility, and real flag-off collapse to v1.0 shape.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Note the expected SHA: `EXPECTED_SHA=$(git rev-parse --short HEAD)`. Record it.
    2. Build the production bundle AGAINST THE LIVE API:
       ```bash
       cd ui
       rm -rf dist
       VITE_API_URL=https://y9w9qwegwe.execute-api.us-east-1.amazonaws.com/ npm run build
       npm run preview -- --host --port 4173
       ```
    3. Open Chrome → DevTools → Device Toolbar → Responsive → set width 1280, height 800, DPR 1.0. Close or undock DevTools for the capture pass (bottom-right marker must not be occluded by a docked-right DevTools panel).
    4. Visit `http://localhost:4173/`.

    **Capture pass A — flag ON, success state (3 screenshots):**

    For each persona, look up the customer ID (or click the persona chip), wait for the cards to render, then screenshot the full 1280×800 viewport.

    - `08-SAMPLES-CUST-001-on.png` — CUST-001 (Sarah)
    - `08-SAMPLES-CUST-002-on.png` — CUST-002 (Marcus)
    - `08-SAMPLES-CUST-003-on.png` — CUST-003 (Elena)

    **Verify for each screenshot:**
    1. Both Green and Cheapest cards visible above the fold (no vertical scroll needed to see the bottom of the Cheapest card's call_script block).
    2. Card content order top-to-bottom inside each card: plan name → `$X.XX /mo $Y.YY /yr` savings grid → italic muted narrative paragraph → "Based on your 12-month kWh usage…" methodology line → bordered quote block with `❝ … ❞` call script.
    3. Narrative paragraph is italic + muted (visually subordinate to the numbers).
    4. Call script block has a visible track-colored left border (green = emerald; cheapest = blue).
    5. Bottom-right corner of viewport: `v2.0 · <sha>` marker. The `<sha>` MUST match `$EXPECTED_SHA` recorded above.
    6. For each persona, the narrative + call_script strings MATCH what you expect from the live API (they may be model-generated copy differing from the committed fallbacks — that's normal for live; verify they are demo-ready and digit-free).

    **Capture pass B — flag OFF (3 screenshots):**

    Append `?narrative=off` to each persona URL (or paste the URL directly, e.g. `http://localhost:4173/?narrative=off`, then look up the customer). Capture the success state.

    - `08-SAMPLES-CUST-001-off.png`
    - `08-SAMPLES-CUST-002-off.png`
    - `08-SAMPLES-CUST-003-off.png`

    **Verify for each flag-off screenshot:**
    1. NO narrative paragraph visible anywhere on either card.
    2. NO bordered call-script quote block visible anywhere on either card.
    3. Card height visibly shorter — matches the v1.0 shape (roughly plan name + savings grid + methodology line only).
    4. `v2.0 · <sha>` corner marker STILL visible (the flag only suppresses narrative, not the version indicator).
    5. Both cards still above the fold (trivially — they are shorter).

    **Capture pass C — loading state, flag ON vs flag OFF (3 screenshots — one per persona, merged into a single side-by-side image OR 3 loading-only screenshots is acceptable):**

    Trick the app into a persistent loading state (easiest: throttle the network to "Slow 3G" in DevTools, then look up a persona; capture the skeleton before the response resolves). If that is too flaky, set a very-long artificial delay via DevTools' "Network → Throttling → Custom → add 10s delay" profile.

    - `08-SAMPLES-CUST-001-loading-on.png` — skeleton with narrative + call_script placeholder rows visible (2-line narrative skeleton; 3-line muted-border-left call_script skeleton shell)
    - `08-SAMPLES-CUST-001-loading-off.png` — `?narrative=off` loading state; skeleton DOES NOT contain narrative-row or call_script-shell placeholders; matches v1.0 loading shape
    - `08-SAMPLES-CUST-002-loading-on.png` (or any one additional persona to confirm parallelism) — skeleton narrative placeholders visible

    **Verify for each loading screenshot:**
    1. Flag-ON loading: narrative (.space-y-2) + call_script (.border-l-muted) placeholders visible.
    2. Flag-OFF loading: NEITHER placeholder visible. This is the D-10 non-negotiable — the skeleton must also collapse to v1.0 shape.
    3. Skeleton → content transition is visually shift-free (i.e., when you watch cards go from skeleton to real content, the Cheapest card's bottom edge does not jump; optional — describe subjectively in the narrative field below).

    **Produce `08-SAMPLES.md`:**

    Commit a new file at `.planning/phases/08-ui-integration-feature-flag-version-indicator/08-SAMPLES.md` with this structure (fill in bracketed values):

    ```markdown
    # Phase 8 Closeout Evidence — 08-SAMPLES.md

    **Captured:** [YYYY-MM-DD]
    **Viewport:** 1280 × 800, DPR 1.0, Chrome [version]
    **Expected __GIT_SHA__:** `[EXPECTED_SHA]`
    **Live API:** https://y9w9qwegwe.execute-api.us-east-1.amazonaws.com/

    ## D-23 Gate Results

    | Gate | Command | Result | Evidence |
    |------|---------|--------|----------|
    | 1 | `npm test` | PASS ([N] tests) | /tmp/08-04-vitest.log |
    | 2 | `npm run build` | PASS | `$EXPECTED_SHA` embedded in dist/assets/[hash].js |
    | 3 | `npm run build:mock` | PASS in [T]s (<10s) | `$EXPECTED_SHA` embedded in dist-mock/assets/[hash].js; mock narrative strings confirmed |

    ## Success Criteria (ROADMAP Phase 8)

    - [x] 1. Narrative + call_script rows render on every persona lookup with no layout shift — verified visually in flag-ON success screenshots below.
    - [x] 2. Both cards above the fold at 1280×800 at maximum length — verified in all 3 flag-ON success screenshots.
    - [x] 3. `?narrative=off` hides both rows AND skeleton placeholders — verified in all 3 flag-OFF success screenshots + flag-OFF loading screenshot.
    - [x] 4. `v2.0 · <git-sha>` corner marker matches built SHA — verified corner read matches `$EXPECTED_SHA` in all 6 success screenshots.
    - [x] 5. `build:mock` regenerates a working dist with extended TrackInfo — verified in D-23 gate 3; dist-mock renders narrative + call_script from the 6 verbatim fallback strings.

    ## Persona Captures

    ### CUST-001 (Sarah Chen)

    **Flag ON (success):** `08-SAMPLES-CUST-001-on.png`
    Narrative rendered: "[paste actual narrative text from screenshot]"
    Call script rendered: "[paste actual call_script text]"
    Corner marker: `v2.0 · [SHA]` — matches expected: YES

    **Flag OFF (success):** `08-SAMPLES-CUST-001-off.png`
    Narrative visible: NO. Call script visible: NO. Card matches v1.0 shape.
    Corner marker still visible: YES.

    **Loading — flag ON:** `08-SAMPLES-CUST-001-loading-on.png`
    Narrative placeholder visible: YES. Call_script muted-border shell visible: YES.

    **Loading — flag OFF:** `08-SAMPLES-CUST-001-loading-off.png`
    Narrative placeholder visible: NO. Call_script muted-border shell visible: NO. Matches v1.0 loading shape.

    ### CUST-002 (Marcus Webb)

    [... same 4 sub-sections ...]

    ### CUST-003 (Elena Vasquez)

    [... same 4 sub-sections ...]

    ## Layout-Shift Observation

    [Single paragraph — did the skeleton → content transition look shift-free? Did the Cheapest card's bottom edge move? Record subjectively. This is the only subjective UAT field; all other criteria are binary.]

    ## Rollback Contract Holds

    The `?narrative=off` flag collapses the UI to v1.0 shape in BOTH loading and success states (CUST-001/002/003 verified). The flag is the primary runtime rollback lever and is ready for Phase 10's DEMO-06 drill.
    ```

    Save screenshots alongside the MD file in the same directory. Commit all 10 artefacts (9 PNGs + 1 MD) in a single commit.

    **If any success criterion fails:** STOP. Do not claim the phase closed. File the specific failure (e.g., "CUST-002 Cheapest card's call_script block pushes below the fold at 1280×800") and loop back to the relevant plan for a fix pass.
  </how-to-verify>
  <resume-signal>
    Type `approved` after:
    - All 9 screenshots captured and saved into the phase directory.
    - `08-SAMPLES.md` written with all 5 success criteria checked off (all [x]).
    - Corner-marker SHA visually matches `$EXPECTED_SHA` in every flag-ON and flag-OFF screenshot.
    - `?narrative=off` screenshots (both success AND loading) show ZERO narrative/call_script elements.
    - All cards above the fold in all 6 success screenshots.

    If ANY of the above fails, type `blocked: <specific failure>` and do NOT mark the phase complete. The planner will loop back to the failing plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
Phase 8 introduces NO new runtime attack surface. See 08-01-PLAN.md `<threat_model>` for the phase-wide register. This plan is pure verification — it runs existing build commands and captures screenshots of an already-deployed stack. No new attack surface is created by executing the closeout gate.
</threat_model>

<verification>
Automated portion (Task 1) can be verified without human input:

```bash
cd ui
npx tsc -b --noEmit
npx vitest run
rm -rf dist dist-mock
npm run build && grep -l "$(git rev-parse --short HEAD)" dist/assets/*.js
time npm run build:mock
grep -l "$(git rev-parse --short HEAD)" dist-mock/assets/*.js
grep -l "Strong cool-season usage" dist-mock/assets/*.js
```

Human UAT portion (Task 2) is verified by the presence of 9 PNGs + a completed `08-SAMPLES.md` in the phase directory with every success criterion ticked.
</verification>

<success_criteria>
- `08-SAMPLES.md` exists in the phase directory.
- All 9 screenshots exist in the phase directory (3 personas × {on, off, loading-on/off}).
- `08-SAMPLES.md` has all 5 ROADMAP success criteria checked off, each with a single-sentence pointer to the evidence image or log.
- Corner-marker SHA matches the built-bundle SHA in every success-state screenshot.
- `?narrative=off` screenshots show zero narrative/call_script DOM (verified visually — no italic muted paragraph, no bordered quote block, no skeleton placeholders for those rows).
- `build:mock` wall-clock time under 10s recorded in the MD gate table.
- No regression in `npm test` or v1.0 behaviour.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-integration-feature-flag-version-indicator/08-04-SUMMARY.md` capturing:
- D-23 gate table (all 3 pass).
- Confirmation that all 5 ROADMAP success criteria are TRUE.
- Pointer to the 9 screenshots + `08-SAMPLES.md`.
- The `$EXPECTED_SHA` used as the corner-marker comparison target.
- `build:mock` wall-clock time (for Phase 10 DEMO-06 drill reference).
- Any operator notes for Phase 10 (e.g., "DevTools docked right at 1280px DOES occlude the bottom-right marker — leave DevTools undocked during demo").
- Phase 8 complete: all 5 requirements (UI-03, UI-04, UI-06, UI-07, UI-08) satisfied.
</output>
</content>
