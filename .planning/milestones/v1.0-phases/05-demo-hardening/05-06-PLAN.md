---
phase: 05-demo-hardening
plan: 06
type: execute
wave: 5
depends_on:
  - "05-02"
  - "05-03"
  - "05-05"
files_modified:
  - ".planning/phases/05-demo-hardening/DEMO-RUNBOOK.md"
autonomous: false
requirements:
  - DEMO-01
tags:
  - demo-hardening
  - runbook
  - presenter-cheat-sheet

must_haves:
  truths:
    - "Runbook has pre-demo setup section with AWS account/model-access confirmation + git-checkout + npm-ci + cdk-deploy references"
    - "Runbook has T-24h, T-2h, T-0 timed checklist per D-19"
    - "Runbook has presenter cheat sheet with 3 persona IDs, expected $ values, and equal-cards talking point"
    - "Runbook has launch commands: npm run preview (primary) and npm run preview:mock (emergency swap)"
    - "Runbook has fallback procedure explaining what the presenter says while swapping"
    - "Runbook has post-demo teardown section with cdk destroy commands (D-18 — documented, not executed)"
    - "All persona dollar values in the cheat sheet match tests/conftest.py and the live rehearsal results"
  artifacts:
    - path: ".planning/phases/05-demo-hardening/DEMO-RUNBOOK.md"
      provides: "Presenter-facing demo execution guide covering T-24h through post-demo teardown"
      contains: "cdk destroy"
      min_lines: 150
  key_links:
    - from: "DEMO-RUNBOOK.md Launch commands"
      to: "ui/package.json scripts (preview, preview:mock from Plan 03)"
      via: "exact command strings referenced in runbook"
      pattern: "npm run preview"
    - from: "DEMO-RUNBOOK.md Presenter cheat sheet"
      to: "tests/conftest.py persona fixtures + 05-VERIFICATION.md rehearsal table"
      via: "dollar values copied verbatim"
      pattern: "\\$30.00/mo"
    - from: "DEMO-RUNBOOK.md Pre-demo setup"
      to: ".planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md"
      via: "reference for captured ARNs and endpoints"
      pattern: "05-DEPLOY-OUTPUTS.md"
---

<objective>
Write `.planning/phases/05-demo-hardening/DEMO-RUNBOOK.md` — the single-document presenter guide for demo day. It collects the command sequences, checklist timing, cheat-sheet values, and fallback procedure so the presenter runs the demo from this one file. The runbook is the downstream consumer of everything produced in Plans 02, 03, 04, and 05.

Purpose: D-17 and D-19 specify this as a required Phase 5 deliverable. The presenter (user) should be able to rehearse end-to-end by following the runbook top-to-bottom — no prior-phase archaeology required on demo day.

Output: A single committed Markdown file with 6 required sections (pre-demo setup, timed checklist, cheat sheet, launch commands, fallback procedure, post-demo teardown). Persona values cross-checked against Plan 05's rehearsal evidence AND `tests/conftest.py` (the authoritative source of persona fixtures for Phases 1-3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-demo-hardening/05-CONTEXT.md
@.planning/phases/05-demo-hardening/05-PATTERNS.md
@.planning/phases/05-demo-hardening/05-02-SUMMARY.md
@.planning/phases/05-demo-hardening/05-03-SUMMARY.md
@.planning/phases/05-demo-hardening/05-05-SUMMARY.md
@.planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md
@.planning/phases/05-demo-hardening/05-VERIFICATION.md
@tests/conftest.py
@ui/package.json

<interfaces>
<!-- Authoritative persona values (source: tests/conftest.py fixtures mock_savings_response / mock_marcus_response / mock_elena_response) -->
<!-- These values are the ground truth: if the rehearsal surfaces anything different, that's a bug in the seed or savings math, not a runbook update. -->

- CUST-001 Sarah Chen (high usage):   Green $30.00/mo · $360.00/yr · EcoFlex 100  |  Cheapest $55.00/mo · $660.00/yr · Value 12
- CUST-002 Marcus Webb  (mid usage):   Green $16.90/mo · $202.80/yr · EcoFlex 100  |  Cheapest $30.98/mo · $371.76/yr · Value 12
- CUST-003 Elena Vasquez (low usage):  Green $14.00/mo · $168.00/yr · EcoFlex 100  |  Cheapest $25.67/mo · $308.04/yr · Value 12

Cross-check (verified by planner against tests/conftest.py, confirmed 2026-04-24):
- `mock_savings_response` (CUST-001): saving_monthly=30.00, saving_annual=360.00 (green); 55.00, 660.00 (cheapest) ✓
- `mock_marcus_response` (CUST-002): saving_monthly=16.90, saving_annual=202.80 (green); 30.98, 371.76 (cheapest) ✓
- `mock_elena_response` (CUST-003): saving_monthly=14.00, saving_annual=168.00 (green); 25.67, 308.04 (cheapest) ✓

<!-- Launch commands — come from ui/package.json (Plan 03) -->
Primary:  `cd ui && npm run preview`          (serves ui/dist/ — baked with live ApiEndpoint)
Mock:     `cd ui && npm run preview:mock`     (serves ui/dist-mock/ — no live API)

<!-- Teardown commands (D-18: documented, not executed in this phase) -->
The deploy order was FoundationStack → AgentCoreStack → BackendApiStack.
Teardown is the reverse order: Api → Agent → Foundation.
Commands:
  AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariffApi
  AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariffAgent
  AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariff
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft DEMO-RUNBOOK.md with all 6 required sections</name>
  <read_first>
    - .planning/phases/05-demo-hardening/05-CONTEXT.md (D-17 required sections, D-19 T-24h/T-2h/T-0 shape)
    - .planning/phases/05-demo-hardening/05-PATTERNS.md (runbook section map + cheat sheet persona values table at lines 209-214)
    - .planning/phases/05-demo-hardening/05-05-SUMMARY.md (authoritative rehearsal dollar values to cross-check)
    - .planning/phases/05-demo-hardening/05-VERIFICATION.md (rehearsal table rows 1-3 for persona values)
    - .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md (for the endpoint reference in pre-demo)
    - tests/conftest.py (source of truth for persona expected values — fixtures mock_savings_response, mock_marcus_response, mock_elena_response at lines 46-100)
    - ui/package.json (preview / preview:mock script strings)
  </read_first>
  <action>
    Create `.planning/phases/05-demo-hardening/DEMO-RUNBOOK.md` with the exact structure below. Pull dollar values from `05-VERIFICATION.md` Pass 1 / Pass 2 rows — these are the actually-observed live values, not derived. Cross-check all 3 personas against `tests/conftest.py` before writing. If the rehearsal surfaced a value different from <interfaces> (e.g., savings rounded differently), STOP — that's a data regression to investigate, not an issue to paper over in the runbook.

    Exact file shape (this is the entire file contents — byte-for-byte):

    ```markdown
    ---
    phase: 05-demo-hardening
    artifact: demo-runbook
    status: ready
    audience: presenter (user)
    created: <ISO-8601 UTC>
    depends_on:
      - 05-DEPLOY-OUTPUTS.md (live endpoint + ARNs)
      - 05-VERIFICATION.md (rehearsal evidence + latency baselines)
    ---

    # Demo Runbook — Customer Tariff & Billing Optimisation Agent

    Presenter-facing guide for the v1.0 demo. Read top-to-bottom the day before. On demo day, follow the T-24h / T-2h / T-0 checklist.

    **Environment:** live AWS deployment in us-east-1 (see `05-DEPLOY-OUTPUTS.md` for ARNs + endpoint).
    **Primary launch surface:** `vite preview` on presenter laptop, pointing at `ui/dist/`.
    **Emergency fallback:** `vite preview` on presenter laptop, pointing at `ui/dist-mock/` (no AWS dependency).

    ---

    ## 1. Pre-demo setup (do this once, before T-24h)

    1. Confirm AWS account and Bedrock model access:
       ```bash
       aws sts get-caller-identity --region us-east-1
       # Account should match the one recorded in .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md
       ```
       In the AWS console: Bedrock -> Model access -> us-east-1 -> Claude models show "Access granted".

    2. Check out the tagged demo commit:
       ```bash
       git fetch --tags
       git checkout demo-v1.0
       ```

    3. Install / refresh local dependencies:
       ```bash
       cd ui && npm ci && cd ..
       python3 -m venv .venv && source .venv/bin/activate
       pip install -r requirements.txt -r requirements-dev.txt
       ```

    4. Confirm the deployed stacks are still CREATE_COMPLETE / UPDATE_COMPLETE:
       ```bash
       aws cloudformation describe-stacks --region us-east-1 \
         --query 'Stacks[?starts_with(StackName, `CustomerTariff`)].[StackName,StackStatus]' \
         --output table
       ```
       If any stack is in a failed state: re-deploy following the order in step 5 below.

    5. (Only if re-deploying) Deploy stacks in dependency order:
       ```bash
       AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest deploy CustomerTariff      --require-approval never
       AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest deploy CustomerTariffAgent --require-approval never
       AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest deploy CustomerTariffApi   --require-approval never
       ```
       Then re-capture CfnOutputs into `05-DEPLOY-OUTPUTS.md` and update the tag to `demo-v1.0.1`.

    6. Build both dists on the presenter laptop:
       ```bash
       # Extract the live API URL from the captured-outputs file
       export LIVE_API_URL=$(grep -oE 'https://[a-z0-9]+\.execute-api\.us-east-1\.amazonaws\.com[^ `|]*' \
         .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md | head -1)

       # Primary bundle (baked against live endpoint)
       cd ui && rm -rf dist && VITE_API_URL="$LIVE_API_URL" npm run build

       # Fallback bundle (mock mode — no live API)
       rm -rf dist-mock && npm run build:mock
       cd ..
       ```

    ---

    ## 2. Timed checklist (D-19)

    ### T-24h

    - [ ] `git tag --list demo-v1.0` shows the tag exists
    - [ ] `05-DEPLOY-OUTPUTS.md` reflects the currently-deployed ARNs (re-run `aws cloudformation describe-stacks` and diff)
    - [ ] `05-VERIFICATION.md` latency table shows warm median <3s for all 3 personas; if stale, re-run the rehearsal
    - [ ] Runbook scanned end-to-end; any customer-specific branding or slides updated

    ### T-2h

    - [ ] `ui/dist/index.html` exists and a grep confirms it contains the live hostname:
      ```bash
      grep -l 'execute-api.us-east-1.amazonaws.com' ui/dist/assets/*.js | head -1
      ```
    - [ ] `ui/dist-mock/index.html` exists and does NOT contain the live hostname:
      ```bash
      ! grep -q 'execute-api.us-east-1.amazonaws.com' ui/dist-mock/assets/*.js && echo "mock isolated"
      ```
    - [ ] Emergency-swap test: in a second terminal,
      ```bash
      cd ui && npm run preview:mock -- --port 4174
      # open http://127.0.0.1:4174/ in a private browser window and confirm CUST-001 returns Sarah's cards
      # then Ctrl+C to stop
      ```
    - [ ] Laptop browser has a tab pre-opened at `http://127.0.0.1:4173/` (closed for now; will open at T-0)
    - [ ] AWS console tab pre-opened to the `CustomerTariffApi` stack (useful if you need to show infrastructure)
    - [ ] Phone stopwatch accessible (in case a reviewer asks for live latency evidence)

    ### T-0 (2 minutes before going live)

    1. Start the primary preview:
       ```bash
       cd ui && npm run preview
       # wait for "Local:   http://127.0.0.1:4173/"
       ```

    2. Warm the stack (ad-hoc warm-up per D-04):
       ```bash
       export BACKEND_API_URL=$(grep -oE 'https://[a-z0-9]+\.execute-api\.us-east-1\.amazonaws\.com[^ `|]*' \
         .planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md | head -1)
       curl -s -o /dev/null "$BACKEND_API_URL/recommendations/CUST-001"
       # Confirms the Lambda + AgentCore runtime are warm for the first demo call
       ```

    3. Open `http://127.0.0.1:4173/` in the demo browser tab. Resize window to 1280×800 if not already. Confirm the idle state renders ("No customer selected").

    4. You are live.

    ---

    ## 3. Presenter cheat sheet

    **The three demo personas** (use in this order):

    | ID | Persona | Expected Green | Expected Cheapest | One-line narrative |
    |----|---------|----------------|-------------------|---------------------|
    | CUST-001 | Sarah Chen — high usage | $30.00/mo · $360.00/yr · EcoFlex 100 | $55.00/mo · $660.00/yr · Value 12 | "Flagship retention save — biggest delta, clearest story." |
    | CUST-002 | Marcus Webb — mid usage | $16.90/mo · $202.80/yr · EcoFlex 100 | $30.98/mo · $371.76/yr · Value 12 | "Typical customer — moderate delta, both tracks are viable options." |
    | CUST-003 | Elena Vasquez — low usage | $14.00/mo · $168.00/yr · EcoFlex 100 | $25.67/mo · $308.04/yr · Value 12 | "Low-usage customer — savings still meaningful, not a rounding error." |

    **Equal-cards talking point (say this once, early — REC-03):**

    > "We deliberately present Green and Cheapest side by side, with no ranking between them. The call centre agent picks based on what the customer cares about — environmental preference or lowest bill. The system never decides for the customer."

    **Error paths to rehearse (if a reviewer asks to see them):**
    - `cust999` (no dash, lowercase) → 400 alert with copy "That doesn't look like a customer ID. Format is CUST followed by 3–6 digits."
    - `CUST-999` → 404 alert with copy "No customer found for CUST-999. Check the ID and try again."

    ---

    ## 4. Launch commands (D-06)

    Per D-06, the launch surface is `vite preview` on the presenter laptop — continuing Phase 4 D-05. No cloud hosting, one command, localhost only.

    **Primary (live API):**
    ```bash
    cd ui && npm run preview
    # Open http://127.0.0.1:4173/
    ```

    **Emergency fallback (mock mode — if live API fails mid-demo):**
    ```bash
    # In the terminal running the primary preview: Ctrl+C
    cd ui && npm run preview:mock
    # Refresh http://127.0.0.1:4173/ (note: preview:mock uses the same port 4173 by default)
    ```

    The mock dist serves the same 3 personas with the same dollar values — the demo story is unchanged. The only difference is that the request is satisfied from local fixture data rather than a live Lambda + AgentCore call.

    ---

    ## 5. Fallback procedure

    **Symptom:** a persona lookup spins for more than ~10 seconds, or an error alert appears unexpectedly.

    **What to say** (keep talking, keep eye contact):

    > "We're running on a live AWS deployment today, which occasionally has a cold-start moment. Let me swap to our pre-built local mode so we can keep moving — the data and recommendations are identical; this is just a network-path substitution."

    **What to do** (should take <10 seconds):

    1. Ctrl+C the primary preview terminal.
    2. `npm run preview:mock` (same `ui/` directory).
    3. Browser tab: reload.
    4. Re-enter the persona ID that failed.

    **After the demo:** investigate root cause. Do NOT attempt live debugging during the presentation.

    ---

    ## 6. Post-demo teardown (D-18 — documented, not executed)

    Tear down the stacks after the demo when you no longer need the live environment. Order is the reverse of deploy (dependencies come last):

    ```bash
    AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariffApi    --force
    AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariffAgent  --force
    AWS_DEFAULT_REGION=us-east-1 npx aws-cdk@latest destroy CustomerTariff       --force
    ```

    After teardown:
    - DynamoDB `BillingTable` is deleted (seed data is versioned in `infrastructure/seed_data/` — redeploy re-seeds)
    - AgentCore runtime is deleted — re-creation triggers a cold-start on the next deploy
    - API Gateway endpoint URL changes on redeploy — update `05-DEPLOY-OUTPUTS.md` accordingly and, if the live URL changes materially, cut a new tag (`demo-v1.0.1`)

    ---

    ## Cross-references

    - Live ARNs and endpoint: `.planning/phases/05-demo-hardening/05-DEPLOY-OUTPUTS.md`
    - Rehearsal + latency evidence: `.planning/phases/05-demo-hardening/05-VERIFICATION.md`
    - Phase 4 UI contract (error copy, layout): `.planning/phases/04-agent-assist-ui/04-UI-SPEC.md`
    - Requirements: `.planning/REQUIREMENTS.md` (DEMO-01, DEMO-02, UI-02 are what the demo shows)
    ```

    Fidelity rules when writing:
    1. Persona dollar values MUST come from Plan 05's rehearsal table. If the rehearsal table disagrees with <interfaces> or `tests/conftest.py`, STOP — that's a data regression to investigate, not an issue to paper over in the runbook.
    2. Launch command strings MUST match `ui/package.json` exactly — if Plan 03 renamed a script, sync here.
    3. Teardown commands are REVERSE of deploy order (API → Agent → Foundation).
    4. No promises the demo doesn't deliver. If Plan 05's latency showed CUST-003 cold at 6s, don't claim "<3s cold" in the runbook.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 1. Pre-demo setup' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 2. Timed checklist (D-19)' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^### T-24h' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^### T-2h' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^### T-0' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 3. Presenter cheat sheet' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 4. Launch commands' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 5. Fallback procedure' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 6. Post-demo teardown' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-001' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-002' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-003' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$30.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$55.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$16.90/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$30.98/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$14.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$25.67/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '16.90' tests/conftest.py &amp;&amp; grep -q '30.98' tests/conftest.py &amp;&amp; grep -q '14.00' tests/conftest.py &amp;&amp; grep -q '25.67' tests/conftest.py &amp;&amp; grep -q 'cdk.*destroy CustomerTariffApi' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'cdk.*destroy CustomerTariffAgent' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'cdk.*destroy CustomerTariff[^A]' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'npm run preview:mock' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; echo "DEMO-RUNBOOK.md structure verified across all 3 personas AND conftest.py"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/05-demo-hardening/DEMO-RUNBOOK.md`
    - All 6 top-level sections present: `## 1. Pre-demo setup`, `## 2. Timed checklist (D-19)`, `## 3. Presenter cheat sheet`, `## 4. Launch commands`, `## 5. Fallback procedure`, `## 6. Post-demo teardown`
    - T-24h, T-2h, T-0 sub-sections all exist under §2
    - All 3 personas mentioned: `CUST-001`, `CUST-002`, `CUST-003` (grep matches for each)
    - CUST-001 dollar values present: `$30.00/mo` and `$55.00/mo` (verbatim, from conftest.py mock_savings_response)
    - CUST-002 dollar values present: `$16.90/mo` and `$30.98/mo` (verbatim, from conftest.py mock_marcus_response)
    - CUST-003 dollar values present: `$14.00/mo` and `$25.67/mo` (verbatim, from conftest.py mock_elena_response)
    - Cross-check: `tests/conftest.py` still contains the raw numeric values `16.90`, `30.98`, `14.00`, `25.67` (drift catch — if conftest changed and runbook wasn't updated, this grep fails)
    - Launch commands verbatim: `npm run preview` (primary) AND `npm run preview:mock` (fallback)
    - Teardown has 3 destroy commands, one per stack, in reverse-deploy order (Api, Agent, Foundation)
    - Cross-reference section points at `05-DEPLOY-OUTPUTS.md`, `05-VERIFICATION.md`, `04-UI-SPEC.md`
    - Frontmatter contains `phase: 05-demo-hardening`, `artifact: demo-runbook`
    - File is ≥150 lines (reasonable runbook density)
  </acceptance_criteria>
  <done>
    DEMO-RUNBOOK.md is written, all 6 sections present with real content (not placeholders), persona values for ALL 3 personas sourced from live rehearsal AND cross-checked against conftest.py, teardown commands in correct order, and all cross-references resolve to real files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews presenter cheat sheet wording and approves</name>
  <read_first>
    - .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md (the file just drafted)
    - .planning/phases/05-demo-hardening/05-05-SUMMARY.md (Plan 05 rehearsal notes — any presenter observations to incorporate?)
  </read_first>
  <what-built>
    Task 1 drafted the runbook. This checkpoint exists because D-17 explicitly flags the presenter cheat sheet wording as "Claude drafts, user confirms." The presenter is the one who will read this aloud — they get final say on the exact phrasing.

    Specifically in scope:
    - The "equal-cards talking point" quote in §3 (REC-03 narrative framing)
    - The "what to say" script in §5 (fallback procedure)
    - The one-line narratives for each persona in §3
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/05-demo-hardening/DEMO-RUNBOOK.md` in a reader-friendly view (or `cat` it).
    2. Read §3 (Presenter cheat sheet) top to bottom. For each of the 3 persona narratives, ask: "Would I actually say that in front of a customer?" If not, rewrite in your own voice.
    3. Read §5 (Fallback procedure) aloud. The "what to say" quote should sound natural, not rehearsed. Rewrite if it doesn't.
    4. Verify the equal-cards talking point in §3 reflects how you personally frame the two-track recommendation (REC-03). If it sounds stilted, rewrite.
    5. (Optional) Check the T-24h/T-2h/T-0 checklist timings — are you comfortable with the phrasing and ordering? Adjust if needed.

    Any rewording goes directly into DEMO-RUNBOOK.md — either you edit the file yourself OR paste your rewrites back and Claude edits them in.
  </how-to-verify>
  <resume-signal>
    Type `approved` once:
    1. The §3 cheat sheet wording reads like your voice, not Claude's
    2. The §5 fallback quote sounds natural to say live
    3. The equal-cards talking point matches how you'd introduce REC-03
    4. All timings in §2 are realistic for your workflow

    If you rewrote anything: the changes are already saved in DEMO-RUNBOOK.md before you type `approved`.
  </resume-signal>
  <acceptance_criteria>
    - User has typed `approved`
    - `DEMO-RUNBOOK.md` still exists and still contains all 6 required top-level sections
    - Structural invariants from Task 1 survive the user's edits: all 6 section headings still present, all 3 personas still present with their dollar values unchanged, launch commands still present
    - No part of §3 or §5 contains obvious AI-voice phrasing flagged by the user as unnatural (this is subjective — the `approved` signal is the acceptance)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## 1\. Pre-demo setup' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 2\. Timed checklist (D-19)' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 3\. Presenter cheat sheet' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 4\. Launch commands' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 5\. Fallback procedure' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '^## 6\. Post-demo teardown' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-001' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-002' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'CUST-003' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$30.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$55.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$16.90/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q '\$14.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; grep -q 'npm run preview:mock' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md &amp;&amp; echo "Post-approval structural check: all 6 sections, 3 personas, dollar values, and launch commands still present"</automated>
  </verify>
  <done>
    User has signed off on the cheat-sheet wording and fallback script. Any rewrites are saved. Structural integrity of DEMO-RUNBOOK.md verified after the human edit pass (6 sections, 3 personas with their dollar values, launch commands).
  </done>
</task>

</tasks>

<verification>
Overall runbook checks:

```bash
# File exists with all required sections
test -f .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md
for hdr in "^## 1. Pre-demo setup" "^## 2. Timed checklist" "^## 3. Presenter cheat sheet" "^## 4. Launch commands" "^## 5. Fallback procedure" "^## 6. Post-demo teardown"; do
  grep -q "$hdr" .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md || echo "MISSING: $hdr"
done

# Personas and key values present (all 3 personas, not just flagship)
grep -c 'CUST-00[123]' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md   # Expected: >=3 (one per persona, at least)
grep -q '\$30.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-001 green
grep -q '\$55.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-001 cheapest
grep -q '\$16.90/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-002 green
grep -q '\$30.98/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-002 cheapest
grep -q '\$14.00/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-003 green
grep -q '\$25.67/mo' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md     # CUST-003 cheapest

# Drift catch: conftest.py still contains the raw numbers the runbook references
grep -q '16.90' tests/conftest.py                                            # CUST-002 green monthly
grep -q '25.67' tests/conftest.py                                            # CUST-003 cheapest monthly

# Teardown commands in reverse-deploy order
grep -n 'cdk.*destroy' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md
# Expected lines (in order): CustomerTariffApi, CustomerTariffAgent, CustomerTariff

# Launch commands present
grep -q 'npm run preview:mock' .planning/phases/05-demo-hardening/DEMO-RUNBOOK.md
```
</verification>

<success_criteria>
- `DEMO-RUNBOOK.md` exists in `.planning/phases/05-demo-hardening/`
- All 6 required sections from D-17 present with real content
- T-24h / T-2h / T-0 sub-sections per D-19 present
- Presenter cheat sheet has 3 personas with dollar values sourced from Plan 05 rehearsal AND cross-checked against tests/conftest.py (all 6 monthly values + all 6 annual values verifiable)
- Launch commands reference the exact scripts Plan 03 added (`npm run preview`, `npm run preview:mock`)
- Fallback procedure has "what to say" script and <10s swap steps
- Teardown section has `cdk destroy` commands in REVERSE deploy order
- User has approved presenter cheat sheet wording (Task 2)
- Post-approval structural check confirms all 6 sections, 3 personas, and launch commands survived any human rewrites (Task 2 `<automated>`)
</success_criteria>

<output>
Create `.planning/phases/05-demo-hardening/05-06-SUMMARY.md` recording:
- Path to the runbook
- User approval timestamp for the cheat-sheet wording
- Any rewrites the user made (if recorded in Task 2 dialogue)
- Confirmation that all 3 personas' dollar values in the runbook still match tests/conftest.py after the approval step
- Cross-reference map (which other Phase 5 artifacts the runbook links to)
</output>
</content>
</invoke>